# Contexto del Proyecto: Moto Routes

## Identidad
- **Nombre**: Moto Routes (Ride Tracker)
- **Propósito**: Aplicación móvil para motociclistas que combina navegación GPS, grabación de rutas y bitácora multimedia. Desde la sesión `entorno-api-docker` (2026-08-03), monorepo: app móvil (`apps/mobile/`) + API backend (`apps/api/`).
- **Repositorio**: d:\Git\Otros\moto-routes

## Stack Tecnológico
- **Lenguaje Frontend**: TypeScript 5.7 (strict mode) — `apps/mobile/`
- **Lenguaje Móvil/Desktop backend**: Rust (stable, edition 2021) — `apps/mobile/src-tauri/`
- **Lenguaje API backend**: Go 1.25 (chi router, pgx/v5, golang-jwt, bcrypt) — `apps/api/`. Migrado desde Java/Spring Boot en la sesión `migrar-api-golang` (2026-08-04, ver [[ADR-034]]) — el histórico Java/Maven queda solo en `git log`/ADRs, no en el árbol de trabajo.
- **BBDD API backend**: PostgreSQL 16 (Docker, imagen `postgres:16-trixie`)
- **Bundler**: Vite 6
- **Desktop/Mobile Framework**: Tauri 2
- **UI**: Web Components nativos (Custom Elements v1)
- **Testing Frontend**: Vitest 3 (jsdom, coverage v8) → 80% threshold
- **Testing Móvil backend**: cargo test (unit + integration)
- **Testing API backend**: `go test ./...` (unit + integración real contra Postgres, aislada por schema — `internal/dbtest`)
- **Linting TS**: ESLint 9 (strictTypeChecked + stylistic)
- **Linting Rust**: Clippy (deny warnings)
- **Formatting TS**: Prettier 3
- **Formatting Rust**: rustfmt
- **Git Hooks**: Husky 9 (pre-commit, en orden: `pnpm audit --audit-level=high` + `cargo audit` con excepciones documentadas → ESLint → Vitest → Clippy → rustfmt → cargo test → E2E Cypress vía `pnpm test:e2e`. El paso de auditoría real se cableó en la sesión `auditoria-seguridad` — antes solo estaba documentado en `docs/06-seguridad.md`, nunca implementado. Desde `entorno-api-docker`, todos los comandos de `apps/mobile` se ejecutan con `cd apps/mobile && ...`/`cd apps/mobile/src-tauri && ...`, el hook en sí sigue viviendo en `.husky/` de la raíz. Desde `skip-quality-gates-docs-only` (2026-08-20): si TODO lo staged es documentación (markdown en cualquier ruta, `openspec/**/*.yaml` de tracking o `memory/**/*.jsonl` de métricas), el hook salta directo sin correr nada de lo anterior — mismo patrón replicado en el job `detect-doc-only-change` de `.github/workflows/ci.yml`, que además gatea `quality-ts`/`quality-tauri`/`quality-go`; en un push de tag (`v*`) esas tres jobs corren siempre, ignorando ese output)
- **Package Manager**: pnpm (workspace: `packages: [apps/mobile]`) + Cargo + Go modules (`go.mod`)
- **Security**: CSP estricto, permisos mínimos, sin eval, path validation (app móvil); sin secretos en código en `apps/api` (credenciales de Postgres vía `infra/docker/.env`, no versionado)
- **BBDD Local (app móvil)**: SQLite vía `@tauri-apps/plugin-sql` (archivo: `moto-routes.db`)
- **Docker**: `infra/docker/docker-compose.yml` (servicios `api`+`postgres`, ambos con imagen base Debian 13/trixie — ver [[ADR-032]]), solo para desarrollo local, sin despliegue real todavía

## Herramientas de Desarrollo
- **GitHub CLI**: `gh` (oficial, ya instalado) → issues, PRs, releases
- **rtk**: Proxy de comandos (https://github.com/rtk-ai/rtk) → reduce tokens 60-90% en comandos repetitivos
- **Cline**: Extensión VSCode con DeepSeek como modelo

## Estructura del Proyecto

Monorepo desde `entorno-api-docker` (2026-08-03, ver [[ADR-032]]): `apps/mobile/` (app móvil, todo lo que antes vivía en la raíz) + `apps/api/` (backend Java nuevo) + `infra/docker/` (orquestación local). `package.json`/`pnpm-lock.yaml` de la raíz son solo para la documentación transversal (`docs:*`) y `husky` — `apps/mobile/package.json` es el paquete pnpm real del workspace (`pnpm-workspace.yaml` → `packages: [apps/mobile]`).

```
package.json                  # Raíz: solo docs:* (typedoc/vitepress) + husky, sin deps de la app
pnpm-workspace.yaml           # packages: [apps/mobile]
typedoc.json                  # entryPoints: apps/mobile/src (se queda en la raíz)
scripts/
├── docs-coverage.mjs         # Lee docs/api/coverage.json — se queda en la raíz
└── docs-prepare.mjs          # Copia memory/decisions.md y specs/ui/ a docs/reference/ — se queda en la raíz
apps/
├── mobile/                    # App móvil (Tauri + frontend) — antes en la raíz del repo
│   ├── src/                   # Frontend (TypeScript + Vite)
│   │   ├── app/
│   │   │   └── app.element.ts        # Componente raíz <app-root>, monta <cockpit-view>
│   │   ├── cockpit/                  # Dominio "cockpit" (grabación de ruta)
│   │   ├── routes/                   # Dominio "routes" (listado, detalle, timeline)
│   │   ├── shared/
│   │   │   ├── styles/tokens.css     # Design tokens globales
│   │   │   ├── repositories/         # Repositorios SQLite
│   │   │   └── tauri/commands.ts     # Wrappers tipados para invoke()
│   │   ├── index.css
│   │   ├── main.ts
│   │   └── vite-env.d.ts
│   ├── src-tauri/                    # Backend móvil (Rust)
│   │   ├── src/{main,lib}.rs, commands/mod.rs
│   │   ├── capabilities/default.json # Permisos explícitos (mínimo privilegio)
│   │   ├── Cargo.toml, tauri.conf.json, build.rs
│   │   └── .cargo/config.toml        # Cross-compiler NDK para Android
│   ├── cypress/                       # E2E
│   ├── tests/setup.ts                 # Test setup global Vitest
│   ├── scripts/                       # kill-port.mjs, setup-android.sh, pull-db.{cmd,ps1}
│   └── package.json, tsconfig.json, vite.config.ts, vitest.config.ts, cypress.config.ts, eslint.config.js
└── api/                        # API backend (Go 1.25 — migrado desde Java/Spring en migrar-api-golang, ver [[ADR-034]])
    ├── cmd/api/main.go            # Entry point: router chi, wiring de handlers/middleware
    ├── internal/
    │   ├── auth/                  # Registro/login/JWT/rate-limit, PostgresUserStore
    │   ├── config/                # Config.Load() — lee env vars, sin defaults para secretos
    │   ├── dbtest/                # Helper compartido: aísla cada paquete de test en su propio schema Postgres
    │   ├── httpmw/                # Middleware transversal (Recover, PublicCORS)
    │   ├── migrate/                # Runner de migraciones propio (sin Flyway/Liquibase), embed.FS
    │   ├── migrate/migrations/     # 0001_create_users.sql, 0002_create_stop_types.sql, ...
    │   ├── ping/                   # GET /api/ping — consulta real a Postgres
    │   ├── secretscan/             # Escáner de secretos propio (pre-commit)
    │   └── stoptypes/              # GET /api/stop-types — catálogo de tipos de parada, público
    ├── Dockerfile                  # Multi-stage: golang:1.25-trixie (build) → debian:trixie-slim (runtime)
    ├── go.mod, go.sum
infra/
└── docker/
    ├── docker-compose.yml    # Servicios api + postgres (postgres:16-trixie), solo desarrollo local
    ├── postgres/init.sql     # Tabla dummy `healthcheck`, vía docker-entrypoint-initdb.d/
    ├── .env.example           # Versionado — claves sin valores reales
    └── .env                   # NO versionado (.gitignore) — credenciales triviales de solo dev
specs/features/               # Especificaciones funcionales por feature (histórico SDD, congelado)
specs/api/                    # Contratos de API (SDD histórico — no confundir con apps/api/)
specs/data/                   # Modelos de datos, schemas
specs/ui/
├── design-system.md          # Filosofía visual + design tokens (actualizado)
└── frontend-conventions.md   # Reglas de frontend
openspec/                     # Metodología SDD activa (proposals/specs/design/tasks)
docs/                         # Documentación transversal (VitePress + TypeDoc + cargo doc)
memory/                       # Sistema de memoria persistente
moto-routes-design/           # Referencia de diseño (README, css/, docs/, screens/) — material de
                               # apoyo visual versionado, no consumido por ningún build ni test
```

**`memory/sessions/`**: en la práctica, legacy — contiene un único fichero (`2026-07-26-mejoras-fotos-mapa.md`), de antes de que `memory/context.md` empezara a llevar el resumen de cada sesión inline en "Estado Actual del Proyecto" (patrón usado desde entonces, incluso para sesiones largas — ver p. ej. `optimizar-bundle-produccion` o `rutas-en-la-nube`). No se borra el fichero histórico, pero no se espera que se añadan más — el resumen inline en este mismo fichero es la vía real hoy (decisión documentada en `openspec/changes/limpieza-tecnica-monorepo/design.md`, D2).

## Filosofía Visual: "Asfalto Nocturno"
- **Concepto**: Cuero oscuro, negro asfalto y ámbar cálido. Cuaderno de bitácora, no HUD de competición. **Prohibido**: neón, glassmorphism, azules fríos, diales circulares.
- **Source of truth**: `specs/ui/design-system.md` y `src/shared/styles/tokens.css`.
- **Tokens activos** (`--bg-top`, `--panel`, `--amber`, `--ink`, `--font-display/ui/data`). **No usar** `--color-*`, `--glow-*`, `--neon-*` (eliminados).
- **Modo oscuro obligatorio**: Por seguridad vial, sin modo claro.
- **Hitbox mínima**: 56×56px para uso con guantes de moto.
- **Paleta**: Asfalto/cuero oscuro (`--bg-top`/`--bg-bottom`), ámbar único acento (`--amber`), óxido de apoyo (`--rust-line`).
- **Tipografía**: Roboto Slab (titulares) + Barlow (interfaz) + Barlow Semi Condensed tabular (cifras). Self-hosted en `src/assets/fonts/`.
- **Shadow DOM**: Los estilos globales reales viven en `tokens.css` (importado en cada `*.element.css`). `index.css` solo alcanza DOM ligero.
- **Historial**: Sustituye a "Telemetry & Freedom" (ADR-019). Archivos legacy eliminados (`estilos_base.scss`, `filosofia de diseño.md`).

## Quality Gates
- **Test pass rate**: 100% (frontend + backend)
- **Code coverage (TS)**: 80% (lines, functions, branches, statements)
- **AC coverage**: 100% (cada criterio de aceptación debe tener al menos un test)
- **ESLint**: 0 warnings, 0 errors
- **Clippy**: 0 warnings (deny)
- **rustfmt**: Código formateado
- **cargo audit**: Sin vulnerabilidades conocidas
- **Build**: tsc sin errores + cargo build exitoso + vite build exitoso + tauri build exitoso

## Estado Actual del Proyecto
- **Sesión 2026-08-21 (2) — verificación de la última ruta real en producción usada como fixture de pruebas en Docker local; en el proceso, encuentra y cierra `fotos-rutas-cloud-only` (ADR pendiente: no, detalle de implementación) — deja abierto un segundo gap ya diagnosticado para la próxima sesión**: continuación directa de la sesión `desplegar-osrm-produccion` (más abajo, mismo día). El usuario pidió coger la última ruta real subida a producción ("Rutitas larga", cuenta `gcarmenado@gmail.com`, 2327 puntos con normalización GPS ya aplicada, 3 fotos) y replicarla en Docker local para tener un fixture de pruebas real. Hecho vía SSH+Tailscale al servidor (mismo patrón que ADR-045): `\copy` de `routes`/`route_points`/`route_stops` a CSV, fotos descargadas cifradas de MinIO (`mc`) y descifradas con un script Go puntual (mismo AES-256-GCM que `apps/api/internal/photos/encryption.go`, clave de prod nunca impresa en consola — extraída a fichero remoto vía `ssh ... > file` y traída solo por `scp`, borrada de scratch en cuanto se usó). Datos importados a `docker-postgres-1` local vía SQL directo (preserva `matched_lat`/`matched_lng` tal cual, sin renormalizar) y fotos re-subidas vía la API local (re-cifradas con la clave de dev local) bajo la cuenta de pruebas ya existente `prueba@prueba.com`/`prueba123`.
  **Verificación en dispositivo Android real, con dos causas raíz reales encontradas y corregidas, ninguna relacionada con la importación en sí**: el usuario no veía la ruta en el móvil (`75fe536b`). Primera causa (falsa pista inicial): la app apuntaba a `http://localhost:8080`, que en el dispositivo es el propio teléfono — corregido con `adb reverse tcp:8080 tcp:8080` (túnel USB, sin tocar CSP ni IP LAN, más simple que el patrón de sesiones anteriores de añadir la IP LAN al `connect-src`). En el camino, un intento con IP LAN reveló que el firewall de Windows bloqueaba el puerto 8080 entrante (el usuario creó la regla como admin, `moto-routes-api-dev-8080` — quedó activa aunque al final no fue la vía usada). Causa raíz real: el token de sesión guardado en el dispositivo estaba invalidado para esta instancia de Docker local (401 en `/api/auth/me`) — un simple logout/login en la app lo arregló. **Gotcha de build de Android reencontrado, tal cual documentado más abajo**: `pnpm tauri android build --debug` empaquetó JS desactualizado dos veces distintas en esta sesión — cada vez, copiar `dist/` a mano a `gen/android/app/src/main/assets/` + repetir el build completo lo arregló; los ficheros de `gen/android/` tocados por este proceso se revirtieron con `git checkout --`/`rm` antes de cerrar, no son parte del cambio real.
  **Encontrado durante la verificación: las 3 fotos de la ruta no aparecían en el detalle**, aunque la API sí las servía bien (confirmado por `curl`). Causa: `loadCloudRouteData()` en `route-detail.element.ts` fijaba `this._photos = []` explícitamente para cualquier ruta "cloud-only" (nunca grabada en este dispositivo) — comentario propio en el código decía "sin fotos (fuera de alcance, ver design.md)", una limitación deliberada de `route-cloud-sync` desde su diseño original, no un bug de esta sesión. El usuario pidió cerrarlo de verdad: cambio OpenSpec nuevo `fotos-rutas-cloud-only` (rama `feature/fotos-rutas-cloud-only`), propuesto y aplicado en la misma sesión.
  **Implementación (TDD estricto, 6 grupos de tasks.md, todos en verde)**: `listRoutePhotos`/`downloadRoutePhoto` nuevos en `photo-cloud-api.service.ts` (mismo patrón snake_case→camelCase que el resto del fichero; descarga binaria sin `fetchJson`, igual que `exportRouteGPX`). `loadCloudRoutePhotos()` nuevo en `route-detail-cloud.service.ts`: lista + descarga en paralelo, nunca lanza (degrada a `{ photos: [], error }`, mismo patrón que `loadCloudRouteDetail`). Reutiliza `PhotoWithUrl` en vez de un tipo `CloudPhoto` nuevo — decisión explícita en `design.md` para no ampliar el radio de cambio a `route-map-photos.ts`/visor, con `filePath` sintético inerte (`cloud:${photoId}`) que grep confirmó nunca se derefencia fuera del flujo de ruta local. `route-detail.element.ts`: `loadCloudRouteData()` ahora lanza detalle y fotos en paralelo (`Promise.all`); solo lectura gateada por la bandera `_isLocalRoute` ya existente (sin bandera nueva) tanto en `buildPhotosSection()` (parámetro `readOnly` nuevo, omite `<photo-capture>`) como en los dos `openPhotoViewer()` (`onDelete` condicional vía spread, por `exactOptionalPropertyTypes`).
  **Gap real de proceso, no de producto**: mi primer test E2E del visor dejó un `<photo-viewer>` huérfano en `document.body` (sin `viewer.remove()`) que contaminó 3 tests de un describe posterior en la misma suite (buscaban `[data-cy="photo-viewer-delete"]` y encontraban el viewer viejo sin botón) — arreglado añadiendo la limpieza que le faltaba. Lección: en este fichero de specs, todo test que abre `<photo-viewer>` debe quitarlo explícitamente de `document.body` al terminar, no solo el `<route-detail>` que lo contiene.
  **Verificación real completa**: Vitest 1258/1258 (cobertura 96.9% líneas/90.82% branches, muy por encima del 80%), `tsc --noEmit` limpio, ESLint 0 avisos, Cypress **80/80** contra el backend real (suite completa, no solo el spec nuevo) — incluye `route-cloud-sync-photos.cy.ts` nuevo (2 escenarios: fotos reales vía API+`fetch` con `FormData` real desde `cy.window()`, ruta sin fotos) en fichero aparte porque `route-cloud-sync.cy.ts` ya rozaba `max-lines`. Verificación visual en dispositivo real confirmada por el usuario ("funciona") sobre la propia ruta "Rutitas larga" ya importada. Spec `route-cloud-sync` sincronizada (`Ver el detalle completo de una ruta que solo existe en la nube` ampliado con 4 escenarios de fotos). **Corregido de paso, no planeado**: `vite.config.ts` tenía un error de TS preexistente sin commitear (`hmr: ... | undefined` bajo `exactOptionalPropertyTypes`, WIP del propio usuario sobre `tauri android dev --host`) que bloqueaba cualquier build — arreglado con un spread condicional para poder seguir.
  **Segundo gap encontrado en la propia verificación visual, diagnosticado pero sin implementar — pendiente real para la próxima sesión**: el usuario notó que la miniatura de "Rutitas larga" en el listado parece una ruta vacía (icono de placeholder) aunque el detalle ya funcione. Causa confirmada por código: `route-list-thumb.ts` decide expresamente no calcular `previewPolyline` para `syncState === 'cloud-only'` ("no tiene puntos locales de los que calcular el trazado") — mismo patrón deliberado que el de las fotos, pero en una capability distinta (listado, no detalle). El usuario eligió explícitamente abrirlo como cambio OpenSpec aparte, no ampliar este — **sin proponer todavía**, queda como próximo hito.
  **Cierre**: gate de revisión independiente (releído el diff completo + suite completa re-ejecutada, no solo aceptado el resumen de `apply`) — único hallazgo real, una fuga acotada de `Blob` URL en el caso límite de que una descarga de foto falle mientras otras de la misma ruta ya se resolvieron (`route-detail-cloud.service.ts:230`, `route-detail.element.ts:203`), severidad baja, sin impacto de seguridad ni datos, anotada para una futura sesión sin bloquear. `review.md` con veredicto **APPROVED WITH MINOR ISSUES**. Archivado en `openspec/changes/archive/2026-08-21-fotos-rutas-cloud-only/`. **Sin cerrar todavía**: PR a `master` (disciplina rama+PR del proyecto — nada de esto se ha pusheado). Proponer y aplicar el cambio nuevo para la miniatura de listado cloud-only (ver más abajo). La regla de firewall `moto-routes-api-dev-8080` (Windows, PC del usuario) sigue activa, sin usarse realmente — se puede borrar si se quiere (`Remove-NetFirewallRule -DisplayName "moto-routes-api-dev-8080"`).
- **Sesión 2026-08-21 — `desplegar-osrm-produccion`: cierra el pendiente de OSRM en producción abierto en la sesión `skip-quality-gates-docs-only` (2026-08-20, todavía en su propia rama sin mergear a `master` — por eso su resumen no aparece encadenado aquí, ver nota) — servicio `osrm` nuevo en `docker-compose.prod.yml`, procesado de datos deliberadamente manual**: el usuario, ante la disyuntiva planteada esa sesión (ejecutar `prepare-osm-data.sh` a mano vs. automatizarlo), eligió automatizar la parte de infraestructura vía un cambio de OpenSpec nuevo (`skip_specs: true` — la spec `normalizacion-gps` ya cubre "servicio no disponible" como best-effort, activar la variable no cambia ningún requisito).
  **Nota de proceso**: esta sesión creó `feature/desplegar-osrm-produccion` desde `master` (siguiendo la disciplina de rama+PR del proyecto), pero `master` todavía no incluye el commit que cerró la sesión `skip-quality-gates-docs-only` (vive en `docs/cierre-sesion-2026-08-20`, sin mergear). Su resumen de sesión no está en el histórico de este fichero en esta rama — se recupera leyendo esa otra rama directamente si hace falta.
  **Decisión de diseño real, no anticipada en el `proposal.md` inicial**: `docker-compose.prod.yml` usa `network_mode: host` (igual que `api`, para alcanzar el Postgres nativo del servidor por loopback) — pero `osrm-routed` escucha en `0.0.0.0` por defecto, lo que en modo host lo habría dejado expuesto en las interfaces públicas del servidor, no solo loopback, contradiciendo el propio diseño ("sin publicar el puerto 5000 fuera de loopback"). Corregido añadiendo `--ip 127.0.0.1` explícito al comando — encontrado durante la implementación de la tarea 1.1, no en el análisis previo. Ver [[ADR-053]].
  **Verificación real con obstáculos, resueltos junto al usuario**: Docker Desktop estaba parado al llegar a la tarea de verificación (1.3) — el usuario lo arrancó a petición. Además, `./osrm/data` local ya tenía los datos reales procesados (de la sesión `normalizar-y-exportar-rutas`, 2026-08-19), no vacío como asumía la tarea original — en vez de mover esos datos para simular el caso vacío, se verificó con los datos reales presentes (señal más fuerte: confirma que el servicio nuevo funciona de verdad), y el caso "sin bloquear nada" se confirmó por diseño de Compose (`api` no tiene `depends_on: osrm`). Contenedor de prueba levantado con la config de producción, verificado (`healthy`, `/nearest` responde 200 con match real cerca de Puerta del Sol, log confirma `IP address: 127.0.0.1`), y el entorno de dev restaurado después (`docker compose -f docker-compose.yml up -d osrm`) para no dejar el contenedor de dev sustituido por el de prod.
  **README de `infra/docker/osrm/` reescrito**: la sección "Pendiente: servidor de producción" (narrativa, sin pasos) pasa a ser un procedimiento con pasos numerados (comprobar `free -h`, ejecutar el script, rellenar `MAPMATCH_OSRM_URL`, redeploy normal).
  **Segunda mitad de la sesión — el usuario autorizó acceso SSH real y se activó de verdad en producción, con un giro real de plan**: el usuario cambió la ACL de Tailscale desde el panel web y aprobó la conexión SSH (Tailscale SSH) a mitad de sesión. Al comprobar `free -h` antes de lanzar nada (paso explícito acordado), el servidor real resultó tener solo **5.6 GiB de RAM total** — muy por debajo de los ~11.4 GB que picó `osrm-extract` en local — y aloja además `intercom-signaling`, un servicio de otro proyecto ajeno a moto-routes corriendo en el mismo host. Ejecutar el procesado ahí habría arriesgado un OOM sobre un servicio que ni siquiera es de este proyecto. **Plan sustituido, con el usuario informado antes de proceder**: en vez de reprocesar en el servidor, se copiaron por `scp` los ficheros ya procesados en local (~3.4 GB, sin el `.pbf` ni logs) directamente a `infra/docker/osrm/data/` del servidor — evita el pico de RAM del todo, mismo resultado final. El directorio no existía y `osrm/` pertenecía a `ci-deploy` (checkout de `deploy-local.sh`) sin permiso de escritura para `gonzalo` — creado con `sudo mkdir`+`chown` (sudo sin contraseña disponible para ese usuario, verificado antes de usarlo). Verificado con el servicio real corriendo en el servidor (`healthy`, `/nearest` responde 200, log confirma `IP address: 127.0.0.1`) sin afectar a `docker-api-1` ni a `intercom-signaling-signaling-1`. La copia temporal de `docker-compose.prod.yml` usada para esa prueba se revirtió con `git checkout --` para no dejar el checkout del servidor divergido de git antes de mergear. `MAPMATCH_OSRM_URL` añadido al `.env.prod` real (variable que no existía en ese fichero — predata la feature) sin volcar el resto de secretos a la conversación (solo `grep`/`echo` quirúrgicos sobre esa línea), y `api` recreado — confirmado sano vía `/api/ping` (200, en la IP de Tailscale, no en `127.0.0.1`: `api` está atado a `SERVER_ADDRESS` por diseño, ver [[ADR-044]]). **La normalización GPS está activa en producción desde el cierre de esta sesión.**
  **Hallazgo colateral, sin relación con este cambio, no abordado**: los logs de arranque de `api` muestran `FCM_SERVICE_ACCOUNT_JSON not set` — las notificaciones push de [[ADR-050]] también están inactivas en producción. Pendiente para otra sesión.
  **Cierre**: `openspec validate --strict` sin errores. `proposal.md`/`design.md` actualizados para reflejar lo realmente ejecutado (no solo lo planeado). `review.md` (gate obligatorio del proyecto): veredicto **APPROVED WITH MINOR ISSUES** — único hallazgo, el `osrm` del servidor no está aún bajo control oficial de git/Compose hasta el próximo despliegue tras el merge (autolimitante, sin acción adicional). Archivado en `openspec/changes/archive/2026-08-21-desplegar-osrm-produccion/`. **Próximo hito**: PR a `master` (el servicio `osrm` ya corre en el servidor con una config idéntica a la que traerá el merge — sin downtime esperado en el próximo despliegue automático). Sigue pendiente, sin relación con este cambio: mover la card huérfana del Project a `Hecho` (sin decidir desde la sesión anterior), y el hallazgo de FCM en producción.
- **Sesión 2026-08-20 — `actualizar-mapa-tras-normalizacion`: cierra un hueco real de `normalizar-y-exportar-rutas` (PR #136 aún sin mergear) antes de mergear, y en el proceso encuentra y arregla un bug real de map-matching**: sesión que retoma directamente la pausa de la sesión anterior. El usuario, antes de mergear la PR #136, pidió dos cosas: (1) que la subida a la nube ya normalizada actualizara el mapa del detalle al instante — confirmado que la normalización síncrona ya existía en `Upsert`, pero la respuesta solo devolvía `{id}`, nunca los puntos ajustados, así que el mapa se quedaba con los puntos crudos hasta recargar (y ni siquiera recargar lo arreglaba: una ruta local sincronizada lee de SQLite local, que nunca tiene los valores `matched_*`); (2) confirmar si la validación de "Exportar" (gate `existsOnServer`) seguía haciendo falta — sí, no tiene relación con la normalización, es sobre si la ruta existe en el servidor. Cambio OpenSpec nuevo propuesto (`/opsx:propose`) y aplicado (`/opsx:apply`) en la misma rama `feature/normalizar-y-exportar-rutas` (decisión explícita: es una corrección sobre código aún sin mergear, no una feature nueva independiente).
  **Backend**: `Store.Upsert` cambia de `(ctx, userID, route) error` a `(ctx, userID, route) ([]Point, error)` — `normalizePoints` ahora devuelve los puntos resultantes (originales o con `MatchedLat`/`MatchedLng`) en vez de limitarse a persistirlos. `upsertResponse` gana `points`, mismo shape que `DetailHandler`. Dos call sites reales fuera de `internal/routes` necesitaron actualizarse para compilar (no estaban en el plan inicial, encontrados por el compilador): `routesharing/accept.go` (clonado de ruta al aceptar una invitación) y su doble `fakeRouteStore` en `handler_test.go`.
  **Frontend**: tipo nuevo `UploadedRoutePoint` (`route-cloud-api.service.ts`) — puntos ya resueltos (matched preferido sobre crudo), sin exponer `matched_lat`/`matched_lng` por separado. `uploadRoute`→`uploadRouteToCloud`→`onUploaded` propagan esos puntos hasta `route-detail.element.ts`, que actualiza `_points`/`_routePoints` antes de `render()` — el propio `render()` ya reconstruía el mapa, solo faltaba que los datos estuvieran al día. Síntesis de `id`/`routeId` extraída a `uploadedPointsToLocal()` (`route-detail-cloud.transform.ts`, mismo patrón que `cloudRouteDetailToLocal`), necesaria en parte para no superar el límite de 400 líneas de `route-detail.element.ts` (`eslint.config.js`).
  **Gap encontrado en el propio `tasks.md`, corregido antes de implementar**: la tarea de Cypress E2E decía "puntos de la respuesta del backend fake", contradiciendo la convención ya establecida de este spec (ADR-035, backend real vía `docker compose`) y, además, MapLibre pinta en `<canvas>` — nada localizable por DOM para comprobar el trazado. Test real añadido a `route-cloud-sync.cy.ts`: sube vía UI una ruta con puntos ~15m desplazados de una calle real de Madrid (Calle de Chinchilla, localizada con `/nearest` de OSRM) y comprueba contra el servidor real que `matched_lat` llegó poblado — mismo criterio que `expectSyncedField`, ya usado en ese spec para verificar efectos en segundo plano por su resultado observable.
  **Bug real encontrado por el usuario en la verificación de dispositivo (grupo 7), no relacionado con el alcance original del cambio — corregido en la misma rama con su permiso**: el usuario probó subir una ruta grabada 100% dentro de un edificio y sospechó que la normalización no había funcionado. Cierto: `internal/mapmatch/client.go` (bug preexistente de `normalizar-y-exportar-rutas`, no de este cambio) trataba cualquier status HTTP ≠ 200 de OSRM como fallo de infraestructura *antes* de mirar el body — pero OSRM real responde `HTTP 400` + `{"code":"NoSegment"}`/`{"code":"NoMatch"}` cuando no encuentra ninguna carretera cerca de ningún punto del bloque (verificado reproduciendo la llamada real contra el `osrm` local), un resultado *válido* de map-matching (ver design.md de `normalizar-y-exportar-rutas`, Decisión 7), no un error. El código que sabía interpretarlo (`code != "Ok"` → sin coincidencia) era papel mojado: solo alcanzable si OSRM respondiera 200 con un código de error, cosa que nunca hace. El test que debería haberlo cubierto (`TestClient_Match_NoMatchForWholeChunkLeavesAllPointsUnadjusted`) usaba un doble HTTP con status 200 implícito, sin reproducir el 400 real — lección: **un doble de un servicio HTTP externo debe reproducir sus códigos de status reales para los casos de negocio "válidos pero no exitosos", no solo el body**. Arreglado (`matchChunk` decodifica el body antes de decidir según el status; 400+`NoMatch`/`NoSegment` ya no es error, cualquier otro código con status≠200 sigue siéndolo), con 3 tests nuevos reproduciendo el status real. Verificado tanto el caso corregido (misma ruta del edificio, ya no aparece `map-matching failed` en los logs) como el de no-regresión (la ruta de Calle de Chinchilla sigue ajustando bien).
  **Verificación de dispositivo Android real, parcial, aceptada así por el usuario**: build debug reconstruido dos veces (mismo gotcha ya documentado más abajo — hash `dist/`↔APK desincronizado en la primera vuelta), instalado en `75fe536b`, confirmado servido en el WebView real vía Chrome DevTools Protocol (no solo `unzip`). El propio intento del usuario de probar la subida fue lo que destapó el bug del grupo 7 (con una ruta grabada dentro de un edificio, sin carretera cercana — el caso donde el bug se manifestaba). Sin una segunda ruta en exterior grabada a mano para una confirmación visual final tras el fix, el usuario aceptó la verificación de backend equivalente (mismos puntos reales de Calle de Chinchilla, repetida tras el fix) como suficiente.
  **Suite completa en verde**: Go 239/239 (`govulncheck`/`gofmt`/`go vet` limpios), Vitest 1224/1224 (cobertura ≥80%), `tsc --noEmit` limpio, ESLint `--max-warnings 0` limpio, Cypress 78/78 contra backend real.
  **Cierre**: `openspec validate --strict` sin errores. Sin archivar todavía en esta sesión — **próximo hito**: `/opsx:archive` de este cambio (sincroniza deltas de `normalizacion-gps` y `route-cloud-sync`), y decidir si se suma como commits nuevos a la PR #136 ya abierta (rama compartida, ver sesión anterior) o si se abre una PR aparte.
- **Sesión 2026-08-19 — `normalizar-y-exportar-rutas`: propuesta OpenSpec completa, implementada y verificada de punta a punta en dispositivo Android real (grupo 7 y 9 de `tasks.md` cerrados)**: cambio nuevo, propuesto tras una pregunta exploratoria del usuario sobre cómo montar un servicio de verificación de rutas en el servidor (puntos GPS que "atraviesan edificios" por error de precisión). Recomendación: OSRM self-hosted (servicio `match`, map-matching HMM) frente a GraphHopper Map Matching — más ligero para trazas de moto. Aprobada, propuesta con `/opsx:propose`: `proposal.md`, dos capabilities nuevas (`normalizacion-gps`, `exportacion-gpx`), `design.md` (9 decisiones, [[ADR-051]]) y `tasks.md` (38 tareas, TDD). Implementado con `/opsx:apply` en la misma sesión.
  **Infra OSRM, con un obstáculo real de recursos no anticipado**: extracto de España (Geofabrik, 1.4 GB) descargado y procesado (`osrm-extract`+`osrm-partition`+`osrm-customize`) en local. `osrm-extract` murió por OOM (`exit 137`) con el límite de memoria por defecto de WSL2/Docker Desktop (7.67 GiB) — pico real medido después: 11.4 GB. Subido a 12 GB en `~/.wslconfig` (`wsl --shutdown` + relevantar el stack) con permiso explícito del usuario, ya que reiniciar WSL tumba temporalmente todos los contenedores corriendo. Reintento exitoso. Documentado en `infra/docker/osrm/README.md` y automatizado en `infra/docker/osrm/prepare-osm-data.sh` — **pendiente real para la próxima sesión de despliegue**: repetir este procesado en el servidor (`docker-compose.prod.yml` usa `network_mode: host`, mismo patrón que `MINIO_ENDPOINT` para `MAPMATCH_OSRM_URL=http://127.0.0.1:5000`).
  **Segundo gap encontrado en la propia verificación**: el `healthcheck` inicial del servicio `osrm` en `docker-compose.yml` usaba `wget`, que la imagen oficial `osrm-backend` no trae — sustituido por una comprobación TCP con `bash -c '</dev/tcp/...'` (sí incluido en la imagen).
  **Backend (`apps/api`)**: paquete nuevo `internal/mapmatch` (cliente HTTP propio del `/match` de OSRM, sin SDK — troceado en bloques de 100 puntos con solape de 1, descarte de ajustes a más de 30 m del punto original, todo con tests unitarios contra un `httptest.Server`). Migración `0011_add_route_points_matched.sql` (columnas nullable `matched_lat`/`matched_lng`). `PostgresRouteStore.Upsert` normaliza tras el commit, best-effort (un fallo de OSRM no bloquea el guardado, mismo patrón que el registro de push best-effort de `notificaciones-push-fcm`). `GPXExportHandler` nuevo (`encoding/xml` de la librería estándar, sin dependencia nueva) en `GET /api/routes/{id}/export.gpx`, mismo criterio 404 que `DetailHandler`. Variable de entorno nueva `MAPMATCH_OSRM_URL`, opcional (mismo patrón que `FCM_SERVICE_ACCOUNT_JSON`).
  **Bug real encontrado verificando con el backend real, no con tests**: tras implementar todo, la verificación manual (`curl` a `/api/routes/{id}/export.gpx`) devolvía 404 puro de Go (`net/http`), no el 404 JSON de `DetailHandler` — el contenedor `docker-api-1` seguía corriendo la imagen construida *antes* de todos estos cambios (`docker compose up -d` no reconstruye una imagen ya existente). Resuelto con `docker compose build api && docker compose up -d api`; confirmado end-to-end después: upsert de una ruta real con puntos cercanos entre sí en Madrid → `route_points.matched_lat/matched_lng` poblados con las coordenadas que OSRM realmente devolvió, y el GPX exportado usándolos.
  **Frontend (`apps/mobile`), primera vuelta**: `exportRouteGPX()` nuevo en `route-cloud-api.service.ts` (no usa `fetchJson`, la respuesta es XML). Botón "Exportar GPX" nuevo (`route-detail-export.ts`, mismo patrón de extracción que `route-detail-share.ts`), implementado inicialmente con la Web Share API (`navigator.share`/`canShare`) + fallback a `<a download>` de blob, sin ningún plugin Tauri nuevo.
  **Verificación real con Cypress (backend real, no mocks), con un gap encontrado en el propio test**: `route-gpx-export.cy.ts` fallaba en silencio — el `<a>` sintético de la descarga nunca se adjuntaba al DOM antes de `.click()`, y el Electron headless de Cypress no dispara la descarga de un enlace desconectado del documento. Corregido adjuntándolo a `document.body` antes del click.
  **Verificación en dispositivo Android real (`75fe536b`), tres gaps reales que ningún test detectó — build debug apuntando al backend local vía IP de LAN (`VITE_API_BASE_URL` en `.env.local`, host añadido temporalmente al CSP de `connect-src`, revertido al cerrar)**:
  1. **"Exportar GPX" no hacía nada, sin error**: verificado con Chrome DevTools Protocol (mismo procedimiento ya documentado más abajo para builds obsoletas) que `navigator.share`/`canShare` son `undefined` en el WebView de Android de Tauri — ni la Web Share API ni `<a download>` funcionan ahí (sí en un navegador normal, de ahí que Cypress no lo detectara). Sustituido por `@tauri-apps/plugin-dialog` (`save()`, selector nativo "Guardar como") + `@tauri-apps/plugin-fs` (`writeFile()` sobre la ruta elegida) — primer intento con `writeFile` directo a `downloadDir()` tampoco valía: escribe sin error, pero Android lo resuelve a la carpeta privada de la app (`Android/data/com.motoroutes.app/files/Download/`), invisible para un gestor de archivos normal. Ver [[ADR-052]].
  2. **Sincronizar una ruta con `osrm` parado se quedaba colgada hasta el timeout de 8s del móvil**, en vez de fallar best-effort como decía `design.md`: `mapmatch.Client` en `cmd/api/main.go` nunca tuvo `HTTPClient` con timeout (usaba `http.DefaultClient`, sin límite). Corregido con `http.Client{Timeout: 5 * time.Second}`; reverificado con `osrm` parado de verdad — la ruta se guarda en ~4s, sin normalizar, sin error visible. Ver [[ADR-051]] (Consecuencias, actualizada).
  3. **El propio `docker-api-1` corría una imagen vieja** tras `docker compose up` sin `--build` en más de una ocasión durante esta verificación — recordatorio de que en este proyecto (a diferencia del build de Android, que sí regenera assets solo) hay que reconstruir explícitamente la imagen Go tras cada cambio de backend antes de verificar contra el contenedor real.
  **Rediseño de UI a petición del usuario, tras ver la fila de 4 acciones (favorito/subir/compartir/exportar) con pills de ancho desigual rompiendo mal de línea**: solo iconos, sin etiqueta de texto (`.detail-actions-row .favorite-icon`/`.sync-icon-btn`/`.detail-action__label` eliminados de `route-detail.element.css`, `withLabel()` eliminado de `route-detail-header.ts` — favorito/subida ya comunican su estado con el propio icono, compartir/exportar son universales; cada botón conserva su `aria-label`). Además, "Exportar" ahora abre un menú de formato (reutiliza `confirmDialog`, con solo "GPX" hoy) en vez de exportar directamente — sin rediseñar el botón el día que haya un segundo formato.
  **Verificación final de punta a punta en dispositivo real, completa**: sincronización con puntos normalizados (3/15 de una ruta estática — correcto, no un fallo: el resto era jitter GPS sin movimiento direccional, el umbral de 30 m de la Decisión 7 de `design.md` se abstuvo en vez de forzar un ajuste erróneo), exportación con el selector nativo funcionando, fallback con `osrm` parado confirmado sin errores visibles. Suite completa en verde tras todos los ajustes: Go 231/231 + `govulncheck` limpio, Vitest 1219/1219 (cobertura ~97%), Cypress 77/77, `cargo test`/`clippy`/`fmt` sin cambios (Rust no tocado salvo el registro del nuevo plugin en `lib.rs`).
  **Cierre del cambio**: tres commits (`feat: normaliza puntos GPS de ruta vía OSRM y exporta rutas a GPX`; `test:` puntual — el gate de revisión encontró que el test de normalización con éxito no comprobaba explícitamente que el punto GPS original quedaba intacto tras el `UPDATE` de las columnas `matched_*`, comportamiento ya correcto, solo faltaba la aserción; `docs(openspec): archiva...`). `review.md` con mapeo Requirement→Scenario→test completo (13/13 escenarios, 3 verificados también a mano en dispositivo real), veredicto **APPROVED**. `normalizacion-gps` y `exportacion-gpx` sincronizadas como specs principales nuevas (`openspec/specs/`). Cambio archivado en `openspec/changes/archive/2026-08-19-normalizar-y-exportar-rutas/`. Rama empujada y **PR #136 abierta** ([github.com/crzverde/moto-routes/pull/136](https://github.com/crzverde/moto-routes/pull/136)), sin mergear todavía.
  **Estado exacto al pausar la sesión (2026-08-19, fin de jornada) — retomar desde aquí**: PR #136 abierta, esperando revisión/merge — nadie la ha mergeado todavía. **Sin decidir**: si mover la card del proyecto de GitHub a "En revisión" (pregunta hecha al usuario, sin respuesta antes de pausar — preguntar de nuevo al retomar, no asumir). El stack local (`docker compose`, incluido `osrm` con el extracto de España ya procesado) queda corriendo tal cual — no hace falta rehacer el procesado de OSRM si se retoma pronto en esta misma máquina. Cuenta de prueba local ya creada y verificada: `prueba@prueba.com` / `prueba123`.
  **Próximo hito**: decidir sobre la card del proyecto, esperar/gestionar el merge de la PR #136. Después (o en paralelo, no bloqueante): procesar el extracto OSM en el servidor de producción (`infra/docker/osrm/prepare-osm-data.sh`, solo se ha hecho en local) antes o junto con el despliegue de este cambio.
- **Sesión 2026-08-19 — `agrupar-fotos-proximidad-mapa`: el visor a pantalla completa abierto desde un marcador del mapa muestra solo las fotos GPS-cercanas (<75m), no la ruta entera**: a petición del usuario ("microgestión" sobre una spec ya existente, sin crear spec nueva). Investigación previa (agente Explore) confirmó que ninguna spec de OpenSpec cubría hoy el visor/mapa de fotos — solo `route-photo-storage` (API backend) tocaba "fotos"; el comportamiento vivía únicamente en las specs legacy congeladas (`specs/features/fotos-ruta.md` AC-014/016/017/018, `mejoras-fotos-mapa.md` AC-029), nunca migradas a OpenSpec. Decisiones resueltas con el usuario vía `AskUserQuestion`: alcance solo marcadores de foto (no paradas), agrupación solo por distancia GPS (sin componente temporal), radio fijo **75 metros**, y ampliar `route-photo-storage` en vez de crear spec nueva (aunque su alcance era puramente backend).
  **Ciclo completo `/opsx:propose` → `/opsx:apply` en la misma sesión**, en rama nueva `feature/agrupar-fotos-proximidad` desde `origin/master` (la local estaba desactualizada). Delta spec con 4 escenarios (marcador individual, marcador-cluster, foto sin vecinas, grid/timeline sin cambios).
  **Implementación (TDD estricto, rojo confirmado en cada bloque)**: constante `PHOTO_PROXIMITY_GROUP_RADIUS_METERS = 75` en `route-map-photos.ts`, reutilizando `clusterPhotos()` ya existente (antes solo usada para clustering visual escalado por zoom). El clic en un marcador-cluster dejó de hacer `map.flyTo()` (zoom) y pasa a invocar el mismo callback que un marcador individual — sustituida la regresión AC-017 existente en `route-map.element.spec.ts` por la nueva, con comentario explicando el porqué. Lógica de agrupación extraída a `route-detail-photo-proximity.ts` (función pura `groupPhotosByProximity`), no como método de `route-detail.element.ts`, porque ese fichero ya rozaba el límite de `max-lines` de ESLint y el método nuevo lo hacía saltar — extracción por tamaño, no dominio propio (mismo patrón que `route-detail-notes.ts`).
  **Decisión de diseño encontrada implementando, no anticipada**: el orden "por hora de captura" del grupo se fijó como **más reciente primero**, igual que ya hace `MemoryPhotoRepository.getByRouteId()` para toda la galería/timeline — un test preexistente (`route-detail.element.spec.ts`) asumía ese orden y detectó el desajuste cuando se probó ascendente primero.
  **Bug de test propio, no de producto**: un test nuevo esperaba el contador "1 de 1" en el visor para una foto sin vecinas — `<photo-viewer>` no renderiza el contador cuando solo hay una foto (`buildCounter()` existente, `photos.length <= 1` → `null`); corregido el test para comprobar la imagen mostrada en su lugar.
  **Verificación**: `tsc --noEmit`/`eslint src/ --max-warnings 0` limpios, `vitest run --coverage` 1217/1217 (96.86% líneas/90.73% branches). Verificación manual en Android real (tarea 4.1) confirmada por el usuario. Cypress no re-ejecutado esta sesión (Docker Desktop no levantado) — justificado en `review.md`: ningún spec E2E existente ejercita el clic en marcador de foto con coordenadas (fixtures con `lat`/`lng` `null`), sin riesgo de regresión oculta.
  **Cierre**: gate de revisión independiente pasado antes de archivar — 4/4 escenarios del delta spec mapeados a test, suite completa re-ejecutada de forma independiente. `review.md` con veredicto **APPROVED**. Sin ADR nueva (los dos ajustes reales de `apply` — extracción por `max-lines`, orden del grupo más-reciente-primero — son detalle de implementación, no alcanzan el umbral de `rules.design`). Spec `route-photo-storage` sincronizada (Purpose ampliado a UI de visualización + requisito nuevo con sus 4 escenarios). Archivado en `openspec/changes/archive/2026-08-19-agrupar-fotos-proximidad-mapa/`.
  **Próximo hito**: PR de `feature/agrupar-fotos-proximidad` a `master`, referenciando `review.md` ya archivado.
- **Sesión 2026-08-18 — cierra el gap de `tauri-plugin-notification` (Rust) de la sesión anterior, verificación end-to-end en dispositivo real completada**: continuación directa de la sesión `notificaciones-push-fcm` del día anterior. Añadidas las tres piezas que faltaban: crate `tauri-plugin-notification = "2"` en `Cargo.toml`, `.plugin(tauri_plugin_notification::init())` en `lib.rs`, permiso `notification:default` en `capabilities/default.json`. Build Android debug reconstruido (`pnpm tauri android build --target aarch64 --debug`, con `JAVA_HOME` a `jdk-24` — mismo gotcha documentado más abajo) e instalado en `75fe536b` (`adb install -r`, sin desinstalar — este dispositivo bloquea `adb uninstall`, ya documentado).
  **Segundo gap real encontrado durante la propia verificación, no anticipado**: `docker-api-1` local llevaba la imagen construida el 2026-08-16, antes de que existiera la migración `0010_create_device_tokens.sql` (de la sesión anterior) — el registro del token habría fallado en silencio igual que el permiso, por la misma razón (best-effort try/catch), aunque el permiso ya se concediera. Resuelto con `docker compose build api && docker compose up -d api`; `schema_migrations` confirma `0010` aplicada tras el rebuild.
  **Verificación end-to-end confirmada con la cuenta ya creada** (`notiftest3@example.com`): `dumpsys package com.motoroutes.app` → `POST_NOTIFICATIONS: granted=true` (antes `false`, sin que el diálogo se hubiera disparado nunca); tras logout+login repetido con la API ya reconstruida, `device_tokens` tiene una fila real (`platform=android`, token FCM real) para ese usuario. Ciclo completo login→permiso→token→persistencia verificado de punta a punta.
  **Grupo 7.2 (push real vía FCM) completado en la misma sesión, más tarde**: el usuario sí tenía ya la clave de cuenta de servicio de Firebase (`FCM_SERVICE_ACCOUNT_JSON`) — la había pegado en `infra/docker/.env` el día anterior pero mal formada (el JSON pegado tal cual, multilínea, rompe el formato `.env` de una sola línea `CLAVE=VALOR`; solo `{` se leía como valor). Colapsada a una línea válida sin reescribir la clave a mano (script Node puntual, para no arriesgar una transcripción errónea de la clave privada). **Tercer gap encontrado, del mismo estilo que los dos anteriores**: `docker-compose.yml` (local) nunca reenviaba `FCM_SERVICE_ACCOUNT_JSON` al contenedor `api` en su bloque `environment:` (a diferencia de `docker-compose.prod.yml`, que usa `env_file: .env.prod` con passthrough completo y no tenía el problema) — añadida la línea que faltaba. Push real enviado y confirmado recibido en el dispositivo (invitación real vía `POST /api/route-shares` desde una cuenta de prueba nueva `notif-e2e-sender@example.com`, forzada a verificada por `psql` igual que `notiftest3@example.com`).
  **Bug real de producto encontrado en la propia prueba del push (no un gap de infra)**: el tap de la notificación abría la pantalla de Invitaciones pero vacía — `route-sharing.element.ts` refresca sus datos escuchando `window.addEventListener(APP_EVENTS.VIEW_SHARING, ...)`, pero `app.element.ts` (wiring del tap, sesión anterior) llamaba directamente a `this.onViewSharing()` (método privado que solo hace `showView('sharing')`) en vez de `dispatchAppEvent(APP_EVENTS.VIEW_SHARING)` — nunca disparaba el evento real que la vista de invitaciones escucha para recargar. Corregido en `app.element.ts` (import de `dispatchAppEvent`, sustituida la llamada directa). Sin test nuevo: `src/app/app.element.ts` está excluido de cobertura en `vitest.config.ts` (composition root), mismo criterio ya aplicado al resto del grupo 6.
  **Segundo bug real, más profundo, encontrado al reverificar el fix anterior**: con la app completamente cerrada (no solo en segundo plano), el tap seguía sin navegar — aterrizaba en "Grabar" (vista por defecto). Causa: `MainActivity.onCreate()` nunca comprobaba el extra `open_screen` del intent, solo `onNewIntent()` (por diseño explícito de `design.md` Decisión 5, que asumía la app ya en marcha — cold start no estaba contemplado). Fix aplicado siguiendo el patrón ya existente en el propio código para el mismo tipo de problema (evento nativo que llega antes de que JS esté listo para escucharlo): `MainActivity.onCreate()` deja constancia en `SharedPreferences` (reutiliza `NotificationsPlugin.PREFS_NAME`, nueva clave `PREF_PENDING_TAP_SCREEN`) en vez de reenviar directamente por el canal (que se habría perdido en la misma carrera de arranque que sufría `get_pending_token_refresh`, nunca conectado). Dos comandos Kotlin nuevos (`getPendingTapScreen`/`clearPendingTapScreen`) + su cableado Rust (`NotificationsHandle`, comandos Tauri `get_pending_tap_screen`/`clear_pending_tap_screen` en `commands/mod.rs`, registrados en `lib.rs`) + wrappers TS en `commands.ts`, consultados desde `app.element.ts::init()` justo después de `render()` (para que la vista de invitaciones ya tenga su propio listener montado). Verificado en real: app cerrada del todo → tap → Invitaciones con los datos ya cargados, sin pasos intermedios.
  **Gap de la Decisión 6 (`getPendingTokenRefresh`/`clearPendingTokenRefresh` nunca conectados) cerrado en la misma sesión, a petición explícita del usuario, antes del commit**: mismo patrón que 6.4.2 — cableado Rust (`NotificationsHandle::get_pending_token_refresh`/`clear_pending_token_refresh`, comandos Tauri en `commands/mod.rs`, registrados en `lib.rs`) + wrappers TS en `commands.ts` + `reregisterDeviceTokenAfterRefresh()` nueva en `device-token.service.ts` (con tests unitarios nuevos), consultada desde `app.element.ts::init()` cuando hay sesión activa. Verificado en dispositivo real sin esperar a una rotación real de Firebase: ya había un `pending_token_refresh` genuino en `SharedPreferences` (el primer `onNewToken` también dispara al generarse el token inicial, no solo en rotaciones) — confirmado con `adb shell run-as com.motoroutes.app cat shared_prefs/notifications_prefs.xml` (app debuggable, sin necesidad de root); reiniciar la app lo consumió (`device_tokens.updated_at` avanzó, SharedPreferences quedó vacío).
  **Cierre del commit pendiente**: la rama `feature/notificaciones-push-fcm` llevaba **cero commits** desde que se abrió (dos sesiones completas de trabajo — backend groups 1-3, frontend group 6 — solo en el árbol de trabajo, nunca en git). Commiteado en dos commits el 2026-08-18 (`ad337b4` feature completa, `96628d1` esquemas Tauri desktop/windows regenerados como efecto colateral de añadir `tauri-plugin-notification`), pre-commit en verde ambas veces (audit, ESLint, Vitest, Clippy, rustfmt, `cargo test`, Cypress 75/75). Dos eventos añadidos a `memory/metrics/events.jsonl`: repetición del gotcha ya documentado de Docker Desktop no arrancado (`memory-miss`, cuarta vez), y el propio riesgo de dos sesiones sin commitear (`other`).
  **Cierre completo del cambio**: ADR-050 añadida (Decisión 1 de `design.md`, FCM como transporte/backend propio como dueño de la lógica, con las consecuencias reales de esta verificación incluidas). Gate de revisión obligatorio pasado antes de archivar: `review.md` con mapeo Requirement→Scenario→test completo (4 requirements, 11 escenarios, sin gaps), suite completa re-ejecutada de forma independiente (incluido `go test ./...` — 151/151, que el pre-commit no cubre —, `govulncheck` limpio, Vitest 1199/1199, Cypress 75/75), veredicto **APPROVED**. `notificaciones-push` sincronizada como spec principal nueva (`openspec/specs/notificaciones-push/spec.md`, sin conflicto — no existía antes). Cambio archivado en `openspec/changes/archive/2026-08-18-notificaciones-push-fcm/`.
  **Próximo hito**: PR de `feature/notificaciones-push-fcm` a `master`, referenciando `review.md` ya archivado. Grupo 7.2 completado en esta misma sesión (push real verificado), pendiente real futuro: silenciar/configurar tipos de notificación por ajustes (Non-Goal explícito de este cambio, candidato a spec nueva si hace falta).
- **Sesión 2026-08-17 (4) — `notificaciones-push-fcm`: backend+frontend completos, verificación en dispositivo real interrumpida por un gap real encontrado**: continuación directa de la sesión anterior (exploración FCM + `contador-invitaciones-pendientes`, ya fusionado). El usuario creó el proyecto Firebase y dejó `google-services.json` en la raíz del repo durante la sesión (tarea 0.1 resuelta) — **el JSON de la cuenta de servicio (secreto de backend) todavía no**, así que el envío real desde `apps/api` sigue sin probarse contra Firebase real (los tests con `FCMNotifier` usan un servidor HTTP de pruebas, no Firebase).
  **Completado y verificado con tests esta sesión**: grupos 1/2/3 backend (`device_tokens`, `internal/notifications`, hook en `routesharing`) y grupo 6 frontend (permiso/registro/tap) — 1196/1196 Vitest, Go 100%, `govulncheck` sin vulnerabilidades explotables, `docs:coverage` 70% exacto (fix: `@packageDocumentation` en `notification-tap.service.ts`), Cypress 75/75. Grupo 4 (Kotlin: `NotificationsPlugin.kt`, `FcmService.kt`, manifest, `MainActivity.onNewIntent`) y grupo 5 (Rust: `notifications.rs`, wiring en `lib.rs`) escritos y compilando/buildando en verde (`pnpm tauri android build --target aarch64 --debug`, dos builds limpios, cero warnings el segundo).
  **`google-services.json` — decisión revisada respecto al `design.md` original**: el draft proponía versionarlo (no es secreto según Google). El usuario pidió explícitamente minimizar exposición en el repo público; se optó por `.gitignore` + inyección en CI vía secret `GOOGLE_SERVICES_JSON_BASE64` (mismo patrón que `MOBILE_PROD_API_BASE_URL` en `ci.yml`) — **el usuario ya creó ese secret en GitHub**, confirmado con `gh secret list`. `design.md` actualizado para reflejar esto. `FCM_SERVICE_ACCOUNT_JSON` documentado (vacío) en `.env.example`/`.env.prod.example`, mismo patrón que `RESEND_API_KEY`.
  **Gap real encontrado en la verificación de dispositivo, todavía sin arreglar — punto de partida de la próxima sesión**: la tarea 6.2 se dio por buena con tests unitarios (mocks), pero `@tauri-apps/plugin-notification` solo se añadió como dependencia **npm** — nunca se añadió el crate **Rust** `tauri-plugin-notification` a `Cargo.toml` ni `.plugin(tauri_plugin_notification::init())` a `lib.rs` (confirmado con `grep`, cero coincidencias en ambos ficheros). Sin el lado Rust, `isPermissionGranted()`/`requestPermission()` invocan un comando Tauri que no existe — fallan en silencio, atrapadas por el `try/catch` best-effort de `device-token.service.ts`, así que nunca aparece ni el diálogo del sistema ni ningún error visible. Confirmado en el dispositivo real (`75fe536b`, instalación limpia tras desinstalar manualmente — ver gotcha de ADB más abajo): `dumpsys package` mostraba `POST_NOTIFICATIONS: granted=false` tras un login exitoso, sin que el diálogo se hubiera disparado nunca. Evento registrado en `memory/metrics/events.jsonl` (categoría `rework`, `detected_by: user`). **Siguiente paso inmediato**: añadir la dependencia Rust que falta, reconstruir, reinstalar limpio y repetir la verificación de principio a fin.
  **Cuenta de prueba local creada para la verificación** (solo Docker local, no producción): `notiftest3@example.com` / `TestPassword123`, `email_verified` forzado a `true` a mano vía `psql` contra `docker-postgres-1` (Resend en local no envía emails reales, ver `.env.example`) — reutilizable en la próxima sesión, ya tiene sesión guardada en el dispositivo.
  **Gotcha nuevo de este dispositivo (`75fe536b`), documentarlo para el futuro**: la ROM (Oplus/ColorOS) **bloquea `adb uninstall`/`pm uninstall`** con `DELETE_FAILED_INTERNAL_ERROR` pese a que el `adb install -r` normal funciona sin problema — hay que desinstalar a mano desde Ajustes del sistema. También bloquea `adb shell pm grant <permiso>` sin root (`Neither user 2000 nor current process has GRANT_RUNTIME_PERMISSIONS`) — no sirve como atajo para conceder permisos sin pasar por el diálogo real. Automatizar taps con `adb shell input tap` sin más es frágil en esta app (Shadow DOM + animaciones): mejor sacar bounds exactos con `uiautomator dump` (`grep 'text="X"'`) que estimar coordenadas a mano desde una captura de pantalla — varios taps fallidos esta sesión por error de estimación.
  **Próximo hito**: (1) arreglar el gap de `tauri-plugin-notification` (Rust) — bloqueante para probar nada de esto en dispositivo; (2) repetir el ciclo completo login→permiso→token→`device_tokens` con la cuenta `notiftest3@example.com` ya creada; (3) grupo 7.2 (push real end-to-end, requiere además la clave de cuenta de servicio que el usuario aún no ha dado); (4) grupo 8 (ADR de la Decisión 1 de `design.md`, cierre de este mismo `context.md`); (5) `/opsx:archive` + PR. `tasks.md` ya refleja el estado real de cada tarea (incluida la 6.2.1 nueva, sin marcar, con el detalle del gap).
- **Sesión 2026-08-17 (3) — exploración de notificaciones push + `contador-invitaciones-pendientes` (badge numérico)**: el usuario pidió montar un sistema de notificaciones (caso de uso inicial: aviso al compartir una ruta, con la app abierta o cerrada), preguntando explícitamente si Firebase era obligatorio o si el proyecto podía seguir siendo 100% self-hosted como hasta ahora. Investigación real (`/opsx:explore`, con búsqueda web) antes de proponer nada, sin escribir código.
  **Conclusiones de la exploración, con valor para la próxima sesión que retome el push de verdad**: (1) `tauri-plugin-notification` (el oficial) es solo notificaciones locales, no puede recibir nada del servidor — confirmado. (2) Para que Android despierte la app en background/cerrada hace falta **FCM** (Firebase Cloud Messaging) — es el único mecanismo de sistema real; existen plugins de terceros para Tauri 2 (`tauri-plugin-remote-push`, `tauri-plugin-mobile-push`) pero todos exigen una `FirebaseMessagingService` nativa en Kotlin (territorio ya conocido en este proyecto por `RecordingService.kt` del GPS). (3) La alternativa sin Google, **UnifiedPush**, obliga al usuario a instalar una app "distribuidora" aparte (p. ej. `ntfy`) — descartada para el perfil de usuario real de esta app (uso personal/entre conocidos, no comunidad privacy-focused). (4) Un foreground service propio sin FCM no sobrevive a Doze/background salvo que sea de una categoría protegida (localización) — no es un sustituto viable. (5) No es realmente "FCM vs self-hosted": FCM es solo el transporte/timbre (capa de Google, obligatoria para el wake-up en background); el backend propio (Go/Postgres) sigue siendo dueño de toda la lógica (quién, cuándo, contenido), igual que hoy — decisión enmarcada como "FCM como transporte + backend propio como cerebro", confirmada con el usuario. (6) FCM es gratis sin límite de mensajes y **no exige cuenta de Google Play Developer** (esa cuenta de pago solo hace falta para publicar en Play Store, que este proyecto no usa) — solo una cuenta de Firebase normal (gratis) y que el dispositivo tenga Google Play Services (todos los reales, incluido el `75fe536b` de pruebas). (7) Para mantener el payload realmente opaco (el usuario quiere que Firebase no vea el contenido real), la vía técnica correcta es un **"data message"**, no un "notification message" — el propio código Kotlin decide qué mostrar, sin que Firebase construya la notificación.
  **Decisión de alcance con el usuario**: partir el trabajo en dos cambios — este primero (badge numérico de invitaciones pendientes dentro de la app, sin FCM) y uno futuro más grande para el push de verdad. El modelo de invitación/aceptación de `compartir-rutas` **ya existe tal cual lo pedía el usuario** (invitación pendiente → aceptar clona, rechazar no clona nada) — investigado y confirmado contra `openspec/specs/compartir-rutas/spec.md` antes de proponer nada, para no rediseñar algo que ya estaba bien.
  **`contador-invitaciones-pendientes` implementado**: `hasPendingReceivedInvitations()` pasa de `boolean` a `number` (recuento real, sin endpoint nuevo — ya se tenía el array completo); `buildSharingButton()` añade un badge numérico (1-9 exacto, "9+" por encima, sin badge con 0), reutilizando el patrón de posicionamiento de `.favorite-icon--badge` ya existente. Capability modificada `compartir-rutas` (delta spec). Sin ADR, sin `design.md` (cambio demasiado pequeño para justificarlo). TDD estricto: `tsc --noEmit`/`eslint` limpios, Vitest 1185/1185 (96.89%/95.21%/90.70%), **`pnpm run docs:coverage` comprobado a propósito esta vez** (70% exacto, sin bajar) tras el hallazgo de CI de la sesión anterior, Cypress completo 75/75 (extendido un test ya existente de `route-sharing.cy.ts` que ya enviaba 2 invitaciones reales, en vez de crear uno nuevo).
  **Próximo hito**: `/opsx:archive` + PR de `feature/contador-invitaciones-pendientes` a `master`. Después, cambio nuevo y más grande para el push real vía FCM (registro de token de dispositivo, endpoint de backend, `FirebaseMessagingService` en Kotlin) — la investigación de esta sesión ya deja el terreno resuelto, no hace falta repetirla.
- **Sesión 2026-08-17 (2) — `mejoras-listado-rutas`: filtros local/nube, buscador por nombre y orden en el listado de rutas**: a petición del usuario ("spec muy chiquitita"), que resultó tener 4 decisiones de diseño reales resueltas por `AskUserQuestion` antes de proponer (todas la opción recomendada): filtro de favoritas de texto a icono, filtros local/nube como dos toggles independientes combinables, búsqueda en vivo solo por nombre, orden Fecha/Nombre sin control asc/desc aparte. Nueva capability `listado-rutas` (`openspec/specs/`), sin `skip_specs`.
  **Dos gaps reales encontrados implementando, ninguno anticipado en design.md**: (1) `route.name` es `string | null` — el buscador y el orden por nombre debían comparar contra `buildRouteDisplayName(route.name, route.createdAt)` (lo que el usuario ve realmente en cada tarjeta), no contra el campo en crudo, o una ruta sin nombre propio nunca aparecería en una búsqueda por su nombre visible. (2) Un campo de búsqueda con filtrado en vivo no puede disparar `render()` completo en este componente: `BaseElement.renderShadow()` hace `innerHTML = ''` y reconstruye todo el Shadow DOM en cada llamada, lo que destruiría y recrearía el propio `<input>` en cada tecla — perdiendo el foco a partir del segundo carácter escrito. Resuelto con una actualización parcial (`updateBodyOnly()`) que sustituye solo la sección de resultados, dejando el input intacto. Test de regresión dedicado (`route-list.element.spec.ts`) para que no se reintroduzca sin darse cuenta.
  **`route-list.element.ts` no bastó con la extracción prevista en design.md** (`route-list-controls.ts`, la fila de iconos/buscador/orden) — seguía por encima de `max-lines`. Se extrajeron también `route-list-body.ts` (estados vacíos + selección de cuál mostrar) y `route-list-thumb.ts` (miniatura + backfill perezoso, código preexistente sin relación con este cambio, movido tal cual). Quedó en 0 warnings de ESLint.
  **Verificación real**: TDD estricto en cada grupo de tareas (rojo confirmado antes de implementar). `tsc --noEmit` limpio, `eslint src/ --max-warnings 0` limpio, `vitest run --coverage` 1182/1182 (96.93%/95.20%/90.80% statements/functions/branches). Cypress nuevo dedicado (`route-list-filtros-busqueda.cy.ts`, 6/6 contra backend Docker real) + suite completa **75/75** sin regresiones. **Verificación visual en navegador no posible** (extensión Claude-in-Chrome no conectada, mismo bloqueo ya documentado en `limpieza-tecnica-monorepo`) — sustituida por garantía de construcción (aritmética de anchos a 390px) y por la cobertura estructural de Cypress.
  **Cierre**: sin ADR nueva (ninguna decisión alcanza el umbral de `rules.design`/ADR-048, confirmado también tras implementar — ningún hallazgo de los dos gaps de arriba cambia esa conclusión, son detalles de implementación, no decisiones con alternativas reales evaluadas). Archivado, `review.md` con veredicto **APPROVED**. PR #131 abierta.
  **Hallazgo real de CI, no visto en local — buen ejemplo de por qué el gate de review no sustituye a ver el pipeline en verde**: `Quality gates — TypeScript` falló en la cobertura de documentación TypeDoc (`scripts/docs-coverage.mjs`, umbral 70%, cayó a 69%) — ese gate **no vive en `.husky/pre-commit`**, solo en CI, así que pasar el pre-commit local completo no lo detecta. Las 5 nuevas interfaces/propiedades de `route-list-controls.ts` (`ControlsRowOptions`, `SearchSortRowOptions`) no tenían JSDoc individual (ESLint `jsdoc/require-jsdoc` solo exige doc en funciones/clases exportadas, no en cada propiedad de una interfaz). Corregido documentándolas, cobertura a 70% exacto. Evento registrado en `memory/metrics/events.jsonl` (categoría `gate-bypass`) — primer caso real de uso del sistema de métricas recién creado, útil de inmediato. PR #131 fusionada tras el fix.
  **Verificación en dispositivo real**: build release completo vía `scripts/install-android.sh` (con `JAVA_HOME` apuntando a `jdk-24`, gotcha ya documentado), instalado en `75fe536b`. Verificado con el método fiable ya documentado (comparar hash `index-*.js` entre `dist/` y el APK vía `unzip`, no solo confiar en "build terminado sin error") — coincidían, APK con el código real. Usuario confirmó "todo perfecto" tras probarlo.
- **Sesión 2026-08-17 — repaso de PRs externas pendientes (#96, #91) + `metricas-sdlc-agente`: log de fallos del SDLC (ADR-049)**: sesión con dos partes independientes.
  **Parte 1 — PRs #96/#91 de otro colaborador (`viictoryraves`), abiertas desde hacía semanas sin fusionar**: ambas revisadas y correctas en contenido, pero desactualizadas frente a `master` (PR #96 del 2026-08-05, PR #91 del 2026-08-03 — 14+ ADRs y varios cambios de por medio). PR #96 (README del monorepo) rebasó sin conflicto real, solo reposicionar su entrada de `context.md` cronológicamente. PR #91 (criterio explícito de qué merece ADR, la propuesta detrás de la regla nueva de `rules.design`) sí colisionaba: proponía su propia "ADR-034", número ya ocupado por `migrar-api-golang` desde entonces. Rebasada, renumerada a **ADR-048** (siguiente libre), con la propia ADR corrigiendo su rango de inmutabilidad (de "ADR-013 a ADR-033" a "ADR-013 a ADR-047") para seguir siendo cierta tras el rebase. Ambas con CI en verde, fusionadas (`f5feff0`, PR #96; PR #91) sin tocar más contenido técnico que el propio del autor original.
  **Parte 2 — `metricas-sdlc-agente` (ADR-049), a petición directa del usuario**: quiere poder responder "dónde falla nuestro SDLC al trabajar con un agente" — no consumo de tokens, no productividad, no KPIs de tablero. Esta primera spec es solo el plan de recopilación de datos; el análisis real es un cambio futuro aparte, tras acumular datos varios días. `skip_specs: true` (instrumentación de proceso, no capacidad de la app).
  **Diseño**: log nuevo `memory/metrics/events.jsonl` (JSON Lines, append-only, esquema documentado en `memory/metrics/README.md`), taxonomía cerrada de 5 categorías con válvula de escape (`memory-miss`, `gate-bypass`, `scope-violation`, `spec-drift`, `rework`, `other`), campo `detected_by` (`self`/`user`) pensado para medir en el análisis posterior el sesgo de que sea el propio agente quien decide si se equivocó. Sin gate técnico — mismo criterio consciente ya sentado en [[ADR-029]]/[[ADR-030]] (disciplina documentada, no forzada). Regla nueva en `CLAUDE.md` (se lee siempre, no solo dentro de `/opsx:*`) + una entrada nueva en `operations.archive.guidance` de `openspec/config.yaml` para registrar `spec-drift`/`gate-bypass` detectados al archivar.
  **Log sembrado con un evento real de esta misma sesión, no solo teórico**: uso de `ScheduleWakeup` (herramienta reservada a modo `/loop`) fuera de ese contexto para esperar el CI de la PR #96 — categoría `other`, primer caso real que valida que la taxonomía tiene una válvula de escape que funciona.
  **Próximo hito**: cerrar con `/opsx:archive` (sin código de aplicación que revisar, review más corta de lo habitual) y PR de `feature/metricas-sdlc-agente` a `master`. Después, dejar correr el log unos días de trabajo normal antes de proponer el cambio de análisis.
- **Sesión 2026-08-16 (2) — `auditoria-tecnica-2026-08`: auditoría técnica amplia (seguridad, vulnerabilidades, optimización, reutilización)**: a petición explícita del usuario, tras preguntarle si merecía la pena dado el tiempo/código desde la última auditoría (`limpieza-tecnica-monorepo`, 2026-08-12) — sí, ~4 días y 5 cambios sustanciales, incluido `sistema-logros` (paquete backend entero nunca revisado). Antes de proponer: 3 agentes en paralelo (seguridad, optimización, duplicación) + `pnpm audit`/`cargo audit`/`govulncheck` re-ejecutados.
  **Resultado honesto de la investigación**: sin hallazgos bloqueantes de seguridad, vulnerabilidades ni rendimiento — SQL parametrizado en `achievements`, auth correcta en las ~20 rutas de `main.go`, índices ya presentes (`idx_routes_user_id` cubre la query de agregados), sin dependencia nueva desde `v0.1.7`, gate de tamaño de APK intacto. Las 2 vulnerabilidades que reporta `cargo audit` (`RUSTSEC-2026-0235` rkyv, `RUSTSEC-2023-0071` rsa) siguen siendo excepciones ya documentadas en `.husky/pre-commit`, sin novedad. Solo 3 hallazgos reales, ninguno bloqueante — se convirtieron en el propio cambio (con spec delta, porque uno de los 3 sí cambia comportamiento observable, así que no fue `skip_specs`).
  **Hallazgo real durante la implementación, no anticipado por el agente de auditoría**: el agente de duplicación afirmó que las 3 implementaciones de `buildBackButton()` (`route-detail`, `route-sharing`, `achievement-list`) eran "casi idénticas, solo cambia `data-cy` y evento" — al extraerlas de verdad se descubrió que no era cierto: `route-detail` tenía una clase `back-btn__arrow` con su propia regla CSS (flecha más grande) que las otras dos no tenían, y **`route-detail` no tenía `data-cy` en absoluto** en su botón de volver (incumplía la regla obligatoria del proyecto, sin que ningún test de Cypress lo hubiera detectado porque nada lo seleccionaba todavía). El helper unificado conserva la clase siempre (inocua donde no hay CSS para ella) y añade el `data-cy` que faltaba — verificado por grep que ningún test dependía de su ausencia antes de tocarlo.
  **Extracciones**: `buildBackButton()` → `shared/back-button.ts` (3 sitios); trío Go `writeJSON`/`writeError`/`requireUserID` → `internal/apihttp/` nuevo (4 paquetes: `achievements`, `routes`, `routesharing` — incluyendo `accept.go`, no solo `handler.go` — y `photos`; `internal/auth` se dejó fuera a propósito, ver design.md). Más accesibilidad de teclado (focus-trap + ESC) en `achievement-unlock-overlay.element.ts`, mismo patrón ya usado en `confirm-dialog.element.ts`.
  **Verificación real end-to-end**: `go test ./...` 194/194 (15 paquetes, incluye `apihttp` nuevo); `vitest run --coverage` 1153/1153 (96.87% líneas); Cypress completo 69/69 tras reconstruir el contenedor Docker del backend con el código Go nuevo (sin esto, Cypress habría probado un binario desactualizado); `pnpm audit`/`cargo audit`/`govulncheck` re-ejecutados sin hallazgos nuevos.
  **Próximo hito**: commit + PR de `feature/auditoria-tecnica-2026-08` a `master`.
- **Sesión 2026-08-16 — `sistema-logros`: sistema de logros/achievements (backend + animación de desbloqueo + pantalla "Mis logros")**: ciclo OpenSpec completo (`propose`→`apply`) en una sola sesión, a petición directa del usuario. Antes de proponer, `AskUserQuestion` resolvió las decisiones de diseño clave (todas eligiendo la opción recomendada): evaluación disparada por el cliente (no backend/cron), solo rutas ya sincronizadas cuentan, logros permanentes (nunca se revocan), mes natural (no ventana móvil), catálogo inicial propuesto por Claude, logros múltiples en cola.
  **Correcciones reales de premisa, encontradas investigando antes de escribir código** (documentadas en `design.md`/`specs/logros/spec.md`): (1) no existe ningún auto-sync al parar de grabar — la subida es manual (`route-detail-cloud-upload.ts`) o auto-resync tras editar (`route-detail-sync-triggers.ts`), ambos convergen en `uploadRouteToCloud()`, único punto real de "sync confirmado" — ahí se enganchó la comprobación de logros, confirmado con el usuario vía `AskUserQuestion` antes de implementar. (2) `routes.created_at` es `TEXT` libre sin validar (no `TIMESTAMPTZ`) — la query de agregados usa una función `plpgsql` nueva (`safe_parse_timestamptz`, en la propia migración) que aísla el fallo a nivel de fila en vez de romper todo el cálculo. (3) `routes.duration` está en segundos, no minutos — corregido el nombre del tipo de requisito (`single_route_duration_seconds`) y el umbral (3600, no 60) en migración/design.md antes de implementar. (4) No existe ningún prompt de "inicia sesión" reutilizable en el repo (ni siquiera en `route-sharing`) — se corrigió `specs/logros/spec.md` para no asumirlo, y se construyó uno nuevo mínimo.
  **Backend**: migración `0009_create_achievements.sql` (tablas `achievements`/`user_achievements`, `BIGSERIAL`+`key` como `stop_types` en vez de UUID — design.md corregido para reflejarlo), paquete `internal/achievements` (4 ficheros, mismo patrón que `routesharing`: agregados en una sola query con `FILTER`, `CheckAndGrant` idempotente vía `UNIQUE`+`ON CONFLICT DO NOTHING`), 2 endpoints (`POST /api/achievements/check` otorga, `GET /api/achievements` lee), wiring en `main.go` con CORS+`OPTIONS` explícito. 190/190 tests Go.
  **Frontend**: tipos+cliente API (`achievement-api.service.ts`), hook fire-and-forget en `route-detail-cloud.service.ts` (bug real encontrado por el propio test: `granted.forEach(enqueueAchievementUnlock)` pasaba `(index, array)` de más por cómo `Array.prototype.forEach` invoca el callback — corregido a arrow function), overlay de desbloqueo con cola en memoria (`achievement-unlock-overlay.element.ts`, respeta `prefers-reduced-motion` vía el override global de `tokens.css`, mismo patrón de test que `route-map.element.css.spec.ts`), pantalla nueva `<achievement-list>` (dominio `src/achievements/`), evento nuevo `VIEW_ACHIEVEMENTS` en `app-events.ts`. 1148/1148 tests Vitest, 96.88% líneas/90.84% branches. `tsc`/`eslint --max-warnings 0` limpios. Cypress 69/69 (incluye 3 tests E2E nuevos con un logro de test sembrado por SQL, ya que el catálogo real tiene umbrales demasiado altos para un E2E).
  **Verificación en dispositivo real (75fe536b), con dos gotchas reales de entorno, ninguno del código de la feature**: (1) build `release` falló con `JavaVersion.parse` en `25.0.2` — mismo gotcha ya documentado más abajo en este fichero (JBR de Android Studio autoactualizado), resuelto apuntando `JAVA_HOME` a `jdk-24`. (2) el build `release` tiene `usesCleartextTraffic="false"` (bloquea HTTP) — contra un backend local (`adb reverse tcp:8080`, sin HTTPS) hace falta el build `--debug` (`usesCleartextTraffic="true"`); `release` solo puede probarse así contra el backend real HTTPS. **Hallazgo de UX real, corregido en el momento tras la prueba del usuario**: el botón "Mis logros" era un botón de texto plano al final de la pantalla de perfil, "perdido y sin icono" — rediseñado a una fila con icono+chevron justo debajo de la tarjeta de identidad (`profile-achievements-link.ts`), reinstalado y confirmado ("brutal"). Animación y otorgamiento confirmados también por consulta directa a `user_achievements`.
  **Cierre**: revisión independiente completa antes de archivar (mapeo Requirement→Scenario→test de los 9 requirements/20 escenarios de `specs/logros/spec.md`, sin gaps bloqueantes — el único hueco real encontrado, "sin logros nuevos" sin test dedicado en frontend, se cerró en la propia revisión). `review.md` con veredicto **APPROVED**. Delta spec sincronizada como capability nueva `openspec/specs/logros/spec.md`. Archivado en `openspec/changes/archive/2026-08-16-sistema-logros/`. Sin ADR nueva (confirma la conclusión de `design.md`).
  **Regresión real encontrada por el propio pre-commit al comitear, no vista en la verificación manual previa**: el rediseño del acceso a "Mis logros" (fila nueva entre la tarjeta de identidad y "Mi vehículo") empuja el estado vacío de "Estadísticas" fuera del viewport inicial (390×844, tamaño móvil de Cypress) en el perfil recién creado sin rutas — `cypress/e2e/perfil/perfil.cy.ts` fallaba (`profile-stats-empty` no visible, "clipped by... overflow"). No es un fallo de la propia feature: `should('be.visible')` de Cypress no hace scroll por sí solo, a diferencia de una interacción real de usuario (que sí puede desplazarse). Arreglado añadiendo `.scrollIntoView()` explícito antes de esa aserción concreta, con el motivo documentado en el propio test — no se relajó ni se quitó la aserición. Cypress completo re-verificado 69/69 tras el fix.
  **Fallo real de CI, no visto ni en local ni en el pre-commit — buen ejemplo de por qué el gate de review no sustituye a ver el pipeline real en verde**: `route-cloud-sync.cy.ts` falló en CI (`cy.click()` en `nav-rutas` bloqueado por `achievement-unlock-overlay` cubriéndolo). Causa raíz real: el propio test E2E de `sistema-logros` sembraba un logro de test (`route_count >= 1`, el umbral más bajo posible) directamente en la tabla **compartida** `achievements` — cualquier cuenta de prueba de cualquier otro spec que subiera aunque fuera una sola ruta lo desbloqueaba. Coincidió además con que la ruta de fixture de `route-cloud-sync.cy.ts` usaba `duration: 3600`, el mismo umbral exacto del logro real "Ruta larga" — combinación que no reprodujo en 2 corridas completas en local (máquina más rápida, la animación de 4s ya había auto-cerrado antes del siguiente click) pero sí en el runner de CI, más lento. No reproducía tampoco con una BBDD local recién limpiada hasta forzar el mismo orden de specs — el diagnóstico real vino de leer el log crudo de CI (`gh run view --log`), no de adivinar. Arreglado de raíz: el test de logros dejó de mutar la tabla `achievements` (usa directamente el logro real "Ruta larga"), y `route-cloud-sync.cy.ts` bajó su duración de fixture a 1800s para no cruzar ningún umbral real de forma incidental. Cypress completo re-verificado 69/69 con Postgres recién limpiado (igual que CI) antes de repushear.
  **Commit y PR**: dos commits (feature completa + fix del flake de CI), ambos con el pre-commit completo en verde. PR #126 abierta contra `master`, referenciando `review.md`; CI en verde tras el segundo commit; fusionada (`3a01bdb`).
- **Sesión 2026-08-16 (previa, mismo día) — fix puntual: nuevo icono de la app (Android)**: regenerados todos los tamaños de icono con la CLI de Tauri (`pnpm tauri icon`) a partir de un único source PNG cuadrado (`src-tauri/app-icon.png`), incluidos los mipmaps adaptativos de Android. Rama `fix/icono-app-android`, PR #125 fusionada a `master` (`4fc3fa5`) tras CI en verde.
- **Sesión 2026-08-15 — fix puntual: preflight CORS de `route-shares` + reubicación del icono de favorito**: sin cambio OpenSpec abierto (bug real detectado tras el cierre de `compartir-ruta`, ver `CLAUDE.md` — un fix puntual sigue el mismo patrón rama+PR sin necesitar `/opsx:propose`). Dos arreglos independientes en el mismo PR.
  **Bug real (backend)**: `/api/route-shares/received` y `/api/route-shares/sent` nunca tuvieron handler `OPTIONS` explícito registrado en `main.go`, a diferencia del resto de rutas de `route-shares` — chi no lo genera solo. El preflight CORS del WebView recibía `405` en vez de `204`, bloqueando el `GET` real: las pestañas Recibidas/Enviadas de `route-sharing` se veían vacías sin ningún error visible. Mismo tipo de gotcha ya visto antes en otros endpoints (`ede1313`, `7076e96`). Arreglado registrando el `OPTIONS` que faltaba.
  **Refactor (frontend)**: el icono de favorito, añadido en `favoritos-rutas`, competía por espacio con otros iconos apretados. Listado: pasa de compartir esquina con el botón de borrar en cada card a un badge sobre la miniatura (mismo patrón que el badge de sincronización ya existente). Detalle: sale de la fila del título (donde competía con nube y compartir) a su propia fila de acciones con etiqueta, debajo de la fecha.
  **Verificación**: CI en verde tras el merge (`gh run list` sobre `master` limpio).
  **Commit y PR**: commit único `fix(api): registrar preflight OPTIONS...` + `refactor(mobile): reubica el icono de favorito...`. PR #123 fusionada a `master` (`825f47e`).
  **Próximo hito**: ninguno pendiente de esta sesión.
- **Sesión 2026-08-14 (2) — `compartir-ruta`: invitación directa entre cuentas (por email) para recibir una copia completa de una ruta ya sincronizada**: continuación directa del cierre de `favoritos-rutas` en la misma sesión, a petición del usuario ("dale" tras revisar el resumen de la propuesta). Implementa la spec pendiente que había quedado anotada desde `favoritos-rutas`: compartir = invitación directa (no enlace público), clonado completo al aceptar, ciclo `/opsx:propose` → `/opsx:apply` completo en una sola sesión.
  **Decisiones de alcance, todas por AskUserQuestion antes de proponer (el usuario eligió la opción recomendada en las tres)**: identificar al destinatario por email (no username, evita construir un sistema de búsqueda nuevo); el clonado incluye fotos además de metadatos/puntos/paradas; el emisor ve el estado de sus invitaciones enviadas y puede revocar una pendiente.
  **Backend**: paquete nuevo `internal/routesharing`, migración `0008_create_route_shares.sql` (tabla `route_shares`, `to_user_id` resuelto a un id en el momento de crear la invitación, nunca se guarda el email). Reutiliza al máximo lo ya existente: `routes.Store.GetByIDForUser`/`Upsert` para el clonado (mismo statement que el upsert normal del cliente), `auth.UserStore.FindUserByEmail` + el patrón de respuesta genérica anti-enumeración ya establecido en `request_password_reset.go`, `auth.LoginRateLimiter` reutilizado tal cual para limitar invitaciones repetidas. Clonado de fotos: copia el blob cifrado byte a byte a un `ObjectKey` nuevo sin descifrar/recifrar (la clave AES es única para toda la instalación, no por usuario). `MarkAccepted`/`MarkDeclined`/`MarkRevoked` son `UPDATE ... WHERE status = 'pending'` atómicos (evitan doble-aceptado por condición de carrera) que devuelven el mismo error genérico "no encontrado" tanto si la invitación no existe como si pertenece a otra cuenta. 12 paquetes Go en verde (26 tests nuevos en `routesharing`).
  **Frontend**: dominio nuevo `src/routes/sharing/` (pantalla `<route-sharing>` con pestañas Recibidas/Enviadas vía `<tab-bar>` compartido), botón "Compartir" nuevo en `route-detail` (diálogo `route-share-dialog.element.ts`, mismo patrón de dos pasos que `auth-forgot-password-dialog.element.ts`, con validación en cliente de "no compartir contigo mismo"), icono de acceso nuevo en la cabecera de `route-list` con indicador de pendientes. La ruta clonada no necesita ningún mecanismo de sincronización nuevo: llega al listado del destinatario como cualquier "ruta exclusiva de la nube" ya soportada por `route-cloud-sync`.
  **Corrección real durante `apply` (no anticipada en el diseño)**: el enunciado de `tasks.md` decía gatear "Compartir" igual que "Subir a la nube" (`isSynced`), pero ese flag es `false` para una ruta exclusiva de la nube (nunca local) — habría ocultado la acción justo en el caso que más la necesita. Añadido `existsOnServer = isSynced || !isLocalRoute` en su lugar, con tests dedicados a los 3 casos.
  **Dos bugs reales encontrados escribiendo el propio Cypress E2E, ninguno anticipado**: (1) `route-sharing.element.ts` solo cargaba datos una vez, al arrancar la app (normalmente sin sesión) — nunca se refrescaba al reabrir la pantalla ya logueado; arreglado con un listener del evento `view-sharing` nuevo, mismo patrón que `route-list` ya usaba para `nav-rutas`. (2) Condición de carrera real: el setter de `repository`/`sessionRepository` y `connectedCallback` ya disparaban la carga dos veces al arrancar (sin sesión) en `route-list.element.ts` y `route-sharing.element.ts` — si esas llamadas seguían en vuelo cuando la navegación real disparaba una tercera (con sesión), la más lenta podía resolver la última y pisar el estado correcto con datos obsoletos; arreglado con un token/época por componente (solo la última llamada iniciada confirma su resultado). Dos tests del E2E se sincronizan con `cy.intercept`+`cy.wait` sobre la petición real en vez de confiar en el polling implícito de `.should()` (insuficiente bajo la carga de Docker+Cypress de este entorno, mismo tipo de gotcha ya visto y documentado en `favoritos-rutas`, aquí con una causa distinta).
  **Alcance explícitamente reducido, documentado en tasks.md**: el clonado de fotos no se re-verifica en el propio Cypress (evita construir una petición multipart dentro del test) — ya cubierto con fakes reales de `routes.Store`/`photos.PhotoStore`/`photos.BlobStore` en `accept_test.go` del backend.
  **Verificación real end-to-end**: `go test ./...` 12/12 paquetes; `tsc --noEmit`/`eslint --max-warnings 0` limpios; `vitest run --coverage` 1121/1121 (96.75% líneas / 90.83% branches); Cypress E2E nuevo 6/6 (dos veces seguidas para confirmar estabilidad tras los dos arreglos de condición de carrera) + suite completa 66/66 sin regresiones. Verificación manual en dispositivo real (compartir, ver invitación en la otra cuenta, aceptar, fotos incluidas) confirmada por el usuario sin hallazgos.
  **Cierre**: revisión independiente completa antes de archivar — mapeados los 8 requirements/23 escenarios del delta spec contra sus tests, encontrado y cerrado un hueco real de cobertura ("aceptar sin conexión" sin test, backend ya lo manejaba correctamente) antes de archivar. `review.md` con veredicto **APPROVED**. Delta spec sincronizada como capability nueva `openspec/specs/compartir-rutas/spec.md`. Archivado en `openspec/changes/archive/2026-08-14-compartir-ruta/`. Sin ADR nueva — ninguna decisión de este cambio alcanza el umbral de decisión arquitectónica (todo reutiliza patrones ya existentes: anti-enumeración de `password-reset`, rate limiter de `auth`, mecanismo de "ruta exclusiva de la nube" de `route-cloud-sync`).
  **Commit, PR y fusión**: commiteado (`0fbc2e9`) con el pre-commit completo en verde (incluye Cypress 66/66). PR #121 abierta contra `master`; el primer run de CI encontró un hallazgo real (no local): `gofmt --check` fallaba en `internal/routesharing/accept_test.go` (alineación de campos en un literal struct — el propio `gofmt -l .` local no lo distinguía entre los muchos falsos positivos por CRLF de Windows, hubo que mirar el diff real fichero a fichero). Corregido con `gofmt -w` (commit `524f0e7`), segundo run limpio, PR #121 fusionada (`36ec006`). Con esto, ambas features pedidas al principio de la sesión de `favoritos-rutas` (favoritos + compartir) quedan completas y en `master`.
  **Próximo hito**: ninguno pendiente de esta sesión — ambas specs (favoritos-rutas, compartir-ruta) cerradas y fusionadas.
- **Sesión 2026-08-12 (3) — `favoritos-rutas`: marcar/desmarcar una ruta como favorita con sesión activa (detalle y listado), filtro "Solo favoritas"**: continuación directa de `limpieza-tecnica-monorepo`, a petición del usuario de dos features nuevas (favoritos + compartir ruta). Tras aclarar que "compartir" no tenía un diseño claro aún, se acordó con el usuario: compartir = invitación directa entre usuarios de la app con clonado al aceptar (no enlace público), y **dos specs separadas** — el usuario eligió empezar por favoritos. Compartir-ruta queda para una sesión futura, sin empezar todavía.
  **Decisiones de alcance, todas por AskUserQuestion (el usuario eligió la opción recomendada en cada una)**: toggle visible en `route-list` y `route-detail` (no solo uno); sí filtro "Solo favoritas" (no persistido entre aperturas de la app); sin sesión, el botón de acción no se muestra (misma regla que "Subir a la nube"), pero el indicador de solo lectura sí se muestra siempre.
  **Backend**: migración `0007_add_route_favorite.sql` (`is_favorite BOOLEAN NOT NULL DEFAULT false`), columna aceptada en el `Upsert`/`ListByUser`/`GetByIDForUser` ya existentes — sin endpoint nuevo, mismo mecanismo que las notas (D2/D3 del design.md). 113/113 tests Go en verde.
  **Frontend**: `IRouteRepository.updateFavorite()` (mismo patrón que `updateNotes`, con su propio `ensureColumn` perezoso en SQLite), reutiliza `triggerAutoResync` tal cual para la re-subida en segundo plano. Icono nuevo `favorite-icons.ts` (un único path, relleno controlado por CSS según estado). Componente compartido `shared/favorite-toggle.ts` (usado tanto por `route-detail` como por cada card de `route-list`) — `<button>` con sesión, `<span>` de solo lectura sin ella, mismo `data-cy` en los dos casos.
  **Hallazgo real, autocorregido antes de commitear**: el primer borrador de `favorite-toggle.ts` solo ponía `data-cy` en el `<button>`, no en el `<span>` de solo lectura — violaba la regla del proyecto ("todo elemento interactivo **o localizable por un test** lleva `data-cy`"). Detectado escribiendo el propio test E2E (que había usado un selector de clase como parche, también contra las reglas) y corregido antes de seguir.
  **Ajuste real repetido dos veces durante `apply` (no anticipado en el diseño), mismo patrón que ya había aparecido en `limpieza-tecnica-monorepo`**: cablear el icono de favorito subió tanto `route-detail.element.ts` (415/400 líneas, excepción a 400 de ese fichero) como `route-list.element.ts` (310/300, límite genérico) por encima del límite de `max-lines`. Resuelto extrayendo `route-detail-header.ts` (consolida título+favorito+sync+fecha, con su `.spec.ts`) y `route-list-favorite.ts` (icono por card + toggle del filtro, con su `.spec.ts`) — mismo patrón ya establecido de extracción a fichero propio con test colocado.
  **Decisión de implementación no prevista en el design.md**: `route-list.element.ts` reutiliza `triggerAutoResync`/`SyncTriggerContext` importándolos directamente de `routes/detail/route-detail-sync-triggers.ts` (import cruzado entre dominios `list`→`detail`) en vez de duplicar el mecanismo — son funciones puras sin acoplamiento a componentes.
  **Gap conocido, documentado y explícitamente no arreglado (mismo criterio que un gap ya existente en notas)**: una ruta exclusiva de la nube (nunca guardada en SQLite local) hace que `updateFavorite()` sea un no-op silencioso — no hay fila local que actualizar. Ya afecta a las notas de la misma manera; se documenta en el propio código (`route-detail-favorite.ts`) en vez de arreglar los dos a la vez (fuera de alcance).
  **Verificación real end-to-end**: Vitest 1086/1086 (cobertura ≥80% en las 4 métricas), `tsc --noEmit`/`eslint --max-warnings 0` limpios, Cypress 60/60 contra backend real. **Detalle real del propio proceso de verificación**: `docker-api-1` estaba corriendo una imagen construida antes de añadir la migración `0007` — hubo que `docker compose build api && docker compose up -d api` para que el servidor real aplicara la columna nueva antes de que la suite Cypress (que compara contra el servidor de verdad) pudiera pasar.
  **Pendiente**: verificación manual en dispositivo real (tarea 6.4) — build+instalación lanzados vía `scripts/install-android.sh`, pero la comprobación táctil (marcar/desmarcar, filtro, persistencia tras reinicio) la hace el usuario, no delegable a un agente sin pantalla táctil.
  **Cierre (sesión 2026-08-14)**: verificación en dispositivo real confirmada por el usuario sin hallazgos (tarea 6.4). Revisión independiente completa antes de archivar: releída toda la implementación (backend, modelo/repositorios, componente compartido `favorite-toggle.ts`, wiring de `route-detail`/`route-list`, CSS) y re-ejecutada la suite completa desde cero (Docker parado al empezar la sesión, arrancado y `docker compose build api` para recoger la migración `0007` antes de correr nada) — `go test ./...` 113/113, `tsc --noEmit` y `eslint --max-warnings 0` limpios, `vitest run --coverage` 1086/1086, Cypress `test:e2e` completo 60/60. `review.md` con veredicto **APPROVED** (mapeo Requirement→Scenario→test completo, sin gaps ni desviaciones; dos notas menores no bloqueantes: la re-sincronización de favoritos no tiene test de fallo de red dedicado, cubierta indirectamente por reutilizar sin cambios el mecanismo `triggerAutoResync` ya probado para notas/fotos; y el working tree traía ruido de bundle Android regenerado por la propia verificación en dispositivo, a excluir del commit). Delta specs sincronizadas a `openspec/specs/` (capability nueva `favoritos-rutas` creada; `route-cloud-sync` ampliada con el escenario de re-subida de favoritos). Archivado en `openspec/changes/archive/2026-08-14-favoritos-rutas/`. Sin ADR nueva — ninguna decisión de este cambio (design.md D1-D7) alcanza el umbral de decisión arquitectónica, todas reutilizan patrones ya existentes.
  **Commit y PR**: commiteado (`e050aa6`, excluyendo el bundle Android regenerado por la verificación en dispositivo) tras actualizar localmente el toolchain de Go de 1.26.5 a 1.26.6 tal cual (`go install golang.org/dl/go1.26.6@latest && go1.26.6 download`) — el gate `govulncheck` del pre-commit bloqueaba con 7 vulnerabilidades del propio stdlib de Go (nada que ver con el código de este cambio) hasta actualizar. PR #119 abierta contra `master`, referenciando `review.md`.
  **Fusión (2026-08-14, mismo día): tres hallazgos reales de CI, ninguno anticipado, arreglados uno a uno antes de mergear** — buen ejemplo de por qué el gate de review no sustituye a ver el pipeline real en verde:
  1. **`Quality gates — Go (apps/api)` en rojo en CI** — mismo `govulncheck` que ya había bloqueado el pre-commit local, pero esta vez en el runner: `.github/workflows/ci.yml` fijaba `go-version: '1.25'`, sin el parche. Confirmado que no era cosa de esta PR (el último run de `master`, 2026-08-12, había pasado en verde con el mismo pin — la base de datos de vulnerabilidades de `govulncheck` se actualizó entre medias). Arreglado subiendo el pin a `1.26.6` (commit `b988876`).
  2. **El mismo gate volvió a fallar tras el fix anterior** — la cache key del binario de `govulncheck` (`govulncheck-${{ runner.os }}`) no incluía la versión de Go, así que el runner reutilizaba un binario ya compilado con 1.25, incompatible con paquetes vendored que ahora exigen 1.26 (`"file requires newer Go version go1.26"`). Arreglado añadiendo `${{ steps.setup-go.outputs.go-version }}` a la key (commit `9022500`).
  3. **`Quality gates — TypeScript` en rojo por un Cypress real, no infra** — el test nuevo `"marcar favorita desde el listado..."` (`route-cloud-sync.cy.ts`) falló por timeout de 4000ms. Causa real: `cy.request(...).its('body.X').should('eq', ...)` no reintenta la petición HTTP en cada retry de Cypress, solo relee la respuesta ya resuelta la primera vez — como la re-subida que dispara el campo es fire-and-forget en segundo plano (design.md D3), la aserción podía perder la carrera aunque la re-subida sí terminara a tiempo. Mismo patrón frágil ya existía (sin haber fallado hasta ahora) en otros dos tests de re-sincronización del mismo fichero. Arreglado con un helper nuevo `expectSyncedField()` que reintenta la llamada real hasta 20 veces (mismo presupuesto de 4s), aplicado a los 3 sitios (commit `039b4f2`).
  PR #119 fusionada a `master` (squash, `991c3b4`) tras el cuarto run de CI en verde limpio.
  **Próximo hito**: spec separada de "compartir ruta" (invitación + clonado), todavía sin empezar.
- **Sesión 2026-08-12 (2) — `limpieza-tecnica-monorepo`: cambio de mantenimiento puro (`skip_specs: true`) agrupando los hallazgos de una auditoría técnica del monorepo**: continuación directa de la sesión anterior (`optimizar-bundle-produccion`), a petición explícita del usuario tras pedir un informe de mejoras técnicas y de estructura de carpetas/ficheros/estilos.
  **Hallazgo real más importante, encontrado durante `propose`**: `.cargo/config.toml` estaba duplicado (raíz vs `apps/mobile/src-tauri/`) con NDK API levels distintos (24 vs 34) — por cómo resuelve Cargo, aarch64 compilaba a API 34 mientras el resto de targets usaban 24, ninguno alineado limpiamente con `minSdkVersion: 24` de `tauri.conf.json`. Confirmado con el usuario que 34 no era intencional; unificado a un único fichero en `src-tauri/.cargo/config.toml`, los 4 targets a API 24 — verificado con un build real.
  **Otros hallazgos reales de la propia implementación** (no anticipados en el diseño): (1) el worktree `.claude/worktrees/architecture-decisions` no estaba solo "detrás" de master — dos commits distintos con el mismo mensaje "Merge pull request #83" revelan que el historial se reescribió durante la migración a OpenSpec y el worktree quedó huérfano de esa reescritura; eliminado (`git worktree remove` + limpieza manual de 4.6GB de residuos por un error de ruta larga de Windows), la rama en sí no se tocó. (2) Al mover `nav-bar` a `shared/`, sus imports relativos `../../shared/*` quedaron rotos un nivel de más — corregidos, detectado por `tsc --noEmit`. (3) `cockpit-browser-gps.service.ts` "sin test" era engañoso: sí tenía tests, pero vivían en `cockpit.service.spec.ts` sin colocar junto al fichero, y cubrían solo 2 de sus 4 métodos — movidos y ampliados.
  **Alcance completo**: componente `counter` (leftover de plantilla) borrado; `nav-bar` movido a `shared/`; espaciado hardcodeado sustituido por `var(--space-*)` en 10 ficheros CSS (solo valores con equivalente exacto en la escala, el resto documentado como excepción); sombra OKLCH de un solo uso dejada como excepción documentada (no hay `--success-soft`/`--success-glow`); `font-family: monospace` sustituido por `var(--font-data)` en 2 sitios; `tokens.css` importado directamente en 3 diálogos de `auth/`; 8 ficheros nuevos con test real (62 tests nuevos, 8/10 de los identificados sin cobertura — los otros 2, exención justificada); `CLAUDE.md` documenta el patrón de extracción a `.ts` suelto (aviso previo confirmado por el usuario); matiz de RUSTSEC-2026-0235 (`rkyv`, transitivo vía `rust_decimal`/`sqlx-core`, feature no activada) añadido a la nota existente; `memory/sessions/` documentado como legacy; `moto-routes-design/` documentado.
  **Cierre de una duda abierta desde el `propose`**: `api-security` (backend) vs `security-audit` (frontend/Tauri), y `stop-types-catalog` (datos) vs `route-stop-types-display` (presentación) — sin solape real, confirmado leyendo el `## Purpose` de las 4 specs. Sin acción de consolidación.
  **Verificación**: `tsc`, ESLint, Vitest (1040/1040, 96.64% líneas, +62 tests) en verde; Cypress E2E (54/54) en verde tras el move de `nav-bar` y los cambios de CSS; verificación visual manual no posible (extensión Claude-in-Chrome no conectada) — sustituida por garantía de construcción (solo valores exactos, sin typos en los tokens usados, verificado por grep cruzado). Sin verificación en dispositivo real todavía (pendiente).
  **Próximo hito**: verificación en dispositivo real (tarea 7.1) y PR de `feature/limpieza-tecnica-monorepo` a `master`.
- **Sesión 2026-08-12 — `optimizar-bundle-produccion` (ADR-047): el APK "release" publicado en cada GitHub Release era en realidad un build debug — corregido, de ~170MB a 9MB reales**: petición directa del usuario ("compilar e instalar para ver los iconos" llevó a investigar el tamaño del APK). OpenSpec completo (`propose`→`apply`), verificado con un tag de prueba real (`v0.0.1-optimizar-bundle-test`, borrado después).
  **Causa raíz real, no evidente hasta investigar `ci.yml`**: el job `build-and-release` compilaba con `pnpm tauri android build --target aarch64 --debug` — es decir, lo que se publicaba como "release" desde siempre era un build debug (`isMinifyEnabled=false`, sin proguard/R8, sin `isShrinkResources`, Rust sin el `profile.release` ya bien afinado de `Cargo.toml`). Corregido quitando `--debug`: el buildType `release` de Android reutiliza el mismo `signingConfig` de debug efímero de siempre (sin keystore ni secreto nuevo, decisión explícita para no reabrir ADR-031) — solo cambia de buildType, no de firma.
  **Medido real, no solo razonado sobre config**: `.so` nativo de 148MB (debug, sin stripear) a 6.7MB (release); APK universal de ~170/305MB a **9MB** (confirmado igual en local y en el runner real de CI). Además: sourcemaps de producción desactivados (`sourcemap: false` en `vite.config.ts`, ya no se generan ni se empaquetan) y un quality gate nuevo en CI (`Check APK size budget`, `MAX_APK_SIZE_MB: 20`, medido 9MB real) que bloquea la release si el tamaño se dispara.
  **Gap real encontrado durante `apply`, ya documentado más abajo en este fichero**: el primer build release local empaquetó 8 `.map` huérfanos de builds anteriores a este cambio — el copiado de assets de `pnpm tauri android build` no borra ficheros que ya no existen en el `dist/` actual (mismo gotcha del §Build Android de más abajo, aquí confirmado que también afecta al buildType release, no solo al debug). Corregido con el mismo force-sync ya usado en CI; el script nuevo `apps/mobile/scripts/install-android.sh` (detecta la ABI del dispositivo conectado vía `adb`, compila+instala en un solo paso) lo aplica también para no heredar el bug en cada iteración local.
  **Verificación real end-to-end**: TDD en `ci-workflow.spec.ts` (rojo→verde, 35/35), tag de prueba real disparó el pipeline completo en GitHub Actions (`Build & release Android APK` en verde, 18m44s, APK real de 9MB adjunto al Release), instalación y arranque confirmados en dispositivo real (`75fe536b`) por el usuario — grabación GPS, mapa y fotos siguen operativas tras minificar con R8, sin necesitar ninguna regla `-keep` nueva en `proguard-rules.pro`.
  **Fuera de alcance, decidido explícitamente**: no se toca `JAVA_HOME`/JDK local (problema de entorno de build, no de tamaño en producción — CI ya fija JDK 17); no se introduce ABI splitting (CI ya compila un único target `aarch64`, nunca fue universal en producción); no se añade firma de release real ni keystore nuevo.
  **Próximo hito**: PR de `feature/optimizar-bundle-produccion` a `master`, referenciando `review.md` cuando se archive.
- **Sesión 2026-08-11 (3) — `sistema-iconos-svg` (ADR-046): unifica todos los iconos de la app en SVG sobrio de 2 colores, revierte parcialmente ADR-035**: continuación directa de la sesión anterior, a petición explícita del usuario ("quiero que todos los iconos de la app sean como el de ruta subida a la nube... ese estilo sobrio, en 2 colores").
  **Alcance final, tras dos preguntas de clarificación al usuario durante la implementación**: papelera de borrar (ruta/foto) y cierre del visor de fotos, tipos de parada (menú/timeline/mapa — revierte ADR-035), pestañas Rutas/Perfil de la nav-bar, e iconos nuevos en toasts (éxito/error, capacidad que no existía). **Explícitamente fuera de alcance, decidido con el usuario**: el `record-dot` de la pestaña "Grabar" (indicador de acción con un fix de centrado frágil ya protegido por test, AC-018) y los 4 controles del cockpit grabar/detener/pausar/reanudar (formas rellenas, convención estándar de controles de media) — ninguno de los dos se convierte a línea SVG.
  **Gap real encontrado durante la implementación, no anticipado en el diseño**: ~10 llamadores de `showToast()` en 7 ficheros ya concatenaban `⚠️`/`📷`/`☁` dentro del mensaje — sin quitarlos, el toast habría mostrado el emoji y el icono SVG nuevo a la vez. Limpiados todos. **Dos usos de emoji quedaron fuera de alcance, sin tocar** (no son toasts, no encajan en las 4 categorías de la spec): `route-detail-timeline.ts` (`'📷 Foto'`, label inline) y `route-detail-states.ts` (`⚠️ ${message}`, panel de error) — candidato para un cambio futuro si se decide ampliar el sistema de iconos.
  **Incidente de proceso real, detectado y corregido antes de escribir la ADR**: la rama de este cambio se creó desde un `master` desactualizado (antes de fusionar la PR #115 de `sincronizar-version-app`) — habría producido dos ADR-045 distintas al fusionar ambas PRs. Corregido con `git merge feature/sincronizar-version-app --ff-only` antes de continuar; la ADR de este cambio quedó numerada 046 sin colisión.
  **Verificación**: TDD real en los 5 módulos nuevos de `shared/icons/` y sus consumidores — Vitest 979/979 (96.42% líneas), ESLint limpio (corregido un `max-statements` real extrayendo `buildDelimiterLabel`), Cypress 54/54 contra backend real (encontró y corrigió un bug real de encadenamiento de comandos Cypress: reutilizar una referencia `cy.get()` guardada y llamar `.find()` sobre ella desplaza el "subject" de las aserciones `.should()` siguientes al resultado de `.find()`, no al elemento original — hay que re-consultar el selector en cada expectativa en vez de reutilizar la referencia).
  **Pendiente, no verificable en este entorno**: verificación visual manual (capturas o dispositivo Android real) — la extensión Claude-in-Chrome no estaba conectada en esta sesión. Cypress headless confirma el comportamiento funcional, no el acabado visual fino (alineación, recorte, color en modo oscuro real).
  **Próximo hito**: decidir si se hace la verificación visual pendiente antes o después de fusionar la PR, y si se aborda como cambio futuro el hallazgo suelto de `route-detail-timeline.ts`/`route-detail-states.ts`. Cambio archivado (`openspec/changes/archive/2026-08-11-sistema-iconos-svg/` tras `/opsx:archive`) — pendiente de commit + PR a `master` en el momento de escribir esto.
- **Sesión 2026-08-11 (2) — `sincronizar-version-app`: el APK de cada release pasa a llevar el `versionName`/`versionCode` real del tag, no el placeholder `0.1.0`**: continuación directa de la sesión anterior (incidente de Funnel, ver debajo y [[ADR-045]]), a petición explícita del usuario tras detectar el gap suelto de versionado. Cambio implementado siguiendo OpenSpec completo (`propose`→`apply`), TDD real en `apps/mobile/src/shared/ci/ci-workflow.spec.ts` (2 tests nuevos confirmados en rojo antes de tocar `ci.yml`, verdes después — 33/33 en la suite del workflow).
  **Implementación**: nuevo step "Set app version from release tag" en el job `build-and-release` de `.github/workflows/ci.yml`, antes de compilar — toma `$GITHUB_REF_NAME` (variable de entorno del runner, mismo mecanismo ya usado en "Rename APK with the release tag"; `design.md` mencionaba la expresión `${{ github.ref_name }}`, corregido en la implementación por consistencia con el precedente real del propio job), le quita el prefijo `v`, y sustituye con `sed` el `"version"` de `apps/mobile/src-tauri/tauri.conf.json` en el checkout efímero — nunca escrito de vuelta al repo, mismo patrón que el parcheo ya existente de `connect-src`/`VITE_API_BASE_URL`. El step "Verify the APK bundles..." se amplió para localizar `aapt` bajo `$ANDROID_HOME/build-tools/` (mismo patrón que la localización de `sdkmanager`, ADR-031) y comparar el `versionName` empaquetado (`aapt dump badging`) contra el tag, fallando el job explícitamente si no coincide.
  **Verificación**: `tsc`, ESLint (`eslint src/`) y Vitest con cobertura (967/967, 96.38% líneas) en verde; `cargo fmt`/`clippy`/`test` en verde. **No verificados en este entorno por falta de Docker**: Cypress E2E y los tests Go de `apps/api` (ninguno de los dos toca ficheros de este cambio, pero queda pendiente de confirmar). **No se ha publicado ningún tag/release nuevo** (petición explícita del usuario) — el comportamiento real del step nuevo (`sed`+`aapt` en un runner real) queda pendiente de confirmar en la primera release futura que lo use.
  **Hallazgo real, no relacionado, descubierto de pasada verificando este cambio**: `cargo audit --ignore RUSTSEC-2023-0071` falla localmente con una vulnerabilidad nueva no cubierta por la excepción ya documentada — `RUSTSEC-2026-0235` (`rkyv 0.7.46`, "Insufficient archive validation can cause out-of-bounds reads in archives containing Rc/Arc", solución: upgrade a `>=0.8.17`). Preexistente, no causado por este cambio (ninguna dependencia de Cargo tocada). Curioso que el job `quality-tauri` de la PR #114 (2026-08-11) pasara en verde pese a esto — posible caché de la base de datos de advisories en el runner de CI desactualizada respecto a la copia local recién descargada. **Investigado en la sesión `limpieza-tecnica-monorepo` (2026-08-12)**: `rkyv` no es dependencia directa (ausente de `Cargo.toml`); llega transitiva vía `rust_decimal` (que a su vez llega por `tauri-plugin-sql` → `sqlx-core`). `cargo tree -i rkyv --all-features --target all` no encuentra ningún consumidor real — la feature `rkyv` de `rust_decimal` no está activada por nadie (solo `default`/`serde`/`std`), así que queda fijada en `Cargo.lock` pero probablemente no se compila en el binario final. `cargo audit` sigue reportándolo porque escanea `Cargo.lock` sin importar activación de features. Baja urgencia real confirmada — sin abrir cambio dedicado por ahora.
  **Próximo hito**: decidir si se abre un cambio dedicado para el hallazgo de `cargo audit` (`RUSTSEC-2026-0235`), y confirmar Cypress/Go de este cambio en un entorno con Docker antes de darlo por cerrado del todo (o en la próxima sesión que sí lo tenga). Cambio sin archivar todavía (`openspec/changes/sincronizar-version-app/`, sin `/opsx:archive` ni PR abierta).
- **Sesión 2026-08-11 — incidente real: Tailscale Funnel dejó de resolver públicamente, cuenta creada erróneamente investigada en producción (ADR-045)**: continuación tras fusionar PR #114 (cierre de `despliegue-automatico-backend`). Dos hilos en la misma sesión.
  **Hilo 1 — comprobación de datos personales**: el usuario pidió comprobar si `gcarmenado@gmail.com` estaba guardado en producción y borrarlo si era así. Consultado por SQL directo contra el servidor real (`sudo -u postgres psql`, exacto e `ILIKE` parcial) — no existía ninguna fila, nada que borrar.
  **Hilo 2 — "no se pudo crear la cuenta" en la app real, investigado y resuelto en vivo (ver [[ADR-045]] para el detalle completo)**: la release `v0.1.5` y su CSP/`VITE_API_BASE_URL` estaban correctos (verificado extrayendo el APK real), pero el registro DNS público de Tailscale Funnel (`debian.taildf3dab.ts.net`) había dejado de publicarse tras asignar `tag:prod-server` desde el panel durante `despliegue-automatico-backend` (2026-08-09) — la ACL dejó al nodo fuera de la lista autorizada para Funnel. Diagnosticado conectando por Chrome DevTools Protocol al WebView real del móvil del usuario mientras intentaba registrarse (`net::ERR_NAME_NOT_RESOLVED`) y confirmado con una consulta DNS-over-HTTPS genuinamente externa (Google DoH) — **trampa real evitada durante el diagnóstico**: verificar desde la propia máquina de desarrollo (mismo tailnet que el servidor) da un falso positivo, porque Tailscale intercepta localmente la resolución de `*.ts.net`. Resuelto: el usuario re-aprobó el nodo para Funnel desde el panel (acción de cuenta, no delegable), se repitió `tailscale funnel --bg` en el servidor, y se verificó extremo a extremo con DNS externo real + un registro de cuenta de prueba (`201`, borrada después).
  **Gap detectado de pasada, sin corregir todavía**: `apps/mobile/src-tauri/tauri.conf.json` tiene `"version": "0.1.0"` hardcodeado desde el principio — todas las releases (`v0.1.1` a `v0.1.5`) se compilan con ese mismo `versionName` grabado en el APK (Android muestra literalmente "0.1.0" en cualquier release instalada), aunque el nombre del asset del Release sí lleva el tag correcto. `ci.yml` renombra el fichero del APK con el tag pero nunca actualiza `tauri.conf.json`. Sin corregir esta sesión, el usuario no ha confirmado todavía si quiere el fix.
  **Próximo hito**: (1) decidir si se corrige el versionado de la app (sincronizar `tauri.conf.json` a mano o hacer que `ci.yml` derive `version` del tag `v*` al compilar); (2) cambio futuro candidato, ya anotado en [[ADR-045]]: un health-check post-deploy que verifique la resolución/salud pública real de `apps/api` desde fuera del tailnet, para detectar este tipo de incidente automáticamente en vez de por un usuario reportando un fallo.
- **Sesión 2026-08-09 (2) — `despliegue-automatico-backend` (ADR-044): despliegue de `apps/api` automatizado vía GitHub Actions, revirtiendo deliberadamente ADR-033/036/041; verificado end-to-end con una release real (`v0.1.5`)**: continuación de la sesión anterior tras fusionar `subida-fotos-mobile`. El usuario pidió automatizar el despliegue del backend y preguntó explícitamente si contradecía decisiones previas — se le mostraron ADR-033/036/041 (que rechazaron esto tres veces por "ceremonia manual por defecto") y confirmó proceder igualmente, además de pedir un gate para que solo él pueda aprobar un despliegue real (el repo tiene un segundo colaborador con permiso de `push`).
  **Diseño rehecho dos veces en mitad de la implementación al chocar con la realidad del servidor** (detalle completo en ADR-044/`design.md` del cambio archivado): (1) el plan de clave SSH con `command=` forzado se descartó al confirmar que el servidor no tiene `sshd` tradicional — todo pasa por Tailscale SSH, sin equivalente al `command=` de OpenSSH; sustituido por un usuario `ci-deploy` cuya shell de login es un script nuevo (`scripts/deploy-local.sh`), con acceso acotado al checkout real vía ACLs POSIX (`setfacl`, tránsito sin listar sobre `/home/gonzalo`, rw solo sobre `/home/gonzalo/moto-routes`). (2) La regla `ssh` de Tailscale apuntaba primero a `tag:ingress`, asumiendo que era el tag del servidor — resultó pertenecer a los nodos internos de Tailscale Funnel; corregido creando `tag:prod-server` dedicado y una regla adicional que preserva el acceso SSH humano normal de `gonzalo` (que se habría roto al dejar el servidor de contar como `autogroup:self` tras etiquetarlo).
  **Verificación end-to-end real con la release `v0.1.5`, no simulada — costó cuatro intentos, cada uno un bug real distinto**: falta el scope `Keys → Auth Keys` del cliente OAuth de Tailscale (solo tenía `Devices Core`) → el tag no estaba en el selector de ese scope concreto → `dst` de la regla `ssh` apuntando al tag equivocado (`tag:ingress`, ver arriba) → el commit de `deploy-local.sh` se hizo desde Windows y perdió el bit de ejecución en git (`100644`), rompiendo la shell de login en cada `git pull` (fix dedicado, PR #113, `git update-index --chmod=+x`). El quinto intento completó con éxito: pull + build + recreación real del contenedor + salud en `200`, credenciales y host enmascarados en el log.
  **Hallazgo de seguridad propio, no relacionado con Tailscale**: la IP real del servidor se hardcodeó por descuido tanto en `ci.yml` como en `deploy-local.sh` durante la implementación — violación directa de ADR-035 (host real nunca en un fichero versionado, repo público). Detectado y corregido antes de abrir la PR: `deploy-local.sh` la resuelve en runtime (`tailscale ip -4`), `ci.yml` la lee de un secret nuevo del Environment `prod` (`PROD_SERVER_HOST`).
  **PRs**: #112 (cambio completo) y #113 (fix del bit de ejecución), ambas fusionadas a `master`. `scripts/deploy-prod.sh` no se tocó, sigue siendo el path manual desde fuera del servidor.
  **Cierre**: `/opsx:archive` completado — veredicto `review.md` **APPROVED WITH MINOR ISSUES** (6/7 escenarios verificados contra infraestructura real; el gap: aislamiento de red del runner verificado solo por política ACL, no con una prueba negativa real). Archivado en `openspec/changes/archive/2026-08-09-despliegue-automatico-backend/`, spec principal `server-deployment` sincronizada.
  **Próximo hito (sesión siguiente)**: fusionar **PR #114** (cierre: memoria + ADR-044 + spec sincronizada + archivado, sin cambios de código) — quedó con el check `Quality gates — TypeScript` en rojo por el mismo flake ya conocido de esta sesión (rate-limit de registro agotado en el runner de CI, `auth.cy.ts`, "email ya existente"/reenvío), no relacionado con el contenido de la PR (solo documentación). Revisar si hace falta relanzar el job o si conviene primero investigar por qué este flake ya se ha repetido varias veces esta sesión incluso en runners de CI limpios (antes solo se había visto en el entorno local) — podría merecer su propio ajuste (p. ej. subir `registerRateLimitMaxAttempts` o aislar mejor el estado entre specs de Cypress). Riesgo residual documentado y no resuelto del cambio en sí: `ci-deploy` no tiene `sudo`, pero el grupo `docker` es en la práctica equivalente a `root` sobre el host.
- **Sesión 2026-08-09 — `subida-fotos-mobile` (ADR-043): apps/mobile consume por fin el backend de fotos; verificación en dispositivo real encuentra y corrige dos bugs reales de producción**: sesión larga, empieza recuperando el contexto de la sesión anterior (petición explícita del usuario, "recupera la ultima sesion").
  **Arranque — CI y merge de `almacenamiento-fotos-backend` pendientes de la sesión anterior**: el job `quality-ts` fallaba porque `.env.example` deja `PHOTO_ENCRYPTION_KEY` vacía a propósito (se genera por desarrollador) — corregido generándola en el propio job antes de `docker compose up`. De paso, el pre-commit hook bloqueaba por una vulnerabilidad `high` en `nanoid` (vía `vitepress`/dev-only) — resuelto con `pnpm-workspace.yaml::overrides` (no en `package.json`, que pnpm 11 ya no lee para overrides). PR #110 fusionada a `master`.
  **`subida-fotos-mobile`**: cierra los dos `// TODO` de `route-detail.element.ts` (líneas 433/508) — añadir/borrar una foto en una ruta ya sincronizada ahora sube/borra también el archivo contra `almacenamiento-fotos-backend` ([[ADR-042]]), no solo re-sincroniza metadatos. Nuevo `photo-cloud-api.service.ts` (multipart en subida), `fetchJson` extendido para `DELETE`/`FormData`/`204` sin romper sus consumidores existentes, campo `remotePhotoId` en `Photo` con migración SQLite condicional (mismo patrón que `preview_polyline`), subida/borrado en segundo plano no bloqueante (`uploadPhotoToCloud`/`deletePhotoFromCloud`, extraídos a `route-detail-sync-triggers.ts` por límite de tamaño del fichero). TDD real: 963/963 Vitest, 54/54 Cypress contra backend real.
  **Bug real 1 (Vitest/Cypress, antes del dispositivo)**: `persistCapturedPhoto` devolvía los metadatos de entrada en vez de la entidad `Photo` real persistida (sin `id`) — nunca se habría podido referenciar la foto subida para marcarla sincronizada. Corregido para que devuelva lo que el repositorio realmente persiste.
  **Bug real 2, encontrado en Cypress contra backend real**: `readPhotoBlob` (navegador) usaba `fetch()` sobre un `data:` URL — el CSP de `index.html` (`connect-src`) no incluye `data:` (solo `img-src`), así que la subida quedaba bloqueada en silencio. Corregido decodificando el `data:` URL a mano (`atob`), sin tocar el CSP.
  **Bug real 3, invisible hasta un dispositivo Android real (ADR-043)**: el borrado de una foto sincronizada nunca llegaba al servidor desde el WebView real (sí desde Cypress, cuyo navegador no aplica CORS con el mismo rigor). Causa: `httpmw.PublicCORS` nunca declaraba `DELETE` en `Access-Control-Allow-Methods` — tercera vez que este middleware se queda corto al añadir un método HTTP nuevo. Diagnosticado con un `fetch()` crudo vía Chrome DevTools Protocol contra el WebView real (mismo procedimiento ya usado en `rutas-en-la-nube` para verificar qué bundle sirve de verdad). Corregido con test de regresión en `cors_test.go`, backend redesplegado localmente y reverificado.
  **Verificación end-to-end en dispositivo Android real**: build completo de la CLI (mismos dos gotchas ya documentados — JBR de Android Studio autoactualizado a Java 25, `JAVA_HOME` a `jdk-24`; hash del JS desactualizado en el primer build, resuelto con el ciclo ya documentado de copiar `dist/` a mano + segundo build), verificado con CDP (no solo `unzip`). Login, grabación de una ruta mínima, subida a la nube y navegación completa automatizados vía Chrome DevTools Protocol (pierce de Shadow DOM con un `deepQuery` inyectado) para no depender de que el usuario escribiera/tocara cada paso — reservando su intervención física solo para lo que de verdad la necesita: cámara/galería (pickers nativos fuera del WebView), desbloqueo de pantalla y modo avión. Foto real de cámara (2.5MB, EXIF con modelo de dispositivo) subida y verificada byte a byte tras cifrar/descifrar. Escenario sin conexión verificado cortando también el túnel `adb reverse` (el modo avión solo no basta — el USB seguiría dejando pasar el tráfico): añadir/borrar sin conexión no bloquea la UI ni pierde el cambio local (`remotePhotoId` queda `null`), y borrar una foto nunca subida no intenta ninguna llamada de red.
  **Falso positivo investigado y descartado**: subir una foto desde la galería dio un error de permisos propio de Tauri durante la sesión (la cámara funcionaba bien). Investigado tras `/opsx:archive`: no reproducible ni con toques físicos reales ni vía Chrome DevTools Protocol (`el.click()` síntetico sobre el `<input type="file">` ni siquiera abre el picker nativo — "File chooser dialog can only be shown with a user activation", limitación del propio WebView, no relacionada con el bug reportado). El usuario reinició la app y la galería volvió a funcionar con normalidad — confirmado con selección múltiple real. Se trata como estado transitorio del WebView/Photo Picker de Android, no un bug de código: sin cambios en `apps/mobile` para esto.
  **`/opsx:archive` completado — veredicto `review.md`: APPROVED.** 9/9 escenarios del delta spec cubiertos por test (965/965 Vitest, 113/113 Go, 54/54 Cypress contra backend real), un hueco de cobertura encontrado y cerrado durante la propia revisión (límites del backend sin test específico). Spec principal `route-cloud-sync` sincronizada. Archivado en `openspec/changes/archive/2026-08-09-subida-fotos-mobile/`.
  **Próximo hito**: PR de `feature/subida-fotos-mobile` a `master`, referenciando `review.md` ya archivado.
- **Sesión 2026-08-07 (3) — `almacenamiento-fotos-backend` (ADR-042): primer uso real de MinIO, cifrado de aplicación AES-256-GCM, solo backend**: continuación directa de la sesión anterior, a petición del usuario ("vamos a crear una nueva spec... coge contexto... eso es, demomento solo la parte del back"). Cierra el hueco que dejó `rutas-en-la-nube` (dos `// TODO` en `route-detail.element.ts`, líneas 433/508) y le da a MinIO su primer consumidor real (ADR-041 lo dejó documentado como "provisión anticipada sin consumidor").
  **Diseño acordado con el usuario antes de proponer**: nuevo paquete `internal/photos/` (mismo patrón que `internal/routes/`), `BlobStore` sobre MinIO vía `minio-go`, **cifrado de aplicación** (no SSE-S3 de MinIO — la clave vive solo en `apps/api`, nunca junto a los datos, para que un compromiso de MinIO solo no baste para ver las fotos), cuatro endpoints (`POST/GET /api/routes/{id}/photos`, `GET/DELETE .../{photoId}`) siempre proxied (nunca una URL directa ni presigned de MinIO). El usuario preguntó explícitamente si cifrar era "pesado" antes de decidir — se confirmó con hechos del propio servidor (`/proc/cpuinfo` tiene `aes`, AES-NI real) que el coste es insignificante frente al tiempo de red.
  **Implementación en TDD real** (150/150 tests Go en verde tras el cambio, incluidos tests de integración contra un MinIO real, no solo Postgres): `Encrypt`/`Decrypt` (`crypto/aes`+`crypto/cipher` estándar de Go), `MinioBlobStore`, `PostgresPhotoStore` (propiedad resuelta siempre vía `route_id`→`routes.user_id`, nunca un `user_id` propio en `route_photos`), y los cuatro handlers.
  **Tres gaps reales encontrados verificando con `curl` de verdad contra el stack completo, ninguno visible solo con tests**: (1) `bitnami/minio` (elegido inicialmente para que CI pudiera levantarlo como *service container* declarativo, ya que la imagen oficial `minio/minio` exige el argumento `server /data` que el bloque `services:` de GitHub Actions no admite pasar) **ya no está en Docker Hub** — descubierto al intentar `docker run bitnami/minio:latest` de verdad, no al leer documentación; resuelto levantando MinIO oficial con un paso `docker run` manual + espera por `curl` desde el propio runner. (2) La respuesta de `POST .../photos` devolvía `created_at` vacío — `Create` nunca devolvía el valor real que Postgres asigna por `DEFAULT now()`; corregido con `RETURNING created_at`, cambiando la firma de `PhotoStore.Create` para devolver el `Photo` completo. (3) Una imagen fresca de MinIO (vía `docker compose down -v` + `up`) no tiene el bucket `images` — a diferencia de Postgres, la imagen oficial de MinIO no trae ningún equivalente a `docker-entrypoint-initdb.d`; resuelto con un servicio `minio-init` de un solo uso (`minio/mc`) en `docker-compose.yml`, sin ese problema en producción porque el bucket ya existía de antes (ADR-041).
  **Verificación real end-to-end en dos entornos**: local (`docker compose up --build` desde cero, subida→listado→descarga con bytes idénticos al original→borrado, aislamiento entre cuentas con 404 sin revelar existencia, e inspección directa de los bytes en MinIO con `mc cat` confirmando 97 bytes sin cabecera PNG — 69 originales + 12 nonce + 16 tag GCM) y **producción real, desplegada sin fusionar** (mismo patrón que ADR-034/038/039/041: imagen anterior etiquetada `docker-api:pre-almacenamiento-fotos`). En producción, además: usuario de MinIO dedicado a `apps/api` creado con una política acotada solo al bucket `images` (nunca las credenciales root), credenciales y `PHOTO_ENCRYPTION_KEY` generadas y escritas directamente en `.env.prod` del servidor por un script remoto — nunca visibles en la salida del propio agente. **Un `DELETE` a través de Tailscale Funnel devolvió un 404 transitorio justo tras el redeploy** (confirmado no ser un bug real: la misma petición directa contra `100.114.190.36:8080` devolvió el 401 correcto, y repetida por Funnel poco después funcionó) — probablemente Funnel con una conexión en vuelo hacia el contenedor recién reiniciado. Cuentas y rutas de prueba borradas después, `count(*) = 0` en las tablas relevantes.
  **Disciplina de commits de esta sesión**: el usuario pidió explícitamente comitear en local sin hacer `push` hasta que todo funcionara, para no disparar Actions de más — 7 commits locales en `feature/almacenamiento-fotos-backend`, `push` solo al llegar al paso de despliegue real (necesario para que el servidor pudiera hacer `git fetch`; confirmado antes de hacerlo que `ci.yml` solo dispara en `push` a `master` o en `pull_request`, nunca en el push de una rama feature suelta).
  **Próximo hito**: revisar el diff completo buscando secretos (hecho, sin hallazgos — solo nombres de variables y credenciales triviales de dev ya versionadas desde antes), `/opsx:archive` con review independiente, y PR de `feature/almacenamiento-fotos-backend` a `master`. Después, cambio de frontend aparte para cerrar los dos `// TODO` de `apps/mobile` (integración real de subida/borrado de fotos con este backend).
- **Sesión 2026-08-07 (2) — `hardening-despliegue-servidor` (ADR-041): contenedor no-root, script de despliegue versionado, MinIO documentado, servidor redesplegado en real**: continuación directa de la sesión anterior. Una auditoría de seguridad pedida por el usuario (`docs/informe-tecnico-seguridad.md`) encontró tres hallazgos: el contenedor de `apps/api` corría como `root`, MinIO estaba sin documentar en ningún ADR, y el servidor no se había redesplegado tras `rutas-en-la-nube` (`/api/routes` daba 404, faltaba la migración `0005`).
  **Revisión de los artefactos antes de continuar (a petición explícita del usuario, "revisa si todo es correcto")** encontró varios problemas reales, todos corregidos antes de desplegar: (1) `scripts/deploy-prod.sh` construía el comando remoto como `cd '$REMOTE_DIR'` con `$REMOTE_DIR="~/moto-routes"` — con comillas simples bash no expande el `~`, así que el primer uso real habría fallado con "No such file or directory"; verificado el bug y el fix con una simulación de las dos capas de shell (local construye el string → `ssh` → el shell remoto lo reparsea desde cero) antes de tocar nada. (2) Mismo script, un backtick dentro de un `echo "..."` con comillas dobles ejecutaba de verdad `` `chmod 600` `` (sin argumento) como sustitución de comandos en vez de imprimirlo literalmente. (3) Variable `REPO_DIR` sin usar. (4) `docs/informe-tecnico-seguridad.md` tenía basura de un tool-call mal cerrado al final del fichero. (5) `design.md` documentaba `addgroup`/`adduser` pero el `Dockerfile` real usa `groupadd`/`useradd` — corregido el texto del diseño para que coincida con el código ya verificado, no al revés.
  **Despliegue real verificado contra producción, desde la rama sin fusionar** (mismo patrón que ADR-034/038 — `scripts/deploy-prod.sh` hace `git pull origin master`, que todavía no incluye este cambio, así que se hizo `git checkout feature/hardening-despliegue-servidor` a mano en el servidor + `docker compose up -d --build`, no el script literal). Imagen anterior etiquetada `docker-api:pre-hardening` para rollback. Verificado con hechos, no solo logs: **usuario no-root** confirmado con `docker exec docker-api-1 id` (`uid=999(appuser)`) — `docker exec ... ps` no sirve porque `debian:trixie-slim` no trae `procps`, hay que usar `docker top <contenedor>` (usa el `ps` del host) o `id` dentro del contenedor. **Migración `0005` aplicada** (consultado `schema_migrations` por SQL directo). **`/api/routes` funcional de punta a punta**: cuenta de prueba real registrada contra el servidor de producción, verificada por SQL, login real, `GET /api/routes` con el JWT devuelto → `200 []` (401 sin token) — cuenta borrada después, `count(*) = 0` confirmado. `/api/ping` verificado `200` en la URL pública de Funnel.
  **Próximo hito**: revisar el diff completo buscando secretos antes de abrir la PR (gate de seguridad, tarea 5.3), luego PR de `feature/hardening-despliegue-servidor` a `master` (referenciando esta verificación real, mismo patrón que ADR-038/039) y fusionar.
- **Sesión 2026-08-07 — `rutas-en-la-nube` (ADR-040): primera persistencia de rutas en el servidor, ligada a la cuenta; implementación completa (grupos 1-8) verificada con backend real, Cypress real y dispositivo Android real.** PR #105 (incidencia de GitHub Actions de la sesión anterior) confirmada resuelta y fusionada a `master` al empezar esta sesión.
  **Backend** (`apps/api/internal/routes/`, patrón ya establecido por `internal/auth/`/`internal/stoptypes/`): migración `0005_create_routes.sql` (`routes`/`route_points`/`route_stops`, `routes.id` UUID = mismo id que genera el cliente, sin tabla de mapeo), `PostgresRouteStore.Upsert` (con guarda explícita: un `id` ya usado por otra cuenta nunca se sobrescribe, ni siquiera silenciosamente — `ErrRouteOwnedByAnotherUser`) / `ListByUser` / `GetByIDForUser`, tres endpoints (`POST/GET /api/routes`, `GET /api/routes/{id}`) tras `RequireAuth` + CORS/OPTIONS desde el primer commit. 110/110 tests Go en verde, `go vet`/`govulncheck` limpios.
  **Frontend**: `route-cloud-api.service.ts` (cliente HTTP, patrón `auth-api.service.ts`), fusión local+nube pura (`route-list-sync.transform.ts`) con indicador visual por estado (`data-cy="route-card-sync-badge"`, solo visible con sesión activa), botón "Subir a la nube" en `route-detail` (visible solo con sesión y ruta local), rama de detalle para una ruta exclusiva de la nube (`route-detail-cloud.service.ts`, ids de puntos/paradas sintetizados solo para presentación). 904/904 tests Vitest en verde, cobertura 96.25%/90.81%/94.06%/96.25% (statements/branches/functions/lines).
  **E2E Cypress nuevo** (`route-cloud-sync.cy.ts`, backend real): subida real, listado sin duplicar, ruta cloud-only sembrada vía API en el propio test, aislamiento entre cuentas — 51/51 specs de la suite completa en verde, sin regresiones.
  **Verificación en dispositivo Android real, con hallazgos reales que no habían salido en tests**: (1) **fallo real corregido** — las rutas cloud-only se añadían siempre al final del listado sin importar su fecha (`mergeLocalAndCloudRoutes` no ordenaba el resultado); arreglado con `.sort()` por `createdAt` descendente + test de regresión. (2) **gotcha nuevo de build Android**, ver sección "Build Android" más abajo — el atajo "copiar `dist/` a mano + Gradle-only" (documentado como fiable en una sesión anterior) no sirvió esta vez; solo el build completo de la CLI (con Rust) hizo que el WebView sirviera el JS nuevo, y la única verificación que lo detectó fue Chrome DevTools Protocol contra el WebView real (`unzip` del APK no basta — confirma qué se empaquetó, no qué se sirve en runtime). (3) Dos falsos positivos descartados en el propio proceso: un mensaje "Ã³" que parecía mojibake del pipeline y una réplica JSON corrupta por caracteres acentuados — ambos eran el propio terminal Git Bash de esta sesión mutilando UTF-8 al pasar argumentos, no un bug real (confirmado con un payload UTF-8 limpio vía fichero).
  **Ronda 2, mismo día — refinamiento de UI a petición del usuario tras probar en dispositivo real**: el botón "Subir a la nube" (ancho completo) pasa a un icono pequeño junto al título (nube-subir / nube-check, pulsable en ambos estados), detectando si la ruta ya está sincronizada con una consulta a `checkIfRouteIsSynced` (nueva, `route-detail-cloud.service.ts`) al cargar. Nueva **re-subida automática silenciosa**: guardar una nota o añadir/borrar una foto en una ruta ya sincronizada la re-sube sola en segundo plano (sin toast de éxito, solo aviso discreto si falla) — nunca sube una ruta puramente local; fotos en sí mismas quedan con un `// TODO` explícito en el código para cuando exista blob storage. El indicador de estado del listado pasó por **dos iteraciones visuales** antes de convencer al usuario (chip en la fila de distancia/duración → icono junto al botón de eliminar → **insignia superpuesta en la esquina de la miniatura**, la que se quedó) — las dos primeras dejaban el icono visiblemente descentrado, confirmado con medición real (no solo a ojo) vía Chrome DevTools Protocol contra el WebView del dispositivo. De paso, un `<h1>` sin el margen del navegador reseteado (gap real de ~20px, no solo en esta pantalla) corregido en el reset global de `tokens.css`. Ver ADR-040 (ampliada) y la sección "Build Android" más abajo para el gotcha nuevo de build encontrado reiteradamente en esta ronda.
  **`/opsx:archive` completado — veredicto `review.md`: APPROVED.** Revisión independiente (suites reejecutadas por quien revisa, no solo el resumen): 110/110 Go, 917/917 Vitest (cobertura 96.29%/90.95%/94.1%/96.29%), 52/52 Cypress, `tsc`/`eslint` limpios en todo `apps/mobile/src`. 18/18 escenarios del spec con test, dos gaps menores señalados (no bloqueantes, mismo camino de código ya cubierto por otro escenario). Nueva spec principal creada en `openspec/specs/route-cloud-sync/spec.md` (capacidad nueva, sin spec previa que fusionar). Archivado en `openspec/changes/archive/2026-08-07-rutas-en-la-nube/`.
  **Próximo hito**: PR de `feature/rutas-en-la-nube` a `master`, referenciando `review.md` ya archivado. Si en el futuro se retoma el atajo "Gradle-only" para builds de Android, no confiar en él sin re-verificar con CDP — y aun con el build completo, verificar el hash `dist/` vs APK cada vez, repitiendo copiar+rebuild si no coincide.
- **Sesión 2026-08-06 (4) — `pantallas-auth-mobile` implementado y verificado en dispositivo real; gap de CORS (PR #104) y optimizaciones de CI (PR #105) abiertas, bloqueadas por una incidencia real de GitHub Actions; auditoría de seguridad del servidor de producción y redespliegue a `master`**: sesión larga, continuación directa de las dos anteriores.
  **`pantallas-auth-mobile`** (27/30 tareas, dominio nuevo `src/auth/`): diálogos de registro/login/recuperar-contraseña, extensión de `fetchJson` (POST/headers/`checkStatus` explícito, no inferido del método), persistencia de sesión (`ISessionRepository`, SQLite/memoria), sección "Cuenta" en Perfil, E2E Cypress nuevo (`auth.cy.ts`, backend real). **Gap real encontrado por el propio E2E, invisible con `curl` o páginas same-origin**: `apps/api` no respondía a preflight `OPTIONS` ni declaraba `Access-Control-Allow-Methods`/`Headers` en los endpoints de auth — corregido en `httpmw.PublicCORS`, **PR #104** (rama propia, no bundleado con el frontend por tocar código ya en `master`). De paso, revisados `ci.yml`/`docs.yml`: sin `concurrency` group (runs de PR obsoletos no se cancelaban), `docs.yml` sin caché de Rust para `cargo doc`, Cypress/`govulncheck` sin cachear — **PR #105**. Ambas PRs con CI en verde salvo por una **incidencia real de GitHub Actions (Major Outage, iniciada 2026-08-06 15:22 UTC)** — jobs colgados 15 min sin runner o fallando con "Service Unavailable" al resolver actions, confirmado en `githubstatus.com`, nada que ver con el código. Pendiente: fusionar ambas en cuanto se resuelva.
  **Verificación en dispositivo real (grupo 10), tres gaps de conectividad, ninguno bug de la app**: build local por USB apuntando primero a Wi-Fi LAN, después a Tailscale — `PUBLIC_API_BASE_URL` exige prefijo `https://` (validación intencionada) pero el backend de pruebas solo habla HTTP, rompiendo el enlace de verificación por email en LAN; `tailscale serve` (solo-tailnet) no bastaba porque el móvil nunca se había unido a la tailnet; resuelto con `tailscale funnel` sobre el backend de pruebas de este PC (mismo mecanismo que ya usa producción, ADR-036), build "como la release" (mismo comando y parche de CSP que `ci.yml::build-and-release`). Los 5 escenarios probados a mano en el dispositivo real. Detalle completo en `tasks.md` grupo 10.
  **Auditoría de seguridad del servidor de producción (a petición del usuario, no parte de la spec)**: sin SSH tradicional expuesto (solo Tailscale SSH, cero puertos abiertos fuera de `tailscale0`), `ufw` con *deny* por defecto, Postgres solo loopback con `scram-sha-256`, todos los ficheros de secretos en `600`, sin intentos de login fallidos ni sesiones de origen desconocido. **Hallazgo nuevo, confirmado legítimo por el usuario**: servicio **MinIO** corriendo (`systemd`, solo loopback `127.0.0.1:9000`/`9001`) sin documentar en ningún ADR — provisión anticipada para el "blob storage" mencionado como objetivo futuro en ADR-034, **todavía sin ningún código que lo use**. Borrado un backup suelto de `.env.prod` (`.env.prod.bak.*`, permisos correctos, nunca rastreado por git, solo redundante). Recordatorio, no hallazgo nuevo: `gonzalo` sigue con sudo `NOPASSWD: ALL` (ADR-033, trade-off ya aceptado). **Servidor redesplegado a `master`** (`722307f`, 9 commits que llevaba de retraso) tras la auditoría, verificado sano (`/api/ping` 200, migraciones `0001`-`0004` aplicadas).
  **Próximo hito**: fusionar PR #104/#105 en cuanto GitHub Actions se recupere; commitear el cierre de `pantallas-auth-mobile` (`tasks.md` grupo 11, este mismo resumen); `review.md` independiente; `/opsx:archive`; PR final del frontend a `master`. Si en algún momento se implementa el blob storage de MinIO, documentarlo con su propia ADR.
- **Sesión 2026-08-06 (3) — `estilizar-pagina-verificacion`: la página de verificación de email deja de ser un `<p>` plano**: cambio pequeño, `skip_specs: true` (sin comportamiento observable nuevo, solo presentación — confirmado que ningún `Requirement`/`Scenario` de `email-verification` cambia). Extraído `authPageCSS`/`authPageShell` a `apps/api/internal/auth/auth_page_layout.go`, compartido entre la página de reset de contraseña (`reset-contrasena`, ADR-039) y la de verificación — sin duplicar el CSS. De paso, consolidado `writeVerificationHTML` (duplicado de `writeHTMLPage`, ya existente) en un único helper. Verificado con `curl` (marcado `class="panel"` presente) y confirmado visualmente por el usuario en un navegador real, tanto la página de error como la de éxito. 90/90 tests Go en verde, sin tocar `reset_password_confirm_test.go` (el refactor no cambió su HTML final). Sin ADR nueva — refactor de presentación, no una decisión de arquitectura.
  **Próximo hito**: `/opsx:archive` de este cambio (más corto, probablemente sin necesitar despliegue a producción por ser solo presentación) y luego la pieza grande pendiente: pantallas de `apps/mobile` para registro/login/recuperación de contraseña, como flujo aislado que no exige login para el resto de la app todavía (decisión ya tomada con el usuario).
- **Sesión 2026-08-06 (2) — `reset-contrasena` (ADR-039): página web real con formulario, estilo "Asfalto Nocturno", verificada en local y producción**: continuación directa de la sesión anterior, tras fusionar `confirmacion-email-usuarios` (PR #100). El usuario pidió explícitamente tres garantías de seguridad antes de implementar: caducidad del enlace, que no se pueda cambiar la contraseña de otra cuenta, y que el enlace no lleve email/username — las tres se dejaron como requisitos explícitos en la spec con tests dedicados, no solo implícitas en el diseño.
  **Propuesta completa** (29 tareas) sobre `apps/api/internal/auth/`: `POST /api/auth/reset-password/request` (sin enumeración, rate limited) y `GET`/`POST /api/auth/reset-password/confirm` — a diferencia de la confirmación de email (un enlace basta), el reset necesita un formulario real para escribir la contraseña nueva, así que **esta página sí lleva ya el diseño de la app** (valores `oklch()` de `tokens.css` copiados como CSS embebido en Go, sin pipeline compartido entre `apps/api`/`apps/mobile`) — decisión tomada explícitamente con el usuario, dejando el resto de páginas (verificación de email) para un futuro cambio de frontend.
  **Garantías de seguridad verificadas con tests dedicados, no solo por diseño**: token de reset caduca en 1h (más corto que el de verificación de email, 24h — más riesgo si se filtra), y un test que intenta colar un `email`/`user_id` de otra cuenta en el `POST` del formulario confirma que la cuenta afectada se determina siempre desde el token, nunca desde ningún campo del formulario. Aprovechado para renombrar `generateVerificationToken`/`hashVerificationToken` a nombres genéricos (`generateOneTimeToken`/`hashOneTimeToken`) ya que este cambio introduce su segundo consumidor real — a diferencia del rename de `LoginRateLimiter` que se dejó como deuda en ADR-038, este se hizo en el momento por ser barato.
  **Gap real encontrado verificando en local (no en tests)**: `infra/docker/.env` seguía con el remitente sandbox de Resend de antes de verificar el dominio propio — ya había agotado su envío de prueba único, mismo error `Domain is not verified` que ya se había resuelto en producción. Corregido alineando el `.env` local al remitente ya verificado.
  **Verificación real en dos entornos**: local (formulario visto y confirmado visualmente por el usuario en un navegador real, contraseña cambiada de verdad, login con la anterior rechazado y con la nueva aceptado) y producción real, desplegada sin fusionar (mismo patrón que ADR-038: imagen anterior etiquetada para rollback). En producción, un primer intento de reset con una contraseña de 6 caracteres fue rechazado de verdad server-side (confirma la política mínima en producción, no solo en tests), y el flujo completo funcionó desde fuera del tailnet. Cuenta de prueba borrada después.
  **Próximo hito de la siguiente sesión**: `/opsx:archive` de `reset-contrasena` (con revisión independiente igual que ADR-038) y PR a `master`. Después, el frontend pendiente: pantallas de `apps/mobile` para todo este bloque de auth, y estilizar el resto de páginas de `apps/api` (verificación de email) con el mismo criterio ya sentado aquí.
- **Sesión 2026-08-06 — `confirmacion-email-usuarios` (ADR-038): login exige email verificado, con despliegue y verificación reales en local y en producción**: continuación directa de la sesión anterior — PR #98 (`rules.security`) fusionada, y se detectó que esa sección quedaba huérfana (la CLI de OpenSpec no la inyecta en ningún artefacto, solo reconoce `rules.proposal/specs/design/tasks`) — corregido en PR #99 aparte (fusionada primero, rama propia) fusionando su contenido en `context`, que sí llega a todos los artefactos.
  **Propuesta completa** (`proposal.md`/`specs/`/`design.md`/`tasks.md`, 37 tareas) sobre `apps/api/internal/auth/` ya existente (ADR-034): nueva columna `email_verified`, tabla `email_verification_tokens`, `POST /api/auth/verify-email/request` (solicitud/reenvío sin enumeración de cuentas) y `GET /api/auth/verify-email/confirm` (confirma el token, HTML mínimo — `GET` porque el enlace lo dispara un email sin frontend detrás). **Decisión BREAKING confirmada explícitamente con el usuario antes de escribir la spec**: el login pasa a rechazar cuentas sin verificar (403), no se dejó como puramente informativo.
  **Implementación en TDD real, ciclo rojo→verde confirmado en cada tarea**: 68/68 tests Go en verde, `go vet`/`govulncheck` limpios (misma excepción ya documentada). Detalle completo de las decisiones técnicas (token `crypto/rand`+`sha256` en vez de `bcrypt`, Resend vía `net/http` puro sin SDK, reutilización de `LoginRateLimiter` para el rate limit del endpoint nuevo) en ADR-038.
  **Tres gaps reales encontrados verificando contra servicios externos de verdad, ninguno detectable solo con tests**: (1) `debian:trixie-slim` (runtime del `Dockerfile` de `apps/api`) no traía `ca-certificates` — cualquier HTTPS saliente nuevo fallaba con `x509: certificate signed by unknown authority`, nunca hizo falta hasta ahora porque la única dependencia externa era Postgres sin TLS; corregido instalando el paquete. (2) Los fallos de envío de email se descartaban en silencio pese a que `design.md` prometía loguearlos — corregido con `log.Printf` (nunca el token en claro). (3) El remitente sandbox de Resend (`onboarding@resend.dev`) solo entrega de verdad una vez; a partir de ahí Resend acepta con `200` pero la entrega falla de forma asíncrona por dominio no verificado — invisible para un manejo de errores que solo mira el status HTTP síncrono. Resuelto **comprando un dominio real** (`motor-routes.com`, Cloudflare Registrar) y verificándolo en Resend — detalle completo, incluidas las alternativas descartadas (SMTP propio en el servidor doméstico, relay vía Gmail), en ADR-038.
  **Verificación real end-to-end en dos entornos, no solo simulada**: local (`docker compose up --build`, registro real, email recibido, token extraído del enlace y confirmado contra `localhost:8080` — el enlace real no es alcanzable en local sin TLS en el 443, limitación esperada) y **producción real, desplegada desde la rama sin fusionar** (mismo patrón que ADR-034: imagen anterior etiquetada para rollback, migración `0003` aplicada sola, secretos de `.env.prod` añadidos vía script remoto que nunca los imprime). En producción, con el dominio ya verificado: registro real → email recibido → enlace confirmado **desde fuera del tailnet** (`https://debian.taildf3dab.ts.net`) → login pasa de 403 a 200 → token reutilizado rechazado (400). Cuenta de prueba borrada de la base de datos de producción tras verificar, confirmado `count(*) = 0`.
  **Próximo hito de la siguiente sesión**: `/opsx:archive` (review.md, veredicto) y PR de `feature/confirmacion-email-usuarios` → `master`, referenciando este resumen y ADR-038.
- **Sesión 2026-08-05 — investigación de la release `v0.1.0` rota (ADR-036) + overlay de permiso GPS con falso positivo (ADR-037), releases `v0.1.1`/`v0.1.2`/`v0.1.3`**: tres incidentes reales sobre la primera integración `apps/mobile`↔`apps/api`↔dispositivo real de punta a punta, cada uno con causa distinta a la que parecía a simple vista — detalle completo con alternativas en ADR-036/ADR-037, aquí solo el resumen operativo.
  **Release rota, tres causas acumulativas** (usuario: "no salta el menú de tipo de parada, no sé si es la llamada HTTP o un error de compilación" — ninguna de las dos, en realidad): (1) servidor Debian de producción apagado (`tailscale status` lo mostraba `offline` desde esta misma máquina, también en el tailnet); (2) una vez encendido, backend desplegado seguía en `feature/migrar-api-golang` anterior al catálogo de tipos de parada (`404` en `/api/stop-types`) — redesplegado a `master` (`git reset --hard origin/master`, historia local divergente sin commits propios de valor); (3) el secret de GitHub `MOBILE_PROD_API_BASE_URL` estaba guardado como IP pelada (`100.114.190.36`) en vez de `http://100.114.190.36:8080` — el bundle JS horneaba esa URL rota y el `fetch()` se resolvía como ruta relativa contra el propio origen de la app, nunca contra el servidor real; el check de CI que verifica la release (`grep -qF "$PROD_API_BASE_URL"` sobre el HTML empaquetado) no lo detectó porque solo comprueba presencia del literal, no que sea una URL absoluta válida (deuda técnica anotada en ADR-036, no arreglada esta sesión). Corregido el secret, publicada `v0.1.1`. Efecto colateral real observado: instalar `v0.1.1` sobre el `v0.1.0` ya puesto puede fallar en silencio por firma de keystore de debug distinta (cada build de CI genera una nueva) — la app vieja se queda corriendo sin aviso, indistinguible en la UI de "la release sigue rota".
  **Cuarto problema, esta vez de diseño y no operativo, señalado por el propio usuario** ("cualquier persona debe poder acceder a la API sin entrar con mi cuenta"): con `v0.1.1`/`v0.1.2` ya sirviendo el catálogo bien en el dispositivo de desarrollo, resultó que `100.114.190.36` es una IP de Tailscale — solo alcanzable para dispositivos dentro del tailnet del desarrollador. ADR-033 ya eligió deliberadamente "solo Tailscale" por no tener IP pública, válido para uso interno, pero incompatible con una release pública real (un usuario cualquiera no puede unirse a la red privada del desarrollador). Corregido activando **Tailscale Funnel** en el servidor (`sudo tailscale funnel --bg http://100.114.190.36:8080` — el target por defecto, loopback, no sirve porque `apps/api` solo escucha en la IP de Tailscale) — expone `https://debian.taildf3dab.ts.net`, verificado genuinamente público (DNS público de Google, `curl` con certificado válido sin estar en el tailnet). Secret actualizado a esa URL HTTPS, publicada `v0.1.3`. Detalle completo y alternativas descartadas en la actualización de ADR-036.
  **Overlay de permiso GPS, causa distinta a la sospechada**: `navigator.permissions.query({name:'geolocation'})` (usado por `checkPermissions()` desde que existe, para evitar el crash real de `RecordingService.kt` sin permiso) se queda encallado en `'prompt'` en el WebView real de Android aunque `adb shell dumpsys package` confirme el permiso del SO ya concedido — comportamiento conocido de los WebView Android (Permissions API refleja un estado interno del WebView, no el permiso real del SO), no detectable sin dispositivo real. Diagnosticado con un toast de diagnóstico temporal + `adb exec-out screencap` (logcat inutilizado por saturación de logging JNI verbose del propio proceso — `LOGS OVER PROC QUOTA... DROPPED`). Sustituido por `probeGeolocationPermission()` (pide una localización real con `timeout: 8000`, solo `PERMISSION_DENIED` cuenta como denegación real). Primer intento de la sesión —esperar la comprobación antes del primer `render()`— revertido tras romper `cockpit.cy.ts`/`cockpit-mark-stop.cy.ts` en Cypress (Electron headless no resuelve geolocalización real sin stub) **y** ser mala UX real (cockpit en blanco con señal lenta); solución final: caché de fondo como camino rápido + repetición del sondeo real justo antes de decidir en `handleStartStop()`, sin bloquear nunca el render inicial. Nuevo comando Cypress compartido `stubGpsPermissionGranted`. PR #95 → `master`, release `v0.1.2`.
  **Verificación real repetida en dispositivo, no solo simulada**: build local por USB (nunca la release de CI directamente, para poder iterar) con el gotcha de JBR/Java 25 ya documentado en este mismo fichero (`JAVA_HOME` forzado a `jdk-24`) y el gotcha de assets desactualizados en el APK (sync manual + reempaquetado solo-Gradle) reproducidos y aplicados de nuevo tal cual estaban anotados — sin sorpresas nuevas, la documentación existente bastó. `adb shell dumpsys package`/`screencap`/`input tap`/`input swipe` (long-press simulado) para verificar sin depender de que el usuario reportara lo que veía. 814/814 Vitest, 41/41 Cypress, `tsc`/ESLint limpios en ambos cambios.
  **Docker Desktop y el backend local se pararon y volvieron a levantar varias veces** en la misma sesión para verificar `cockpit-mark-stop.cy.ts` (necesita `apps/api` real) — recordar que el pre-commit hook lo exige también en local, no solo en CI (`.husky/pre-commit` ejecuta la misma suite `pnpm test:e2e` completa).
  **Arranque de la próxima spec, "usuarios" — sesión pausada a mitad, retomar aquí**: el usuario pidió crear infraestructura de usuarios (BBDD, autenticación, confirmación por email). Antes de proponer nada se investigó `apps/api/internal/auth/` y resultó que **la mayor parte ya existe** desde `migrar-api-golang` (ADR-034) — `spec/user-auth` ya está en `openspec/specs/`: registro (`register.go`), contraseñas con `bcrypt` (`password.go`), login + JWT (`login.go`/`token.go`), middleware de autenticación, rate limiting de login. **Lo único que falta de verdad**: confirmación de email (sin columna `email_verified`, sin envío de correo, sin tabla de tokens de verificación) — el resto de lo pedido ya estaba resuelto, evitó duplicar trabajo. Decisiones ya tomadas con el usuario para la spec de confirmación de email (pendiente de abrir con `/opsx:propose`): proveedor transaccional **Resend** (no SMTP propio, la máquina doméstica no tiene reputación de IP saliente), **sin reset de contraseña todavía** (spec aparte más adelante), alcance solo backend (sin frontend por ahora).
  **Paso previo ya hecho, PR #98 abierta, pendiente de que el usuario la revise/fusione**: nueva sección `rules.security` en `openspec/config.yaml` (secretos nunca en código/commits ni "para probar", nada de criptografía o parseo de tokens a mano, respuestas de registro/login/reset sin permitir enumeración de cuentas, rate limiting explícito en cualquier endpoint de auth nuevo, checklist CRÍTICO ampliado en `operations.archive.guidance` para cambios de autenticación) — se adelantó a la spec de usuarios porque va a tocar justo esa superficie. Validado con `openspec validate --all` (11/11 specs existentes siguen pasando). Rama `docs/openspec-normas-seguridad`.
  **Próximo hito de la siguiente sesión**: (1) fusionar PR #98 si el usuario da el visto bueno al texto; (2) `/opsx:propose` para la confirmación de email sobre `apps/api/internal/auth/` (nueva rama desde `master`), citando `spec/user-auth` ya existente como lo que NO hay que reimplementar.
- **Sesión 2026-08-05 — `README.md` actualizado para reflejar el monorepo (`actualizar-readme-monorepo`)**: cambio puramente documental (`skip_specs: true`, sin `design.md` por no aplicar ninguna de sus condiciones — un solo fichero, sin dependencia nueva, sin ambigüedad técnica). El README de la raíz seguía describiendo la app como si viviera entera en la raíz del repo (comandos `npm run dev`, rutas `src/`), desalineado desde `entorno-api-docker` (2026-08-03, ADR-032). Reescrito con el stack real (`apps/mobile` TS+Vite+Tauri+Rust, `apps/api` Go 1.25+chi+pgx+PostgreSQL, `infra/docker/`), scripts reales (`pnpm` desde `apps/mobile`, `docker compose up --build` desde `infra/docker`) y el árbol de carpetas actual. Cada comando y ruta citados se verificó contra el repo real (`package.json`, `go.mod`, `docker-compose.yml`, `.husky/pre-commit`) antes de escribirlos. Sin cambios de código; único fichero tocado además de los artefactos del propio cambio OpenSpec.
- **Sesión 2026-08-04 — `apps/api` migrado de Java/Spring Boot a Go (`migrar-api-golang`, ADR-034), con autenticación de usuarios y desplegado en producción real**: cambio completo `propose→apply` (archive pendiente) sobre la decisión de ADR-032. Motivado por funcionalidad ya conocida (a diferencia de ADR-032, que eligió Spring Boot sin saber qué traería el crecimiento): validación/autenticación de usuarios, apartado de seguridad explícito, y preparar el terreno para blob storage tipo Azure y notificaciones en tiempo real tipo SignalR (ninguna de las dos implementada, solo no bloqueadas). Decisión Go sobre Rust confirmada con el usuario antes de proponer: SDK de Azure Blob Storage más maduro, librerías JWT/OAuth más asentadas, goroutines más naturales para tiempo real — el trade-off descartado fue reutilizar la disciplina Rust ya cableada en `apps/mobile/src-tauri`.
  **Stack Go nuevo** (detalle completo con alternativas en ADR-034): `chi` (router/middleware sobre `net/http`, no un framework completo), `jackc/pgx/v5` (driver Postgres), `golang-jwt/jwt/v5` + `golang.org/x/crypto/bcrypt` (tokens de sesión y hashing), runner de migraciones propio (ficheros `.sql` + tabla `schema_migrations`, se ejecuta solo al arrancar el binario — decisión corregida en `apply`: inicialmente se pensó en aplicar la migración de producción a mano por SSH como la tabla dummy de ADR-033, pero al ser un mecanismo automático embebido en el binario no hace falta, corre igual contra el Postgres nativo), rate limiting de login en memoria (mapa+mutex, sin Redis, aceptado mientras sea una sola instancia).
  **Gap real de seguridad encontrado en `apply`, no detectable antes de tener código**: al llegar a la tarea de auditoría (`govulncheck`, primera vez que se audita Go en este proyecto) aparecieron 2 vulnerabilidades reales y alcanzables desde el propio código — `GO-2026-5004` (inyección SQL por confusión de placeholders en `pgx@v5.8.0`, alcanzable desde `PostgresUserStore.FindUserByID`) y `GO-2026-5970` (bucle infinito en `x/text@v0.29.0`). Ambas versiones vulnerables habían sido fijadas deliberadamente para ser compatibles con Go 1.24 (la versión que trae el paquete `apt` `golang-1.24` de Debian trixie, elegida en un primer momento por coherencia con cómo se instaló el JDK en ADR-032). Las versiones que corrigen ambos CVEs exigen Go 1.25, que trixie no empaqueta todavía — resuelto cambiando la etapa de build de `apps/api/Dockerfile` a la imagen oficial `golang:1.25-trixie` (confirmado que el tag existe) en vez del paquete `apt`, manteniendo `debian:trixie-slim` sin cambios en el runtime. `govulncheck` quedó en 0 vulnerabilidades alcanzables tras el ajuste. Se dejó `govulncheck` cableado en `.husky/pre-commit` (extendiendo `pre-commit-audit-gate.spec.ts`, no duplicándolo) y como job `quality-go` nuevo en `ci.yml`.
  **TDD real en todo el flujo**: cada requisito de `user-auth`/`api-security` con su ciclo rojo→verde confirmado ejecutando `go test` de verdad (no solo revisión de código) — incluye tests de integración reales contra PostgreSQL (vía Docker local) para el runner de migraciones y el `PostgresUserStore`, no solo mocks. **Verificación E2E real repetida en tres entornos**: local sin Docker (binario Go directo contra Postgres del compose), Docker Compose local completo (`docker compose up --build`, ciclo `down`/`up` sin `-v` para confirmar persistencia, `docker stop postgres` para confirmar 503 controlado — tardó ~7.8s por resolución DNS de Docker al parar el contenedor, no por el timeout de conexión que se añadió como red de seguridad adicional; se aceptó por no ser "colgado indefinido", el requisito real), y **producción real** (ver más abajo).
  **Despliegue real en el servidor de producción, sobre la rama sin mergear todavía** (decisión explícita del usuario: desplegar desde `feature/migrar-api-golang`, no esperar al merge — precedente ya sentado por el propio ADR-033 de clonar y construir directamente en el servidor). Encontrado el usuario SSH real del servidor por prueba (Tailscale SSH deniega por política a varios candidatos hasta dar con `gonzalo`, con sudo sin contraseña — coincide con lo ya documentado en ADR-033, solo que esta sesión no tenía el dato a mano por estar redactado en memoria). **Antes de tocar nada**: imagen Java etiquetada `docker-api:pre-go-migration` para rollback; `infra/docker/.env.prod` transformado en el propio servidor vía script remoto que nunca imprime valores reales (`DB_URL`/`DB_USERNAME`/`DB_PASSWORD` → `DATABASE_URL` recompuesto; `AUTH_TOKEN_SECRET` nuevo generado con `/dev/urandom`, no existía antes). Verificación real post-despliegue: `curl` → 200; `ss -tlnp` confirma que solo escucha en la IP de Tailscale; registro/login/`me` reales contra producción (limpiados después); `schema_migrations` confirma la migración aplicada sola, sin paso manual por SSH; `systemctl restart docker` → el contenedor vuelve solo; `pg_hba.conf`/`postgresql.conf` no tocados (por construcción, el mecanismo nunca los toca). **Antes de este despliegue se auditó explícitamente, a petición del usuario, que ningún secreto real quedara en algo commiteable** (`.env`/`.env.prod` fuera de git, valores de ejemplo triviales, `git grep` sin coincidencias de secretos reales).
  **Entorno de esta máquina de desarrollo cambiado**: Go 1.26.5 instalado (`winget install GoLang.Go`, no existía antes) — el `go.mod` de `apps/api` fija `go 1.25` como mínimo, coherente con la imagen de build. `govulncheck` instalado vía `go install` (mismo patrón que `cargo-audit`, ya en `PATH` de usuario). Docker Desktop no estaba arrancado al empezar la sesión, se inició a mano.
  **Cerrado más tarde en la misma sesión**: `review.md` (veredicto APPROVED WITH MINOR ISSUES), `/opsx:archive`, PR #92 pasada de draft a lista y **mergeada a `master`** (confirmado por el usuario). Cambio archivado en `openspec/changes/archive/2026-08-04-migrar-api-golang/`.
- **Sesión 2026-08-04 (2) — catálogo de tipos de parada (`catalogo-tipos-parada`, ADR-035), primera llamada real `apps/mobile`→`apps/api`**: continuación directa de la sesión anterior, sobre `master` ya con el backend Go. Nueva tabla `stop_types` (8 categorías sembradas, icono emoji) servida por `GET /api/stop-types` (único endpoint público además de `/api/ping`), cacheada en SQLite local en el móvil. En el cockpit, pausar la grabación abre un modal para elegir el tipo de la parada (catálogo cacheado, funciona offline); el timeline y el mapa dejan de recalcular/detectar paradas por velocidad y pasan a leer solo paradas manuales con tipo persistidas en `route_stops` — una parada automática (semáforo) nunca se muestra ni se persiste.
  **Corrección de diseño a mitad de `apply`, confirmada con el usuario**: el modal iba a saltar también en paradas detectadas por GPS; corregido para que solo lo dispare una acción manual explícita del usuario.
  **Tres gaps reales encontrados durante la verificación (grupo 8), ninguno detectable sin backend real ni dispositivo real**: (1) `apps/api` no enviaba cabeceras CORS — el navegador bloqueaba en silencio el `fetch` del catálogo, `refreshStopTypesCache` (best-effort) lo confundía con un fallo de red normal; corregido con `internal/httpmw/cors.go::PublicCORS` (`Access-Control-Allow-Origin: *`, acotado a esa única ruta pública). (2) Verificado en dispositivo Android real (Realme `75fe536b`, misma WiFi, CSP/`.env.local` editados solo en local y revertidos después): el botón dedicado "Marcar parada" confundía al usuario, que esperaba que fuera el mismo botón que ya pausa la grabación — consolidado sobre "Pausar" (cancelar el modal no revierte la pausa). De paso, investigado un tercer síntoma que **no** era un bug de este cambio: el overlay de permiso GPS parecía "colgado" — `getCurrentPosition()` no lleva `timeout`, espera indefinidamente sin feedback hasta fijar posición y luego arranca la grabación sola; permiso de Android ya estaba concedido, pre-existente, dejado pendiente a petición del usuario. (3) La release de GitHub Actions (`ci.yml`, tag `v*`) no tenía ningún mecanismo para apuntar al servidor real — habría publicado un APK que solo sabe hablar con `localhost`; corregido con un secret nuevo `MOBILE_PROD_API_BASE_URL` (ya configurado por el usuario) que se inyecta en el runner (nunca comiteado) tanto en `VITE_API_BASE_URL` como en el CSP de `index.html`/`tauri.conf.json` — este último confirmado que `--config` de Tauri **no** lo reescribe para Android, gap documentado en ADR-035.
  **Verificación real, no solo tests**: `docker compose up` con backend real + nuevo test E2E Cypress (`cockpit-mark-stop.cy.ts`, backend real sin mocks — primera vez que la suite E2E ejercita la integración mobile→API de verdad) + build/instalación repetida en dispositivo Android real (gotcha de frontend desactualizado en el APK, ya documentado, reproducido 3/3 veces esta sesión, siempre resuelto con el fix manual de assets + rebuild solo-Gradle). **814/814 tests Vitest, 41/41 Cypress, `tsc`/ESLint/`go vet` limpios.** **Cerrado en la misma sesión**: `review.md` (veredicto APPROVED WITH MINOR ISSUES, único issue no bloqueante: el mecanismo de CI que apunta al servidor real aún no se ha probado en un runner real), `/opsx:archive` (nace `openspec/specs/{stop-types-catalog,cockpit-manual-stops,route-stop-types-display}/`), **PR #93 abierta** (`feature/catalogo-tipos-parada` → `master`, CI en marcha). Cambio archivado en `openspec/changes/archive/2026-08-04-catalogo-tipos-parada/`.
- **Sesión 2026-08-03 (3) — criterio explícito de qué merece una ADR (`criterio-adr`, ADR-048)**: nació de una pregunta del usuario ("qué es una ADR y para qué se usa") que al bajar al detalle destapó un gap real: `openspec/config.yaml` mandaba crear ADRs desde **tres** claves (`rules.design:90-94`, `rules.tasks:118-120`, `operations.archive.guidance:205-206`) y **ninguna definía el umbral** — la única pista era «una decisión nueva de ese calado», sin explicar en ningún sitio. Además dos de las tres empujaban la redacción al cierre del cambio, que es la causa medible de que el log se inflara: ADR-013 a ADR-018 promedian ~800 caracteres frente a los ~4.400 de ADR-031 a ADR-033 y los 12.872 de ADR-027, con la diferencia ocupada por narrativa de sesión (`sha256sum`, salida de `ss -tlnp`) que este mismo fichero ya recoge en paralelo.
  **Hallazgo mecánico que decidió la ubicación**: `rules:` está indexado **por artefacto, no por comando** — `openspec instructions design` devuelve `rules.design` y ninguna entrada de `rules.proposal`. No existe un punto de inyección "al proponer" en general; como `/opsx:propose` escribe también `design.md`, el momento de propose se alcanza vía `rules.design`, que además ya era la única clave que mandaba *crear* la ADR. Verificado en las dos direcciones tras editar: el criterio aparece en `rules.design` y **no** aparece en `rules.proposal`.
  **Cambios**: `rules.design` sustituye su primera entrada por el criterio (tres disparadores, basta uno: coste de reversión alto / alternativas reales evaluadas / hallazgo contraintuitivo que el código no muestra — más lista negativa explícita, que es lo único que separa una ADR de este fichero). `operations.archive.guidance` pasa de «añadir la ADR» a **verificar** que ya quedó registrada en propose, añadiendo una nueva solo si surgió durante `apply`. Las otras dos menciones (`rules.proposal`, `rules.tasks`) se releyeron y se dejaron intactas por coherentes — anotado que «cerrar con una tarea de … `memory/decisions.md`» de `rules.tasks` podría malinterpretarse como "redactar la ADR al final"; no se tocó por estar fuera del alcance declarado, candidato a afinar si deriva.
  **Nada retroactivo** (decisión explícita): ADR-013 a ADR-033 intactas en contenido, orden y numeración, incluidos el hueco 001-012 y el desorden 026/024 — verificado con `git diff` que el cambio a `decisions.md` son 13 adiciones y 0 borrados. Editar ADRs aceptadas destruiría justo la propiedad que las hace valer. **Sin test estructural** pese al precedente de `ci-workflow.spec.ts`: aquél afirma sobre estructura ejecutable, éste afirmaría sobre prosa destinada a reformularse. ADR-048 se redactó cumpliendo su propio criterio (3.872 caracteres, sin narrativa de sesión) — de hecho hubo que recortarla en la propia implementación al detectar que arrastraba una nota de deuda, que es "estado" y va aquí, no en una ADR.
  **Deuda anotada, no atacada**: `docs/05-memory-system.md` §2 documenta el formato de ADR con ejemplos **inventados** (`ADR-001: Usar PostgreSQL`, `ADR-002: Estrategia de branching`) que nunca han existido en este log — candidato a un cambio propio.
  **Cerrado con `/opsx:archive`: veredicto APPROVED WITH MINOR ISSUES** (`review.md`, 11 comprobaciones re-ejecutadas de forma independiente, incluida la inyección real en los 4 artefactos y `pnpm docs:prepare`). El gate se ejecutó bajo la guía que el propio cambio acababa de reescribir — `openspec instructions archive` ya servía el texto nuevo. Dos issues no bloqueantes: (1) **`rules.tasks` es ahora load-bearing** — la ADR se decide en `design.md` durante propose, pero el único puente para que aterrice en `memory/decisions.md` es esa entrada; si alguien la recorta creyendo que `archive` se encarga, las ADRs dejarían de aterrizar sin que nada lo señale; (2) el énfasis markdown `**Merece ADR**` es el único del fichero, que en el resto marca énfasis en mayúsculas — cosmético, sin impacto funcional. Sin ADR nueva en el archivado (ninguna decisión de calado surgió durante `apply`), aplicando la guía nueva. Cambio archivado en `openspec/changes/archive/2026-08-03-criterio-adr/`. Rama `feat/adr-explaination-adr`, PR #91 abierta pendiente de fusionar (nota añadida al rebasar/fusionar el PR el 2026-08-17, sesión posterior — el número final ADR-048, no ADR-034, refleja las 14 ADRs añadidas a `master` entre la redacción original y la fusión; ninguna decisión de esta ADR cambió).
- **Sesión 2026-08-03 (2) — primer despliegue real: `apps/api` corriendo en el servidor de producción (`despliegue-servidor-tailscale`, ADR-033)**: continuación directa de `entorno-api-docker` (misma sesión, ver más abajo). El servidor real es un Debian 13 doméstico sin IP pública, alcanzable solo por Tailscale (`[tailscale-ip-redactada]` MagicDNS `debian`, usuario `[usuario-redactado]` con sudo) — **Tailscale se instaló en esta misma máquina de desarrollo** (`winget install tailscale.tailscale`) para poder desplegar por SSH desde la propia sesión; resultó estar ya autenticada de una instalación previa, sin necesidad de login interactivo.
  **Hallazgos reales por SSH que contradecían lo asumido**: (1) Docker no estaba instalado (se asumía que sí); (2) ya existía un **PostgreSQL 17 nativo** (no Docker) con `appdb`/`appuser` creados de antemano por el propio usuario específicamente para este despliegue — confirmado explícitamente que no es de otro proyecto — solo aceptando conexiones por loopback (`pg_hba.conf` sin exposición de red, sin `listen_addresses` personalizado); (3) Debian trixie solo empaqueta `docker-compose` v1 (Python, EOL), no el plugin `docker compose` v2 — resuelto añadiendo el repositorio oficial de Docker, **verificado por `curl` en esta sesión que sirve paquetes reales para `trixie`** (había reportes contradictorios sobre el soporte, se comprobó en vez de asumir) — Docker 29.7.1 + Compose v5.3.1 instalados (misma versión de Compose que el entorno local).
  **Decisiones de diseño** (detalle completo en [[ADR-033]]): el contenedor de `apps/api` usa `network_mode: host` para hablar con el Postgres nativo por `127.0.0.1` sin tocar su configuración (verificado con `sha256sum` de `pg_hba.conf`/`postgresql.conf` antes y después — idénticos); nueva propiedad `server.address=${SERVER_ADDRESS:0.0.0.0}` en `apps/api/src/main/resources/application.properties` (default `0.0.0.0` para no romper el entorno local) para que la API solo escuche en la IP de Tailscale, no en la LAN doméstica del servidor — confirmado con `ss -tlnp` que el puerto 8080 solo aparece asociado a `[tailscale-ip-redactada]`. Nuevo `infra/docker/docker-compose.prod.yml` (solo servicio `api`, `restart: unless-stopped`) — no se reutiliza el compose local, que levanta su propio Postgres y chocaría por el puerto 5432 ya ocupado por el nativo. Credenciales de `appuser` (regeneradas dos veces en la sesión — la primera copia se borró por higiene antes de guardarla donde tocaba, y hubo que regenerarla) en `infra/docker/.env.prod` en el propio servidor (`chmod 600`, no versionado), con `.env.prod.example` como plantilla versionada.
  La tabla dummy `healthcheck` se creó a mano vía `psql` (sin mecanismo `docker-entrypoint-initdb.d` en un Postgres nativo) — **gap real encontrado**: `sudo -u postgres psql -f ~/moto-routes/...` falla con "Permission denied" porque el usuario OS `postgres` no puede leer dentro del home de `[usuario-redactado]`; resuelto pasando el SQL por stdin. Tabla creada por `postgres` (superusuario), con `GRANT` explícito a `appuser` después (el endpoint actual usa `SELECT now()`, no toca todavía esta tabla).
  **Verificación real end-to-end, no solo local**: `curl http://[tailscale-ip-redactada]:8080/api/ping` desde la máquina de desarrollo (mismo tailnet) → 200 con timestamp real; `ss -tlnp` confirma exposición acotada a Tailscale; `sudo systemctl restart docker` → el contenedor vuelve solo (`restart: unless-stopped`) y sigue respondiendo. Verificado también que ningún fichero rastreado por git contiene la contraseña real (`git grep`).
- **Sesión 2026-08-03 — monorepo + primer backend (`entorno-api-docker`, ADR-032)**: primer paso hacia un servidor propio de Moto Routes (hasta ahora solo SQLite local). Reorganización a monorepo (`apps/mobile/` + `apps/api/` + `infra/docker/`) en el mismo cambio que el backend nuevo, porque la reorganización solo tenía motivo por su llegada.
  **Reorganización**: `git mv` de todo el contenido de raíz de la app móvil a `apps/mobile/` (preserva historial). **Gap real encontrado durante `apply`, no previsto en la propuesta**: `typedoc.json`/`scripts/docs-coverage.mjs`/`scripts/docs-prepare.mjs` leen `memory/`/`specs/ui/` y escriben en `docs/` — todos transversales, no exclusivos de la app móvil — así que NO se movieron; en su lugar, `package.json` raíz nuevo (solo scripts `docs:*` + `husky`, tres `devDependencies`, ninguna nueva) y `pnpm-lock.yaml` se quedan en la raíz (un workspace pnpm tiene un único lockfile). `pnpm-workspace.yaml` → `packages: [apps/mobile]`. CI (`ci.yml`) y `.husky/pre-commit` actualizados con TDD real (test en rojo confirmado antes de tocar cada fichero, verde después): `ci-workflow.spec.ts` y `pre-commit-audit-gate.spec.ts` ahora leen `.github/`/`.husky/` con `../../` (viven en `apps/mobile/src/` pero esos ficheros son transversales). **Segundo gap real, en Rust**: `cargo clippy` falló tras el `git mv` con un error de build script inexplicable a primera vista — la carpeta `target/` (gitignored, pero movida igualmente por ser un `git mv` de directorio a nivel de filesystem) traía cacheado un `OUT_DIR` con la ruta absoluta *anterior* al movimiento; resuelto con `cargo clean`. Sin regresiones: 759/759 Vitest, tsc/ESLint limpios, cargo test 5/5, 39/39 Cypress.
  **Backend nuevo (`apps/api/`)**: Java 21 + Spring Boot 3.5.13 + Maven (wrapper `mvnw` generado con el plugin oficial vía un contenedor Maven desechable, sin instalar Maven en local — coherencia con "todo en Docker"). Único endpoint `GET /api/ping` (`PingController`/`PingService`/`PingResult`) que ejecuta `SELECT now()` de verdad contra Postgres — 200 con el timestamp real si hay conexión, 503 controlado si no la hay. Test unitario (`PingControllerTest`, JUnit+Mockito+AssertJ, sin contexto Spring) con TDD real: rojo (error de compilación) confirmado antes de implementar, verde (2/2, `BUILD SUCCESS`) después, ejecutado dentro de un contenedor Maven (`docker run ... mvn test`) al no haber Maven instalado en local.
  **Docker**: `apps/api/Dockerfile` multi-stage, ambas etapas `debian:trixie`/`debian:trixie-slim` — confirmado en un contenedor real que `openjdk-21-jdk-headless`/`openjdk-21-jre-headless` y `maven` (3.9.9-1) existen tal cual en los repos de trixie (`eclipse-temurin`, la imagen oficial de OpenJDK, no publica todavía ninguna variante trixie). `infra/docker/docker-compose.yml` (`postgres:16-trixie` + `api`, `healthcheck`/`depends_on: condition: service_healthy`, volumen persistente). **Tercer gap real**: la propuesta inicial contemplaba credenciales de Postgres inline en el compose (justificadas como "no es un secreto real" por ser solo de desarrollo) — corregido antes de implementar porque la regla del proyecto de "nunca secretos en código" no tiene esa excepción; ahora vía `infra/docker/.env` (no versionado) + `.env.example` (versionado, documenta las claves). **Verificación end-to-end real con Docker** (no solo tests): `docker compose up --build` arranca ambos contenedores; `curl /api/ping` → 200 con timestamp real; `INSERT` manual + `docker compose down`/`up` (sin `-v`) → la fila persiste; `docker stop` de `postgres` con la API arriba → 503 controlado, sin crash.
  **Nueva ADR-032** en `memory/decisions.md` (decisiones de arquitectura: monorepo por `apps/`, Spring Boot sobre `HttpServer` plano, imágenes Debian trixie vía `apt` en vez de `eclipse-temurin`, sin Flyway/Liquibase todavía). Tres nuevas capabilities sincronizadas a `openspec/specs/`: `monorepo-layout`, `api-backend`, `local-dev-environment` (12/12 escenarios con verificación real, ninguno sin ningún tipo de verificación). **Cerrado con `/opsx:archive`: veredicto APPROVED WITH MINOR ISSUES** (`review.md`, verificación independiente con re-ejecución completa de toda la suite — TS, Rust, Java, Docker E2E — no solo revisión de código). Dos issues pendientes no bloqueantes, ambos dependientes de un entorno al que esta sesión no tiene acceso, no de comportamiento incorrecto conocido: (1) el `ci.yml` actualizado tiene cobertura estructural fuerte (`ci-workflow.spec.ts`) pero no se ha ejecutado todavía en un runner real de GitHub Actions — se confirma con la propia PR, mismo criterio que ya advertía ADR-031 sobre los límites de un test estructural; (2) `pnpm tauri android build` no se ha vuelto a ejecutar tras el movimiento a `apps/mobile/` — no obligatorio por criterio del propio proyecto (cambio puramente estructural, no toca GPS/cámara/persistencia/permisos/mapa), pero recomendado como build de humo antes de dar la reorganización por cerrada del todo. Cambio archivado en `openspec/changes/archive/2026-08-03-entorno-api-docker/`. Rama `feature/entorno-api-docker`, sin PR abierta todavía.
- **Sesión 2026-08-02 (6) — CI/CD real en GitHub Actions (`ci-cd-pipeline`, ADR-031)**: hasta ahora el único workflow (`docs.yml`) estaba roto y ningún gate de calidad corría server-side — todo vivía solo en `.husky/pre-commit`, saltable. Segundo cambio OpenSpec real sobre código (`propose→apply→archive` completo). **Un solo workflow `.github/workflows/ci.yml` con 3 jobs** (no 3 ficheros — `needs:` solo funciona dentro del mismo fichero, `workflow_run` habría sido más lento): `quality-ts` (tsc, cobertura de docs, ESLint, Vitest 80%, Cypress E2E) y `quality-tauri` (fmt, clippy, cargo test, cargo audit con la misma excepción `RUSTSEC-2023-0071`) en cada push a `master` y cada PR; `build-and-release` (`needs: [quality-ts, quality-tauri]`, solo en tags `v*`) compila el APK Android y lo publica como asset de un GitHub Release — debug sin firma (decisión confirmada, sin secretos nuevos). Trigger ya estaba bien acotado desde el principio (`push.branches: [master]`, nunca ramas sueltas — lo que parecía "correr en cada rama" era el trigger `pull_request` re-disparándose en cada commit de la PR abierta, comportamiento correcto).
  **Hallazgo real durante `propose`**: `src-tauri/.cargo/config.toml` tenía hardcodeada una ruta de Windows de esta máquina en concreto para el linker del NDK — inservible en cualquier runner. Resuelto sobreescribiendo `CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER` por variable de entorno del job, sin tocar el fichero (sigue haciendo falta tal cual en local).
  **3 gaps reales encontrados en la primera ejecución de verdad en GitHub Actions** (tag de prueba `v0.0.1-test`, ninguno detectable con el test estructural local — exactamente para lo que sirve verificar en CI real y no solo confiar en el test): (1) `sdkmanager` no está en `PATH` por defecto en `ubuntu-latest` pese a traer el SDK preinstalado — localizado dinámicamente con `find`; (2) `${{ env.ANDROID_HOME }}` en un campo estructurado (`path:` de `actions/cache`) es una expresión inválida si no se declara en un bloque `env:` del workflow — variables de entorno del runner en tiempo de ejecución no cuentan, hay que promocionarlas vía `$GITHUB_ENV` antes; (3) el `GITHUB_TOKEN` automático solo tiene `contents: read` por defecto, `softprops/action-gh-release` fallaba con 403 — añadido `permissions: contents: write` acotado solo al job `build-and-release`. Tras corregir los 3, el tag de prueba compiló, verificó (mismo comando de `memory/context.md` para detectar frontend desactualizado) y publicó el Release correctamente — tag/release de prueba borrados después.
  Test de regresión nuevo `src/shared/ci/ci-workflow.spec.ts` (22 aserciones, texto plano sobre el YAML, sin parser nuevo — mismo patrón que `pre-commit-audit-gate.spec.ts`). **754/754 Vitest, `tsc`/ESLint limpios.** PR #87 (`feature/ci-cd-pipeline` → `master`), todos los checks de CI en verde salvo el `Docs` preexistente sin relación. **Cerrado con `/opsx:archive`: veredicto APPROVED** (`review.md`, 15/15 escenarios verificados — varios con ejecución real en GitHub Actions, no solo estructural). Nace `openspec/specs/ci-cd/spec.md` (segunda spec "oficial" del proyecto tras `security-audit`). Cambio archivado en `openspec/changes/archive/2026-08-02-ci-cd-pipeline/`. Pendiente: mergear PR #87.
- **Sesión 2026-08-02 (5) — flujo de Git explícito (ADR-029) + GitHub Project "Moto Routes" (ADR-030)**: revisión a petición del usuario de qué disciplina se había perdido en la migración a OpenSpec (rama/PR nunca automatizados ni documentados desde entonces — se llegó a ejecutar `propose→apply→archive` de `auditoria-seguridad` directo sobre `master` sin que nada lo señalara; `master` sin branch protection en GitHub). Decisión (confirmada con el usuario, tres niveles de enforcement planteados): **solo disciplina documentada**, sin hook técnico ni branch protection ni CI nuevo por ahora — nueva sección "Flujo de Git y Project" en `CLAUDE.md`/`.clinerules/00-project-rules.md` (idéntica en ambos), rama `feature/<nombre>` + PR obligatorios pero sin gate técnico. Corregida de paso una guía obsoleta en `openspec/config.yaml` que decía que Cypress estaba desactivado en pre-commit (ya no es así). Commit/PR de esta parte: rama `chore/flujo-git-openspec`, PR #86 (abierta, sin mergear todavía).
  **GitHub Project**: creado "Moto Routes" en la organización `crzverde` (dueña real del repo, no un usuario) — [github.com/orgs/crzverde/projects/1](https://github.com/orgs/crzverde/projects/1), requirió ampliar el scope del token (`gh auth refresh -h github.com -s project -s read:project`, login interactivo). Una card por **cambio de OpenSpec, nunca por tarea** (respeta ADR-027, que ya decidió no crear issues por tarea). Campo custom `Fase` (`Backlog`→`Propuesto`→`En progreso`→`En revisión`→`Hecho`) porque el campo `Status` nativo no se puede borrar ni reeditar sus opciones vía `gh` CLI. Cards iniciales: PR #85 (auditoria-seguridad, mergeada) → `Hecho`; PR #86 (flujo-git-openspec, abierta) → `En revisión`; backlog nuevo para la verificación de GPS en background pendiente desde hace sesiones. Limpieza: cerrados los issues huérfanos #53 y #51 (pre-OpenSpec, ya resueltos por trabajo posterior) con comentario explicando el estado real. Detalle completo de alternativas descartadas en [[ADR-030]].
- **Sesión 2026-08-02 (4) — fix crash real de "Grabar" (permiso GPS) + primer cambio OpenSpec end-to-end sobre código (`auditoria-seguridad`)**: dos piezas de trabajo sobre `master` ya con OpenSpec activo.
  **(1) Fix de crash reportado por el usuario**: al pulsar "Grabar" la app crasheaba de verdad (force-close, no solo un error JS) — diagnosticado con `adb logcat` en tiempo real: `RecordingService.kt:78` llama `startForeground(type=location)` sin que Android tuviera el permiso de ubicación realmente concedido (`SecurityException` fatal, Android 14+, `targetSDK=36`). Causa raíz de código real: `createBrowserGpsProvider().checkPermissions()` (`src/cockpit/cockpit.service.ts`) no comprobaba el permiso de verdad, solo `navigator.geolocation !== null` (casi siempre `true`), así que `hasGpsPermission` quedaba `true` de mentira tras un permiso revocado (p. ej. por una reinstalación del APK, que resetea permisos runtime) y el overlay de "pedir permiso" nunca se disparaba. Fix: usa `navigator.permissions.query({name:'geolocation'})` con fallback al comportamiento anterior si la Permissions API no está disponible (jsdom en tests, WebView muy antiguo). Tests de regresión nuevos en `cockpit.service.spec.ts`. Mergeado a `master` junto con el fix ya conocido de `perfil-usuario` (modelos de fabricante mixto) — PR #84, commit `d4d7a81`.
  **(2) `auditoria-seguridad` — primer `/opsx:propose` + `/opsx:apply` real de este proyecto sobre código (no tooling/docs, a diferencia de `migrar-sdd-a-openspec`)**: motivado por un gap real encontrado — `docs/06-seguridad.md` exigía desde hace tiempo que el pre-commit bloqueara en vulnerabilidades `high`/`critical`, pero `.husky/pre-commit` nunca ejecutó ni `npm audit` ni `cargo audit`. **Gap de diseño encontrado durante `apply`** (documentado en `openspec/changes/auditoria-seguridad/design.md`): `cargo audit` no tiene niveles de severidad (`--deny high` no existe, a diferencia de `npm audit --audit-level`) — por defecto ya bloquea en cualquier vulnerabilidad real y no bloquea en avisos `unmaintained`/`unsound`/`yanked`, así que no hacía falta ningún flag para el comportamiento deseado. **Resultado**: `pnpm audit` pasó de 10 vulnerabilidades (7 high, 3 moderate) a **0** — causa real: `vitepress@1.6.4` (última estable, no hay una posterior que lo arregle) arrastra internamente `vite@5.4.21`/`esbuild@0.21.5` viejos pese a que el `vite` de nivel superior del proyecto ya estaba en la versión segura `6.4.3`; corregido con overrides en `pnpm-workspace.yaml` (`vite: ^6.4.3` global, `postcss: 8.5.25` actualizado desde el `8.5.16` que se había quedado atrás, y `brace-expansion` acotado por padre — `minimatch@3>`/`@9>`/`@10>` — para sus 3 líneas major en el árbol sin forzar un major que su consumidor no espera). `cargo audit` quedó con **1 excepción documentada**: `RUSTSEC-2023-0071` (`rsa`, Marvin Attack, sin fix disponible, vía `sqlx-mysql` transitivo de `tauri-plugin-sql` — MySQL nunca se usa en runtime, código inalcanzable), ignorada explícitamente con comentario en el propio hook. Nuevos tests de regresión de configuración en `src/shared/http/` (mismo sitio que ya existía para leer `tauri.conf.json`, no un dominio nuevo): `tauri-conf.spec.ts` ampliado (CSP de `tauri.conf.json` e `index.html` idénticas, sin `unsafe-eval`/`unsafe-inline`) y `capabilities-allowlist.spec.ts` nuevo (permisos de `capabilities/default.json` no crecen sin que un test lo note). Los 4 comandos Rust sin validar (`app_info` y los 4 del foreground service) confirmados sin riesgo — solo reciben `AppHandle`, no input de usuario. Verificación en dispositivo Android real marcada **no aplicable** (confirmado con el usuario): ningún cambio de este audit toca código empaquetado en el APK. **733/733 tests Vitest** (+4 de este cambio), `tsc`/ESLint limpios, `cargo test` 5/5, `cargo clippy`/`fmt` limpios, `pnpm run docs` construye sin errores tras los overrides. Reafirma [[ADR-014]] sin abrir una ADR nueva (no hubo decisión de arquitectura más allá de lo que ADR-014 ya cubre). **Cerrado con `/opsx:archive`: veredicto APPROVED** (`review.md`, 8/8 escenarios con verificación — automatizada o de herramienta real documentada; un gap de cobertura encontrado durante la propia revisión, `pre-commit-audit-gate.spec.ts`, corregido en la misma sesión antes del veredicto). Spec nueva nacida en `openspec/specs/security-audit/spec.md` (primera vez que `openspec/specs/` deja de estar vacío). Cambio archivado en `openspec/changes/archive/2026-08-02-auditoria-seguridad/`.
- **Sesión 2026-08-02 (2) — MIGRACIÓN A OPENSPEC COMPLETADA (cambio `migrar-sdd-a-openspec`, primer `/opsx:propose` real del proyecto)**: el repositorio había quedado describiendo dos metodologías a la vez — el commit `801fdd2` borró `agents/` (la capa agnóstica que leían Cline y Claude) sin reubicar su contenido, dejando ~20 referencias muertas en `CLAUDE.md`, `.clinerules/`, `docs/01`, `03`, `05` y `README.md`, y el criterio de revisión sobreviviendo solo en `.claude/agents/review-agent.md`, es decir solo para Claude. Se arregló con el flujo completo de OpenSpec (`propose` → `apply`), `skip_specs: true` por ser tooling y documentación.
  **`openspec/config.yaml` es ahora el source of truth de la metodología**: `context` (3.036 chars: stack, dominios, Asfalto Nocturno, `data-cy`, seguridad, idioma, `memory/`, `specs/` congelado), `rules` por artefacto (proposal 5, specs 5, design 4, tasks 5) y `operations` (apply 11 guías, archive 8). **Verificado que la CLI lo inyecta de verdad** vía `openspec instructions <artefacto> --change ... --json` — sin un change existente ese comando falla, por eso no se pudo comprobar en la sesión de instalación.
  **Salvaguarda antes de borrar**: se extrajeron las reglas de los 7 subagentes (y de `agents/review-agent.md`, recuperado con `git show 801fdd2^:agents/review-agent.md`, comprobado que es un subconjunto operativo de la versión `.claude/`) y se mapeó cada una a su destino. Resultado en `openspec/changes/migrar-sdd-a-openspec/notas-migracion-agentes.md`: **10 reglas huérfanas rescatadas** como entradas nuevas de `config.yaml` (entre ellas el veredicto `CHANGES REQUESTED`, que faltaba; el gate de cobertura ≥80%, que no estaba en ningún sitio; no instalar dependencias sin confirmar; Rust sin `unwrap()` injustificado) y **9 descartadas** con motivo explícito.
  **Borrado**: 7 subagentes `.claude/agents/*` + 7 comandos `.claude/commands/sdd-*`. `.claude/commands/opsx/`, `.claude/skills/`, `.cline/skills/` y `.clinerules/workflows/` intactos (6 ficheros cada uno). **`CLAUDE.md` 98→39 líneas** y **`.clinerules/00-project-rules.md` 90→43**, con el mismo contenido salvo la sección de Tokens (propia de DeepSeek) y los nombres de workflow — deben mantenerse iguales, si divergen vuelve el problema. **Docs reescritos**: `01-arquitectura-sdd.md` (capas con `openspec/`), `02-workflow-sdd.md` (propose→apply→archive), `03-agentes-skills.md` **sustituido** por `03-configuracion-openspec.md` (incluye la tabla de dónde aterrizó cada agente), `05-memory-system.md` (árboles), `index.md`, `04-token-management.md`, sidebar de `.vitepress/config.mjs` y `README.md` (este último no estaba en el plan — se detectó al borrar el doc 03 y se arregló).
  **`specs/features/` CONGELADO**, no borrado ni migrado: 52 ficheros de 10 features cerradas, se consultan pero no se amplían ni se editan (ni siquiera sus 2 referencias históricas a `agents/review-agent.md`). Aviso en `specs/README.md` nuevo. `specs/ui/` sigue vigente y editable.
  **Sin regresiones**: 719/719 tests Vitest (74 ficheros), cobertura 96.15%, ESLint 0, `tsc` limpio, `cargo fmt`/`clippy -D warnings`/`test` 5/5 OK, `pnpm run docs` construye y cobertura de documentación 72% sobre umbral 70%. Verificado con `git status` que **no se tocó ni un fichero de `src/`, `src-tauri/` ni `cypress/`**.
  **Decisiones tomadas** (detalle en `design.md` del cambio y en la actualización de [[ADR-027]]): (1) se elimina también `review-agent` — trade-off asumido: se pierde la revisión en contexto fresco, con vuelta atrás definida; (2) **no se crean issues de GitHub**, `tasks.md` es la única trazabilidad — las issues abiertas #66-#80 y #53 quedan sin flujo que las cierre, limpieza pendiente aparte; (3) no se forkea el schema de OpenSpec. **Pendiente**: gate de revisión (`/opsx:archive`) y decisión del usuario sobre commit/push. Rama `feat/open-spec`.
- **Sesión 2026-08-01 (4) — ADR-027 ratificada, OpenSpec instalado (worktree `architecture-decisions`)**: el usuario confirmó explícitamente adoptar OpenSpec como framework SDD (ver [[ADR-027]] en `memory/decisions.md`, ahora `Aceptada`). Alcance de esta sesión: **solo tooling, sin migrar ninguna spec** — la propia guía oficial de adopción brownfield de OpenSpec desaconseja migrar specs cerradas como ejercicio de prueba ("write specs only for what you're about to change"), así que se descarta el "spike" original (migrar una feature ya cerrada) y se deja el primer `/opsx:propose` real para la próxima feature de verdad. CLI instalada global (`npm i -g @fission-ai/openspec@latest`, v1.7.0 — ojo, el paquete `openspec` a secas de npm es un placeholder de 2019 sin relación, el real es `@fission-ai/openspec`). `openspec init --tools claude,cline` ejecutado: creó `openspec/{specs,changes,config.yaml}`, `.claude/skills/openspec-*` + `.claude/commands/opsx/*.md` (namespace separado de los `sdd-*` existentes, sin colisión), `.cline/skills/openspec-*`. **Conflicto real encontrado y resuelto con el usuario**: OpenSpec necesita `.clinerules/` como carpeta (para `workflows/opsx-*.md`), pero el proyecto ya tenía `.clinerules` como archivo único (reglas SDD para Cline+DeepSeek, fichero protegido por regla de gobernanza de `CLAUDE.md`) — el init para Cline falló al chocar con eso. Confirmado con el usuario vía pregunta directa: se movió el contenido a `.clinerules/00-project-rules.md` y se re-ejecutó el init solo para Cline, que ahora sí generó `.clinerules/workflows/opsx-*.md` junto al fichero de reglas preexistente intacto. `openspec/config.yaml` → campo `context:` relleno con un resumen del stack/convenciones (no se copió `CLAUDE.md` entero) para que futuros `/opsx:propose` respeten las reglas del proyecto sin repetirlas. Verificado: `openspec doctor` sin errores, `openspec list`/`validate --all` limpios (specs/changes vacíos, esperado — brownfield delta-first). **No se ha tocado** `specs/features/*`, `CLAUDE.md`, el `.clinerules` raíz (solo reubicado, contenido intacto), ni `.claude/agents/`/comandos `sdd-*` — el SDD propio sigue operativo en paralelo, tal como documenta la ADR. Nada de esto está commiteado todavía (worktree `architecture-decisions`, rama `worktree-architecture-decisions` desde `origin/main`) — pendiente de que el usuario decida cuándo commitear/mergear. **Próximo paso real**: el primer `/opsx:propose` se hace sobre la siguiente feature de verdad que se aborde, no como ejercicio aislado.
- **Sesión 2026-08-01 (3) — feature `cobertura-e2e` CERRADO, `review-agent`: APPROVED (45/45 AC)**: la cobertura E2E del proyecto era prácticamente nula (1 spec Cypress roto, sin scripts npm para ejecutarlo). Flujo SDD completo: spec (`specs/features/cobertura-e2e.md`, 45 AC) → plan (16 pasos) → implementación paso a paso con `impl-agent` (TDD estricto en cada paso, con ejecución real de tests, no solo revisión de código) → `review-agent` con verificación independiente. **Pasos 1-8**: reparado `cockpit.cy.ts` (quitado test de "Modo Invisible", retirado 2026-07-27; `data-cy` en vez de selector CSS), scripts npm `cy:open`/`cy:run`/`test:e2e` + `start-server-and-test`, nuevos `data-cy` (`route-card`, `route-list-empty`, `photo-capture-input-file`), y el mecanismo de siembra `cypress-seed-routes` completo: `app-seed.types.ts`/`app-seed.transform.ts` (parseo puro con guardas explícitas)/`app-seed.service.ts` (`applyCypressSeed()`, doble guarda `isTauri()` por defensa en profundidad) + `MemoryRouteRepository.seed()` (carga directa sin pasar por `save()`) + `app.element.ts` reestructurado (decide primero por `isTauri()` en vez de por éxito/fracaso de SQLite) + infraestructura Cypress (`cy.visitWithSeed()` en `cypress/support/commands.ts`). **Pasos 9-11 — comportamiento NUEVO, no solo cobertura**: implementado el límite de 100 fotos por ruta que `fotos-ruta.md` documentaba como Constraint desde su creación pero nunca estuvo cableado (`countByRouteId()` existía sin consumidor de UI); nueva constante `MAX_PHOTOS_PER_ROUTE=100` (`photo-capture.types.ts`), función única `applyPhotoCaptureLimit()` (`photo-capture.limit.ts`) que decide `disabled`/`limitReached` a la vez, nueva propiedad `limitReached` en `PhotoCaptureElement` (mismo patrón que `disabled`/`loading`, texto accesible "Límite de fotos alcanzado" diferenciado del `loading` transitorio), wireada en `route-detail.element.ts` (`buildAddPhotoButton()`) y `cockpit.element.ts` (`refreshGallery()`/`handleDeletePhoto()`). **Pasos 12-16**: 5 specs Cypress (`cockpit.cy.ts` ampliado, `route-list.cy.ts`, `route-detail.cy.ts`, `fotos.cy.ts`, `timeline.cy.ts` nuevos) — **30/30 tests Cypress en verde** en ejecución real headless. Durante la implementación se encontraron y corrigieron **2 gaps reales de infraestructura/código**, no solo de test: (1) `cypress.config.ts` no tenía `includeShadowDom: true` — sin eso, **ningún** test Cypress del proyecto (ni siquiera los 3 ya "reparados" del Paso 1) podía encontrar ningún `data-cy`, porque toda la app usa Shadow DOM y Cypress no lo atraviesa por defecto; los tests fallaban con "elemento no encontrado" pese a que el screenshot mostraba el DOM renderizado correctamente — bug latente desde que existe Cypress en el proyecto, nunca detectado porque nunca se había ejecutado en verde; (2) `<route-detail>` no exponía `data-cy="route-detail-title"` en su `<h1>`, riesgo real de falso positivo al localizar el título por texto con `<route-list>` montado oculto en paralelo con el mismo nombre de ruta — añadido. **567/567 tests Vitest en verde** (549 previos + 18 nuevos de los Pasos 9-11 + regresión de `route-detail-title`), cobertura global 96.68%/90.99% (líneas/branches), `tsc`/ESLint limpios. `review-agent` verificó de forma independiente (no solo aceptando el resumen recibido): re-ejecutó `tsc`/ESLint/Vitest/Cypress él mismo, encontró un gap adicional no reportado (Paso 8 prometía documentar la siembra en `docs/07-cypress-e2e.md` y ese archivo no se había tocado — solo JSDoc en `commands.ts`) y otro de gobernanza (`CLAUDE.md` modificado en el working tree sin estar en ningún paso del plan). Veredicto inicial: **APPROVED WITH MINOR ISSUES**. Ambos issues resueltos en la misma sesión: se añadió la sección "Siembra de datos en Moto Routes" a `docs/07-cypress-e2e.md`; el cambio a `CLAUDE.md` (consolidación de la regla `data-cy` obligatoria, quitando una duplicación real entre "Convenciones de Frontend" y "Tests E2E") se confirmó explícitamente con el usuario vía pregunta directa — se mantiene. Veredicto final tras la corrección: **APPROVED (45/45 AC)**. Documentación: `specs/features/cobertura-e2e.{md,plan.md,review.md}`. **Nada de esto está commiteado a git todavía** — pendiente de que el usuario decida cuándo commitear/pushear.
- **Sesión 2026-08-01 (2) — feature `reorganizar-dominios` CERRADO (issue #52)**: reorganización estructural del frontend aplicando el patrón de `shared/`. `src/routes/` se dividió en `list/` (listado) y `detail/` (detalle + timeline como pestaña). `src/cockpit/` mantiene el núcleo en raíz (`cockpit.element/service/transform/render/types`) y sus servicios se agruparon por sub-responsabilidad funcional: `gps/` (native-gps + foreground), `persist/`, `photo/`, `stop/`, `long-press/`, `save-route-dialog/` — cada `x.ts` junto a su `x.spec.ts`. Refactor puro con `git mv` (git detectó renames 96-100%), sin cambios de lógica/CSS/dependencias ni de `data-cy`. **527/527 tests pasan**, cobertura global 96.15%, tsc/ESLint/Prettier limpias, `cargo test` 5/5. Build APK Android compilado e instalado y **verificado por el usuario en dispositivo real** (grabación, listado, detalle y diálogo de guardado OK). `review-agent`: **APPROVED** (9/9 AC). Documentación: `specs/features/reorganizar-dominios.{md,plan.md,review.md}`. Issues #61-#63 creados como pasos y cerrados; PR #64 mergeado a `master` (commit `5923054`). Commit del refactor cockpit: `7d571bb` (+ `3a52850` review). Rama local eliminada. **Pendiente**: la issue #53 (`specs/features` planos) sigue abierta — la propia spec observa que esa reorganización debe esperar a la decisión de adoptar OpenSpec (PR #50, ADR en curso).
- **Sesión 2026-08-01 — feature `mejoras-visuales-mapa` CERRADO** (`review-agent`: **APPROVED WITH MINOR ISSUES**, informe en `specs/features/mejoras-visuales-mapa.review.md`; commits `d947afe` — Pasos 8-10 — y el commit del review). 39/39 AC cumplidos (35 con test automatizado, 4 de verificación manual en dispositivo real, 4 retirados por decisión de producto). Issues GitHub #54-#60 cerradas con comentario explicando el estado real de cada una (incluida #58, controles de zoom, cerrada como "implementado y luego retirado"). Dos issues de severidad baja no bloqueantes en el review (ROAD_WIDTH_SCALE sin medición formal de contraste, `fullscreenchange` sin filtrar por instancia — sin efecto con el único consumidor actual) quedan anotadas para referencia futura, sin acción pendiente. Retomada tras compactación de contexto — Pasos 1-6 seguían commiteados (`89f4d70`/`f95be5a`) pero el trabajo de una sesión previa había dejado en el working tree (sin commit) el **Paso 8** ya completo: retirada de controles de zoom (decisión de producto, "no me han gustado"), fix del bug real de toggle de pantalla completa (`isElementFullscreen()` comparaba contra `document.fullscreenElement`, que la Fullscreen API retargeta al host del Shadow DOM — corregido leyendo `ShadowRoot.fullscreenElement`), y afinado de contraste (ancho de vía escalado `ROAD_WIDTH_SCALE=0.7` + `text-color` en capas de etiquetas `symbol`, antes ilegibles). Verificado con el usuario que el APK con estos cambios ya estaba instalado y probado — dio **cuatro puntos de feedback nuevos**, resueltos en el mismo turno como **Paso 9**: 1) la atribución OpenFreeMap/OSM (obligatoria por licencia, no se quita — confirmado explícitamente con el usuario vía pregunta directa) se hace más discreta con opacidad 0.35 por defecto y 1 en hover/foco (`.maplibregl-ctrl-attrib-button`); 2) los pines de inicio/fin ahora anclan por su punta inferior (`anchor: 'bottom'` en `maplibregl.Marker`, antes centro por defecto); 3) `ROAD_WIDTH_SCALE` baja de 0.7 a 0.5 (seguía viéndose grueso); 4) los marcadores de foto/cluster ganan una zona de clic invisible de `--hitbox-min` (56px) envolviendo el icono visible (`route-map-photos.ts`, nueva clase `.route-map-marker-hitarea`) sin cambiar su tamaño aparente — mismo enfoque que propuso el propio usuario. Todo hecho con TDD estricto (test RED confirmado antes de cada cambio). Spec (`mejoras-visuales-mapa.md`) y plan actualizados con AC-033 a AC-037 y el nuevo Paso 9, siguiendo el mismo patrón ya usado en el Paso 8 (corrección directa sobre la spec abierta, sin `review-agent` todavía). **526/526 tests TS pasan, `tsc`/ESLint/Prettier limpios.** APK recompilado (`pnpm tauri android build --target aarch64 --debug`) e instalado (`adb install -r`, dispositivo `75fe536b`). El usuario probó el APK y confirmó que los 4 puntos del Paso 9 quedaron bien, salvo un punto nuevo: la atribución (ya con opacidad reducida del Paso 9) seguía apareciendo **expandida** por defecto (mostrando el texto de crédito completo, no solo el icono "i"). Investigado leyendo el código fuente de `maplibre-gl-dev.js`: no es un bug propio — el `AttributionControl` de MapLibre en modo `compact` arranca expandido por diseño (`_updateCompact()` añade la clase `maplibregl-compact-show` + atributo `open` nada más crearse) y solo se colapsa cuando el mapa recibe un evento `drag` real del usuario, cosa que casi nadie hace en un mapa embebido de 200px. Resuelto como **Paso 10** (AC-038/AC-039): nuevo método `collapseAttribution(mapRoot)` en `route-map.element.ts`, invocado en el handler de `load` junto a `applyContrastOverrides`, que quita esa clase y el atributo `open` — replicando exactamente lo que MapLibre hace internamente al arrastrar, sin tocar su lógica de toggle al pulsar (eso sigue intacto). Test nuevo en `route-map.element.spec.ts` que inserta un `<details>` con las clases reales de MapLibre en `.maplibre-root` antes de `triggerLoad()` y confirma que se colapsa. **527/527 tests TS pasan**, tsc/ESLint/Prettier limpios. APK recompilado e instalado de nuevo. **Pendiente**: confirmación visual del usuario sobre este último ajuste antes de commitear (Pasos 8, 9 y 10 siguen sin commit) e invocar `review-agent` para cerrar la spec formalmente.
- **Sesión 2026-07-31 (2) — feature `mejoras-visuales-mapa` EN CURSO (Pasos 1-6/7 del plan implementados y commiteados, Paso 7 a medias)**: spec creada a partir de queja del usuario ("el mapa se ve muy apagado, hay que hacer mucho zoom para distinguir las calles") + petición de botón de pantalla completa integrado. Flujo SDD completo: `specs/features/mejoras-visuales-mapa.md` (29 AC) → `.plan.md` (7 pasos) → issues GitHub #54-#60 (uno por paso) → implementación paso a paso con `impl-agent`. **Pasos 1-6 implementados, testeados (515/515 tests, 0 regresiones, lint/tsc/prettier limpios) y commiteados** (`f95be5a` pasos 1-4: skeleton de carga, atribución OpenFreeMap/OSM reactivada `{compact:true}`, contraste de vías vía `route-map-contrast.ts` con overrides de `paint` sobre `highway_motorway_inner`/`highway_major_inner`/`highway_minor` del estilo `dark` real de OpenFreeMap — confirmado con `curl` que el fondo era casi negro y las vías casi indistinguibles — usando token `--ink-soft`; marcadores inicio/fin como pin SVG con `currentColor`; commit siguiente para pasos 5-6: `NavigationControl` en `top-left` con hitbox 56px, botón de pantalla completa real (Fullscreen API) en `route-map-fullscreen.ts`, aplicado sobre el contenedor exterior — no sobre `mapRoot` — para que el botón siga visible en fullscreen, `map.resize()` tras `fullscreenchange` sin necesidad de recapturar centro/zoom). **Paso 7 (regresión + verificación visual) pendiente de cerrar**: no se pudo verificar visualmente por navegador (extensión Claude-in-Chrome no conectada en este entorno); en su lugar se compiló y instaló el APK Android (`pnpm tauri android build --target aarch64 --debug` + `adb install -r`, dispositivo `75fe536b`) para que el usuario lo pruebe con sus rutas guardadas reales — **verificación pendiente de respuesta del usuario** (contraste real, solape de controles, y sobre todo si el botón de pantalla completa funciona en el WebView de Android, AC-020, la única incertidumbre real de todo el plan). **Próximos pasos al retomar**: 1) recoger feedback visual del usuario sobre el APK instalado, 2) si hay ajustes, aplicarlos y volver a compilar; 3) marcar Paso 7 `[x]` en el plan cuando quede confirmado; 4) invocar `review-agent` para el cierre formal de la spec (no se considera terminada sin `APPROVED`). El usuario avisó que trae más cambios que proponer en la próxima sesión (aún sin detallar).
- **Sesión 2026-07-28 (4) — fix: "Galería" no hacía nada al añadir foto**: bug reportado por el usuario (trabajando en paralelo con Cline) — el botón "Galería" de `<photo-capture>` no seleccionaba ninguna foto ni daba error visible. Causa raíz encontrada en el working tree: Cline había dejado a medias (sin commitear) un puente nativo Android nuevo (`src-tauri/src/photo_plugin.rs` + `PhotoPlugin.kt`, sin trackear; cambios en `lib.rs`/`Cargo.toml`/`AndroidManifest.xml`) que hacía que `pickFromGallery()` (`photo-capture-adapter.service.ts`) bifurcara por `isTauri()` hacia `@tauri-apps/plugin-dialog` → `invoke('copy_photo')` → `readFile`. Ese plugin nunca se instaló (no está en `package.json`), nunca se registró en Rust ni tiene permiso en `capabilities/default.json` — y además `vite.config.ts` (de una sesión mucho más antigua) ya redirigía ese import a un stub local que siempre devuelve `null`, así que la selección fallaba en silencio en vez de dar el error de permisos genérico de Tauri que cabría esperar. La spec `fotos-ruta.md` (AC-022) ya documentaba la decisión correcta desde antes: cámara y galería deben usar `<input type="file">` sin distinción de plataforma, porque el WebView de Android abre igual los selectores nativos — el código de Cline divergía de esa decisión ya tomada. **Fix aplicado**: revertidos los cambios de Cline (`git checkout` sobre `lib.rs`/`Cargo.toml`/`Cargo.lock`/`AndroidManifest.xml`, borrados los ficheros sin trackear), `pickFromGallery()` simplificado para usar `captureFromInput()` sin bifurcar por `isTauri()` (igual que `captureFromCamera()`, que nunca tuvo este problema), eliminado el alias muerto de `@tauri-apps/plugin-dialog` en `vite.config.ts` y su stub (`src/shared/tauri-plugins/plugin-dialog.ts`). Añadido test de regresión explícito (`pickFromGallery (Tauri Android)`) que confirma que con `__TAURI_INTERNALS__` presente se sigue usando `<input type="file" multiple>`. **484/484 tests TS pasan** (1 nuevo), `tsc`/ESLint sin errores, `cargo build`/`clippy -D warnings`/`fmt --check` sin issues. No se ha tocado `specs/features/fotos-ruta.md` porque el código ahora vuelve a cumplir AC-022 tal cual estaba documentado — no hubo cambio de decisión, solo corrección de una regresión no commiteada. **Verificado en dispositivo Android real** (build `aarch64 --debug` + `adb install -r`): "Galería" vuelve a funcionar, confirmado por el usuario. Al hacer `git push` apareció un commit de Cline en remoto (`3ac137e`, pusheado mientras se investigaba) que añadía `READ_MEDIA_IMAGES`/`READ_EXTERNAL_STORAGE` al manifest y `@tauri-apps/plugin-dialog` a `package.json` como intento paralelo — con nota de que "la galería sigue sin funcionar" (diagnóstico previo a este fix). Rebaseado sin conflictos y, con el fix ya confirmado funcionando sin esos permisos/dependencia, se limpiaron en un commit aparte (`chore(fotos)`, principio de permisos mínimos). **Commiteado y pusheado a `origin/master`**: `dc92db6` (fix) + `55f8c14` (limpieza). Feature cerrado.
- **Sesión 2026-07-28 (3) — cierre de `deuda-tecnica-auditoria`**: retomada la sesión anterior. Verificado visualmente en el navegador (`pnpm run dev`, modo web) el resplandor `--amber-glow` (AC-007) — se ve correctamente en el botón "Grabar" del cockpit. AC-003 (formato de fecha) no se pudo verificar contra datos reales en modo web (la BBDD vive en el dispositivo Android, sin rutas guardadas en este entorno) — dado por bueno por criterio del usuario (cambio mecánico de `toLocaleDateString` ya cubierto por tests). Se invocó `review-agent`: veredicto **APPROVED WITH MINOR ISSUES** (ISSUE-001, severidad baja: AC-005 — spinner de `photo-capture` respetando `prefers-reduced-motion` — sin verificación manual documentada; mecanismo en sí correcto). Resuelto en la misma sesión añadiendo 2 tests nuevos a `photo-capture.element.css.spec.ts` que confirman deterministamente que el override global `@media (prefers-reduced-motion: reduce)` de `tokens.css` (con `animation-duration: 0.01ms !important`) llega al CSS resuelto del componente, en vez de una verificación manual en DevTools (frágil de automatizar). **483/483 tests TS pasan** (2 nuevos). Veredicto final: **APPROVED**. Feature `deuda-tecnica-auditoria` cerrado — los 10 AC cumplidos y verificados.
- **Sesión 2026-07-28 (2) — feature `deuda-tecnica-auditoria` (10/10 AC, 9/9 pasos del plan)**: saneamiento técnico puro por SDD/TDD, sin funcionalidad nueva (salvo dos cambios visuales acotados y aprobados de antemano en la spec). **Pasos 1-3 (imports cruzados/ubicación de código compartido, "blast radius" alto — 15+ archivos tocados)**: `formatDuration`/`calculateAvgSpeed` → `shared/utils/format.ts`, `calculateDistance` → `shared/utils/geo.ts` (ambos antes en `cockpit.transform.ts`, importados directamente por `routes/`); `photo-capture.*` movido de `src/photos/` (carpeta eliminada) a `shared/photo-capture/` (mismo patrón que `photo-gallery`/`photo-viewer`); `formatRouteDate`/`buildRouteDisplayName` (nuevo) y `buildDefaultRouteName` (movida) → `shared/utils/date.ts`/`shared/utils/route-naming.ts`, deduplicando el formateo de fecha divergente entre `route-list` (mes corto) y `route-detail` (mes largo, y sin año en el título) — ahora ambos usan `month: 'short'` con año siempre. `detectStop` es la única excepción documentada de import cruzado `routes → cockpit` (depende de `StopDetectionState`), con comentario explícito en ambos extremos del import. **Pasos 4-6 (CSS/tokens)**: `shared/photo-capture/photo-capture.element.css` importa `tokens.css` sin fallbacks hardcodeados; `counter.element.css`/`confirm-dialog.element.css` importan `tokens.css` (antes no lo hacían pese a usar `var(--token)`); nuevo token `--amber-glow: oklch(60% 0.17 45 / 0.45)` sustituye los 6 literales `oklch(...)` hardcodeados en `cockpit.element.css`/`nav-bar.element.css` (ADR-026, referencia ADR-019 — no contradice la prohibición de "neón/glow" al quedar acotado a un token semántico único). **Ajuste no previsto por el plan**: los tests de `*.element.css.spec.ts` requirieron añadir `test: { css: true }` a `vitest.config.ts` (Vitest devuelve `''` para imports CSS por defecto) y comprobar contenido *resuelto* de `tokens.css` (p. ej. `--amber:`) en vez del texto literal `@import`, que Vite reemplaza en línea al procesar `?inline`. **Pasos 7-8 (Rust)**: `src-tauri/src/commands/mod.rs` gana su primer módulo `#[cfg(test)]` (5 tests: `save_file` rechaza absolutas/`..`/contenido vacío, acepta relativas válidas; `greet` rechaza nombre vacío) — `cargo test` pasa de "0 passed" a 5/5; `recording_service.rs` documenta como limitación conocida (comentario en código) que no tiene lógica pura testeable fuera de `#[cfg(target_os = "android")]`. **Paso 9 (cobertura wrappers Tauri)**: `sqlite-photo.factory.ts` 0%→100%, `sqlite-route.factory.ts` 73%→100% (nuevos specs mockeando `Database.load` de `@tauri-apps/plugin-sql`), `photo-storage.service.ts` 74%→98.8% (ramas de éxito/catch de `createPhotoRepository` en Tauri, `buildPhotoMetadata`, mime type `.png`). **481/481 tests TS pasan** (24 tests nuevos netos), cobertura global 95.9%/90.31%/94.48%/95.9% (stmts/branch/funcs/lines). `cargo test`/`cargo clippy -- -D warnings`/`cargo fmt --check` en verde. `tsc --noEmit` y `vite build` sin errores (hubo que ampliar `buildRouteDisplayName` para aceptar `string | null | undefined` en el nombre, ya que `Route.name` es `string | null`). Verificado: no queda ningún import cruzado `routes ↔ cockpit` fuera de la excepción documentada de `detectStop`, ni ninguna referencia residual a `src/photos`. **Pendiente**: verificación visual manual del usuario (antes/después) del resplandor `--amber-glow` (AC-007) y del nuevo formato de fecha en `route-detail` (AC-003) — ambos cambios visuales menores ya aprobados de antemano en la spec, pero sin captura confirmada todavía. AC-001 a AC-010 marcados `[x]` en la spec (a diferencia de `grabacion-rutas` Fase 2, aquí sí son verificables por completo con tests automatizados + compilación). Recomendado invocar `review-agent`.
- **Sesión 2026-07-28 — feature `grabacion-rutas` Fase 2 (AC-011 corregido, AC-020 a AC-023 IMPLEMENTADOS, pendiente de verificación manual — Pasos 12-18/19 del plan)**: se descubrió con datos reales de BBDD que el fix de PR #47 (foreground service) solo mostraba la notificación persistente, pero la captura de puntos GPS seguía dependiendo de `navigator.geolocation.watchPosition()` en el WebView (una ruta real de 64s guardó 1 solo punto). Esta sesión mueve la captura de ubicación a código nativo Android: `RecordingService.kt` ahora usa `FusedLocationProviderClient` (intervalo 1s) con fallback a `LocationManager` si Google Play Services no está disponible (AC-023), maneja `ACTION_PAUSE`/`ACTION_RESUME` sin detener el servicio/notificación, y reenvía cada punto vía `tauri::ipc::Channel` (`RecordingServicePlugin.kt` → `recording_service.rs` → evento Tauri `recording-service://location` → `cockpit-native-gps.service.ts`, nuevo `GpsProvider` que sustituye a `watchPosition()` como única fuente de puntos en Android/Tauri, con `selectGpsProvider()`/`isAndroidTauri()` para no romper web/desktop). De paso se corrigió un bug de duplicación de puntos en pausa/reanudación (`pauseRecordingAction` no llamaba a `loop.stopWatch()`, dejando un watch huérfano). Archivos nuevos: `cockpit-native-gps.service.ts`+spec, `cockpit-foreground.service.spec.ts`, `shared/tauri/commands.spec.ts`. Modificados: `cockpit.service.ts`, `cockpit-foreground.service.ts`, `cockpit.element.ts`, `shared/tauri/commands.ts` (+specs), y en Rust `recording_service.rs`/`commands/mod.rs`/`lib.rs` (nuevo `Channel`, comandos `pause_recording_location`/`resume_recording_location`), y en Kotlin `RecordingService.kt`/`RecordingServicePlugin.kt`/`MainActivity.kt`/`build.gradle.kts` (+ dependencia `play-services-location:21.3.0`). 49 tests TS nuevos, **457/457 tests pasan**, cobertura 94.81%/89.53%/93.7% (líneas/ramas/funciones). `cargo build`/`clippy -D warnings`/`fmt --check`/`test` sin errores en `src-tauri/`. `pnpm tauri android build --target aarch64 --debug` compiló sin errores a la primera e instalado con `adb install -r` en dispositivo real. **IMPORTANTE — esto NO está verificado como funcionando todavía**: falta el Paso 19 del plan (prueba manual de un trayecto real de varios minutos con la pantalla bloqueada, comprobando `route_points` en la BBDD del dispositivo) antes de dar el bug por resuelto — exactamente la misma prueba corta que ya se dio erróneamente por buena una vez con este mismo bug (sesión 2026-07-27) no debe repetirse. No marcar AC-011/AC-020 a AC-024 como `[x]` en la spec hasta completar esa verificación.
- **Sesión 2026-07-27 (3) — feature `timeline-ruta` (21/21 AC, 8/8 pasos del plan)**: implementada por SDD con TDD. Nueva pestaña "Timeline" en `<route-detail>` como 4ª pestaña del `<tab-bar>`, que muestra en orden cronológico Salida, paradas detectadas al vuelo (reutilizando `detectStop()` de `cockpit.transform.ts` con el mismo criterio conservador de 30 puntos ≤3 km/h), fotos intercaladas y Llegada, con velocidad media de cada tramo. Cinco archivos creados: `route-timeline.types.ts` (tipos puros), `route-timeline.transform.ts` (lógica pura: `detectStopsFromPoints`, `formatTimelineTime`/`Coords`/`Speed`, `buildTimelineSegments`, `buildTimelineData`), `route-detail-timeline.ts` (constructor DOM), y sus specs. Tres archivos modificados: `route-detail.element.ts` (4ª pestaña, guarda `_routePoints` completos con timestamp/speed, `refreshAllPanels` al cambiar fotos), `route-detail.element.css` (~100 líneas de estilos con tokens Asfalto Nocturno). 21/21 AC cubiertos con 27 tests nuevos. **419/419 tests pasan**, cobertura >80%. Sin cambios en Rust, SQLite ni dependencias. Build Android compilado (`pnpm tauri android build`), instalado en dispositivo real y **verificado visualmente por el usuario** — pestaña Timeline funcional con datos de rutas ya guardadas. Mergeado a `master` (PR #49).
- **Sesión 2026-07-27 (2) — feature `mejoras-guardado-rutas` (18/18 AC, 11/11 pasos del plan)**: nombre de ruta al guardar, notas editables, fix nav-bar. Mergeado a `master` (PR #48).
- **Sesión 2026-07-27 — fix: GPS no grababa en segundo plano**: foreground service real vía JNI. Mergeado a `master` (PR #47).
- **Fase**: APK Android compilado y funcional, con persistencia de rutas y fotos verificada en dispositivo real. Monorepo con backend propio (`apps/api`, Go) en producción real vía Tailscale — primera integración real `apps/mobile`→`apps/api` en curso de cierre (`catalogo-tipos-parada`).
- **Feature activo**: `catalogo-tipos-parada` (ver sesión 2026-08-04 (2) arriba) — cerrada y archivada, **PR #93 abierta** (`feature/catalogo-tipos-parada` → `master`, CI en marcha, sin mergear todavía). `migrar-api-golang` y `despliegue-servidor-tailscale` (ambas ver sesión 2026-08-04 arriba) cerradas y mergeadas a `master` — `apps/api` corre en Go en producción real vía Tailscale. `entorno-api-docker` (2026-08-03) mergeado (PR #88). Features anteriores a esa fecha: ver sus propias entradas de sesión más abajo para el estado detallado de cada una en su momento — no reauditadas en esta actualización.
- **Sesión 2026-07-31 — fix: cronómetro se quedaba atrás en segundo plano**: `elapsedTime` se calculaba incrementando un contador en cada tick de `setInterval(1000ms)` — mismo mecanismo que Chromium pausa/retrasa en segundo plano (por lo que el GPS ya usa eventos nativos). Distancia/velocidad venían de puntos GPS reales con timestamp correcto, pero la duración se quedaba muy corta → velocidad media disparada (~500 km/h reportado en una ruta real). Fix: `elapsedTime` ahora se deriva del reloj de pared (`Date.now()` menos tiempo en pausa) tanto en el tick como al llegar cada punto GPS (`computeElapsedSeconds()` en `cockpit.service.ts`), autocorrigiéndose si el intervalo se retrasa. 484/484 tests TS pasan, `tsc`/ESLint sin errores. Commiteado y pusheado a `origin/master` (`48c25ec`). **Pendiente igual que con el fix de GPS**: no dar por resuelto sin una prueba real de trayecto largo con pantalla bloqueada — build Android en curso para esa prueba.
- **Limpieza de GitHub (2026-07-31)**: cerrados 15 issues obsoletos (#13-17 Fases persistencia-rutas, #33-42 Pasos fotos-ruta) — trabajo ya mergeado en `master`, issues nunca cerrados tras el merge. Mergeado PR #50 (ADR: adoptar OpenSpec en vez de SDD custom, propuesto por `viictoryraves`) — decisión de arquitectura pendiente de discutir en detalle.
- **AC-017/AC-018/AC-019 de `grabacion-rutas` cerrados como NO implementados (2026-07-31)**: `grabacion-rutas.review.md` los marcaba erróneamente como "✅ Cubierto" citando solo el test del algoritmo puro (`detectStop`); en realidad nunca se persiste ninguna parada (`buildStops()` en `cockpit-persist.service.ts` devuelve siempre `[]`, aunque la tabla `route_stops` y el repositorio ya soportan guardarlas). Corregido el review y añadida nota en `grabacion-rutas.md`. Decisión: no se implementa aquí — la persistencia real de paradas (con diccionario de datos, migraciones, etc.) se abordará en una **spec nueva y dedicada** sobre paradas.
- **Sesión 2026-08-02 — feature `documentacion-codigo` CERRADO, `review-agent`: APPROVED (19/19 AC)**: el proyecto no tenía documentación del código ni normas que obligaran a los agentes (Cline/Claude) a documentar. Se implementó la Opción 1 completa: **TypeDoc** (API reference en `docs/api/` con `typedoc-plugin-coverage`, `entryPointStrategy: expand` sobre `src/`), **VitePress** (sitio navegable en `docs/` con home, Guías 01-07, ADRs, Design System y enlace a la API), **`cargo doc --no-deps`** (doc Rust en `src-tauri/target/doc/`), y **`eslint-plugin-jsdoc`** como regla obligatoria (`jsdoc/require-jsdoc` error, `publicOnly: true`, `*.spec.ts` exentos) — cualquier export público sin JSDoc bloquea el pre-commit. Los agentes quedan informados en `.clinerules` y `CLAUDE.md`. **Gate de cobertura de documentación al 70% (242/342 símbolos)** con script propio `scripts/docs-coverage.mjs` (el plugin v4 no expone `coverageThreshold`). Sitio CI en `.github/workflows/docs.yml`. **Sin regresiones**: 567/567 tests Vitest, lint 0 errores, clippy/fmt/test Rust OK, Cypress 30/30. Detalles: `pnpm run docs` genera todo (⚠️ usar `pnpm run docs` no `pnpm docs` — colisiona con comando nativo de pnpm); override `postcss: 8.5.16` en `pnpm-workspace.yaml` (pnpm v11: los overrides van ahí, no en `package.json`); VitePress rompía el build con `invoke<T>()` de los ADRs — `docs-prepare.mjs` escapa genéricos TS. Commits en `feature/documentacion-codigo`: `e3576c5` (infraestructura + JSDoc exports) + `8da27c6` (umbral 70% + doc de tipos/dominio). Documentación: `specs/features/documentacion-codigo.{md,plan.md,review.md}`.
- **Último push (2026-07-31)**: `48c25ec` (fix cronómetro) pusheado a `origin/master`.

## Desarrollo Web (Vite)
Desde `entorno-api-docker` (2026-08-03), todos los comandos de esta sección y de "Build Android" se ejecutan **desde `apps/mobile/`**, no desde la raíz del repo (monorepo — ver [[ADR-032]]). Las rutas de fichero mencionadas más abajo (`src-tauri/...`, `dist/...`) son relativas a `apps/mobile/`.

Para lanzar el proyecto en modo web (sin Tauri), usar:
```bash
cd apps/mobile
pnpm run dev
```

**Puerto fijo**: `1420` (configurado en `vite.config.ts` con `strictPort: true`).

## Build Android
```powershell
cd apps/mobile
bash scripts/install-android.sh          # detecta la ABI del dispositivo conectado, compila release e instala en un paso
# o, equivalente a mano (release, igual que publica CI desde optimizar-bundle-produccion, 2026-08-12):
pnpm tauri android build --target aarch64
adb install -r src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release.apk
# debug (más rápido para iterar, ~148MB el .so sin stripear frente a los ~6.7MB del release):
pnpm tauri android build --target aarch64 --debug
adb install -r src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk
```
**Desde `optimizar-bundle-produccion` (2026-08-12, ADR-047) el APK que publica CI ya no lleva `--debug`** — es un build `release` real (minify+shrinkResources+profile Rust optimizado), firmado con el mismo keystore de debug efímero de siempre. ~9MB medido real, frente a los ~170MB del debug que se publicaba antes. El gotcha de assets huérfanos de más abajo (§ "puede empaquetar frontend desactualizado") aplica igual al buildType release — `install-android.sh` ya hace el force-sync necesario, no hace falta a mano en local.

**Usar siempre este comando del CLI de Tauri — nunca `cargo build --target aarch64-linux-android` manual.** `scripts/build-apk.ps1` (que hacía justo eso, con copia manual de `dist/` a `app/src/main/assets/` y un `tauri.conf.json` "standalone" propio) se eliminó el 2026-08-02: su premisa era falsa. `tauri::generate_context!()` embebe `devUrl`/`frontendDist` en el binario Rust **en tiempo de compilación**, leyendo siempre `src-tauri/tauri.conf.json` (el real, con `devUrl: "http://localhost:1420"` para poder usar `pnpm tauri dev`). Un `cargo build` en profile `dev` (sin `--release`) hace que Tauri prefiera `devUrl` sobre `frontendDist` en tiempo de ejecución **sin importar qué JSON haya en la carpeta de assets del APK** — ese archivo no se relee en runtime. Síntoma exacto si esto pasa: la app abre con fondo negro y el texto `Failed to request http://localhost:1420/ — error sending request for url (http://localhost:1420/)`. El comando del CLI (`pnpm tauri android build --debug`) sí evita esto correctamente (usa `frontendDist`), es la única vía soportada.

**Gotcha del CLI**: cada `pnpm tauri android build` resetea `src-tauri/gen/android/app/tauri.properties` (`tauri.android.versionCode`) a `1000`. Si el dispositivo ya tiene instalado un `versionCode` mayor (de una build anterior), `adb install -r` falla con `INSTALL_FAILED_VERSION_DOWNGRADE`. No hay forma de evitarlo sin tocar el CLI — si pasa, `adb uninstall com.motoroutes.app` (pierde datos locales de prueba del dispositivo) y reinstalar.

**Gotcha grave (2026-08-02) — el build puede empaquetar frontend desactualizado sin avisar**: en builds sucesivas donde solo cambia el frontend (sin tocar Rust), `pnpm tauri android build` a veces **no vuelve a copiar el `dist/` recién generado dentro del APK** — el paso de `pnpm build` (`beforeBuildCommand`) sí se ejecuta y `dist/` queda correcto, pero el APK final sigue conteniendo el bundle JS de una build anterior (verificado con hashes de archivo distintos entre `dist/assets/index-*.js` y lo que de verdad contenía el APK). No hay ningún error ni warning — el build "termina bien" y el APK se instala sin problema, pero ejecuta código viejo. **Siempre verificar el contenido real del APK antes de dar una build por buena**:
```bash
unzip -p src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk assets/index.html
```
Comparar el hash del `<script src="/assets/index-XXXX.js">` con el de `dist/index.html` recién generado — si no coinciden, el APK está desactualizado. Fix que funciona de forma fiable: copiar `dist/*` a mano a `src-tauri/gen/android/app/src/main/assets/` (`rm -rf .../assets/assets && cp -r dist/assets .../assets/assets && cp dist/index.html .../assets/index.html`) y luego reempaquetar SOLO con Gradle, saltando las tareas Rust (que fallan fuera del wrapper del CLI de Tauri, ver gotcha siguiente):
```powershell
cd src-tauri/gen/android
.\gradlew.bat assembleArm64Debug -x :app:rustBuildArm64Debug -x :app:rustBuildArmDebug -x :app:rustBuildX86Debug -x :app:rustBuildX86_64Debug
```
El APK queda en `app/build/outputs/apk/arm64/debug/app-arm64-debug.apk` (ruta distinta a la del build vía CLI, que usa `apk/universal/debug/`). Reverificar con el mismo comando `unzip` antes de instalar. **No dar nunca una verificación visual del usuario por buena sin haber confirmado primero, con este comando, que el APK instalado contiene el código que se cree que contiene** — en esta sesión se le pidió al usuario que probara una build que en realidad era una versión vieja, y confirmó "funciona" sin haber probado el cambio real.

**Corrección (2026-08-07, sesión `rutas-en-la-nube`) — el "fix fiable" de arriba (copiar `dist/` a mano + reempaquetar solo con Gradle) NO fue fiable esta vez.** El `unzip` del APK confirmaba el JS nuevo (`index-BeQkRJd_.js`), `adb install -r` decía éxito, y hasta forzar `am force-stop` + borrar `app_webview`/`cache`/`code_cache` del propio paquete (`adb shell run-as com.motoroutes.app sh -c 'rm -rf ...'`) + relanzar — la app seguía ejecutando el JS de la build anterior. La verificación por `unzip` del APK **no es suficiente**: confirma qué hay empaquetado, no qué sirve de verdad el WebView de Tauri en runtime. Verificación real vía Chrome DevTools Protocol contra el WebView (mucho más fiable que `unzip` + capturas de pantalla):
```bash
adb shell cat /proc/net/unix | grep webview_devtools   # localiza @webview_devtools_remote_<pid>
adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>
curl -s http://localhost:9222/json                     # lista páginas, cada una con su id/webSocketDebuggerUrl
# con Node 18+ (WebSocket global), Runtime.evaluate sobre ese id:
#   document.querySelector('script').src  → confirma qué bundle sirve de verdad
```
Solo el build completo de la CLI (`pnpm tauri android build --target aarch64 --debug`, con Rust incluido, sin `-x`) hizo que el WebView sirviera el JS nuevo. **Hasta no tener una explicación mejor, tratar el atajo "solo Gradle" como no confiable para cambios de solo-frontend y usar siempre el build completo de la CLI, verificando con CDP (no solo con `unzip`) antes de dar una build por buena.**

**Corrección adicional, misma sesión — ni siquiera el build completo de la CLI es fiable por sí solo, cada vez.** En la misma sesión, repetidas veces (no una excepción puntual): `pnpm tauri android build` terminó "bien" pero el `assets/index.html` empaquetado en el APK seguía referenciando el hash JS de la build ANTERIOR (`dist/index.html` sí tenía el hash nuevo — confirmado con el mismo comando `unzip` de más arriba). Rutina que sí funcionó, de forma consistente, cada vez que se repitió: (1) build completo de la CLI, (2) comparar `grep -o 'index-[A-Za-z0-9_-]*\.js' dist/index.html` contra el mismo grep sobre `unzip -p .../app-universal-debug.apk assets/index.html`; si no coinciden, (3) copiar a mano `dist/assets`→`.../app/src/main/assets/assets` y `dist/index.html`→`.../assets/index.html` (mismo comando que el "fix fiable" de más arriba), y (4) volver a lanzar el build completo de la CLI una segunda vez (no basta con Gradle-only, ver corrección anterior) — esa segunda vuelta sí empaqueta bien. **Tratar esto como el procedimiento normal, no como una excepción: verificar el hash después de cada build y repetir el ciclo copiar+rebuild hasta que coincida, antes de instalar.** Verificación de que el WebView del dispositivo sirve de verdad lo esperado (no solo que el `.apk` lo contenga) vía Chrome DevTools Protocol:
```bash
adb shell cat /proc/net/unix | grep webview_devtools   # localiza @webview_devtools_remote_<pid> (pid cambia en cada relanzamiento)
adb forward tcp:PUERTO localabstract:webview_devtools_remote_<pid>
curl -s http://localhost:PUERTO/json                    # lista páginas — puede devolver vacío/colgarse la primera vez tras instalar, reintentar
# con Node 18+ (WebSocket global) contra el webSocketDebuggerUrl de la página "type":"page":
#   Runtime.evaluate con awaitPromise:true, expresión async con su propio try/catch
#   y un setTimeout de proceso — el canal CDP se queda colgado o se cae sin previo
#   aviso tras uso intensivo; si un eval trivial (`1+1`) no responde en unos segundos,
#   no seguir insistiendo con la misma conexión: `adb forward --remove-all` y
#   reabrir desde cero (nuevo `adb forward` + nueva conexión WebSocket).
```
Con la página cargada, `document.querySelector('script').src` confirma el bundle real servido; `elemento.getBoundingClientRect()` sobre elementos dentro de un Shadow DOM (`customElements` de esta app) permite medir alineación real en píxeles sin depender de capturas de pantalla ni de que el dispositivo esté desbloqueado — más rápido y más preciso que pedirle al usuario una captura para un ajuste visual fino.

**Gotcha adicional (2026-08-02, misma sesión) — el JBR de Android Studio se autoactualizó a mitad de sesión y rompió el build**: `pnpm tauri android build` empezó a fallar con `A problem occurred configuring project ':buildSrc'. > 25.0.2` / `Caused by: java.lang.IllegalArgumentException: 25.0.2` en `JavaVersion.parse` — el parser de versión de Java del plugin Kotlin DSL (bundlado con la versión de Gradle/AGP de este proyecto) no reconoce Java 25. Causa: `C:\Program Files\Android\Android Studio\jbr\bin\java.exe` (usado como `JAVA_HOME` en todos los builds de esta sesión) se autoactualizó de una versión más antigua a `25.0.2` mientras la sesión estaba en curso — build que llevaba horas funcionando con el mismo comando exacto de repente rompió sin que cambiara nada del proyecto. Limpiar `.gradle`/`app/build` **no lo arregla** (no es un problema de caché). Fix: usar un JDK más antiguo como `JAVA_HOME` en su lugar — `C:\Program Files\Java\jdk-24` (24.0.1) funcionó. Si ese tampoco existe, instalar cualquier JDK 17-21 y apuntar `JAVA_HOME` ahí. Si un build que antes funcionaba empieza a fallar con un error de parseo de versión de Java sin explicación, comprobar primero `& "$env:JAVA_HOME\bin\java.exe" -version` — puede haberse actualizado solo.

## Convenciones
- **Estilo de código**: TypeScript strict mode + ESLint strict + Prettier
- **Commits**: Conventional Commits
- **Ramas**: feature/<nombre> desde main
- **Nombrado**: Carpetas en kebab-case, clases/componentes en PascalCase, funciones en camelCase
- **Idioma**: Docs y specs en español, código en inglés