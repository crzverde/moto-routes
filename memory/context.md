# Contexto del Proyecto: Moto Routes

## Identidad
- **Nombre**: Moto Routes (Ride Tracker)
- **Propósito**: Aplicación móvil para motociclistas que combina navegación GPS, grabación de rutas y bitácora multimedia. Desde la sesión `entorno-api-docker` (2026-08-03), monorepo: app móvil (`apps/mobile/`) + API backend (`apps/api/`).
- **Repositorio**: d:\Git\Otros\moto-routes

## Stack Tecnológico
- **Lenguaje Frontend**: TypeScript 5.7 (strict mode) — `apps/mobile/`
- **Lenguaje Móvil/Desktop backend**: Rust (stable, edition 2021) — `apps/mobile/src-tauri/`
- **Lenguaje API backend**: Go 1.25 (chi router, pgx/v5, golang-jwt, bcrypt) — `apps/api/`. Migrado desde Java/Spring Boot en la sesión `migrar-api-golang` (2026-08-04, ver [[ADR-034]]) — el histórico Java/Maven queda solo en `git log`/ADRs, no en el árbol de trabajo.
- **BBDD API backend**: PostgreSQL 16 (Docker, imagen `postgres:16-trixie`)
- **Bundler**: Vite 6
- **Desktop/Mobile Framework**: Tauri 2
- **UI**: Web Components nativos (Custom Elements v1)
- **Testing Frontend**: Vitest 3 (jsdom, coverage v8) → 80% threshold
- **Testing Móvil backend**: cargo test (unit + integration)
- **Testing API backend**: `go test ./...` (unit + integración real contra Postgres, aislada por schema — `internal/dbtest`)
- **Linting TS**: ESLint 9 (strictTypeChecked + stylistic)
- **Linting Rust**: Clippy (deny warnings)
- **Formatting TS**: Prettier 3
- **Formatting Rust**: rustfmt
- **Git Hooks**: Husky 9 (pre-commit, en orden: `pnpm audit --audit-level=high` + `cargo audit` con excepciones documentadas → ESLint → Vitest → Clippy → rustfmt → cargo test → E2E Cypress vía `pnpm test:e2e`. El paso de auditoría real se cableó en la sesión `auditoria-seguridad` — antes solo estaba documentado en `docs/06-seguridad.md`, nunca implementado. Desde `entorno-api-docker`, todos los comandos de `apps/mobile` se ejecutan con `cd apps/mobile && ...`/`cd apps/mobile/src-tauri && ...`, el hook en sí sigue viviendo en `.husky/` de la raíz)
- **Package Manager**: pnpm (workspace: `packages: [apps/mobile]`) + Cargo + Go modules (`go.mod`)
- **Security**: CSP estricto, permisos mínimos, sin eval, path validation (app móvil); sin secretos en código en `apps/api` (credenciales de Postgres vía `infra/docker/.env`, no versionado)
- **BBDD Local (app móvil)**: SQLite vía `@tauri-apps/plugin-sql` (archivo: `moto-routes.db`)
- **Docker**: `infra/docker/docker-compose.yml` (servicios `api`+`postgres`, ambos con imagen base Debian 13/trixie — ver [[ADR-032]]), solo para desarrollo local, sin despliegue real todavía

## Herramientas de Desarrollo
- **GitHub CLI**: `gh` (oficial, ya instalado) → issues, PRs, releases
- **rtk**: Proxy de comandos (https://github.com/rtk-ai/rtk) → reduce tokens 60-90% en comandos repetitivos
- **Cline**: Extensión VSCode con DeepSeek como modelo

## Estructura del Proyecto

Monorepo desde `entorno-api-docker` (2026-08-03, ver [[ADR-032]]): `apps/mobile/` (app móvil, todo lo que antes vivía en la raíz) + `apps/api/` (backend Java nuevo) + `infra/docker/` (orquestación local). `package.json`/`pnpm-lock.yaml` de la raíz son solo para la documentación transversal (`docs:*`) y `husky` — `apps/mobile/package.json` es el paquete pnpm real del workspace (`pnpm-workspace.yaml` → `packages: [apps/mobile]`).

```
package.json                  # Raíz: solo docs:* (typedoc/vitepress) + husky, sin deps de la app
pnpm-workspace.yaml           # packages: [apps/mobile]
typedoc.json                  # entryPoints: apps/mobile/src (se queda en la raíz)
scripts/
├── docs-coverage.mjs         # Lee docs/api/coverage.json — se queda en la raíz
└── docs-prepare.mjs          # Copia memory/decisions.md y specs/ui/ a docs/reference/ — se queda en la raíz
apps/
├── mobile/                    # App móvil (Tauri + frontend) — antes en la raíz del repo
│   ├── src/                   # Frontend (TypeScript + Vite)
│   │   ├── app/
│   │   │   └── app.element.ts        # Componente raíz <app-root>, monta <cockpit-view>
│   │   ├── cockpit/                  # Dominio "cockpit" (grabación de ruta)
│   │   ├── routes/                   # Dominio "routes" (listado, detalle, timeline)
│   │   ├── shared/
│   │   │   ├── styles/tokens.css     # Design tokens globales
│   │   │   ├── repositories/         # Repositorios SQLite
│   │   │   └── tauri/commands.ts     # Wrappers tipados para invoke()
│   │   ├── index.css
│   │   ├── main.ts
│   │   └── vite-env.d.ts
│   ├── src-tauri/                    # Backend móvil (Rust)
│   │   ├── src/{main,lib}.rs, commands/mod.rs
│   │   ├── capabilities/default.json # Permisos explícitos (mínimo privilegio)
│   │   ├── Cargo.toml, tauri.conf.json, build.rs
│   │   └── .cargo/config.toml        # Cross-compiler NDK para Android
│   ├── cypress/                       # E2E
│   ├── tests/setup.ts                 # Test setup global Vitest
│   ├── scripts/                       # kill-port.mjs, setup-android.sh, pull-db.{cmd,ps1}
│   └── package.json, tsconfig.json, vite.config.ts, vitest.config.ts, cypress.config.ts, eslint.config.js
└── api/                        # API backend (Go 1.25 — migrado desde Java/Spring en migrar-api-golang, ver [[ADR-034]])
    ├── cmd/api/main.go            # Entry point: router chi, wiring de handlers/middleware
    ├── internal/
    │   ├── auth/                  # Registro/login/JWT/rate-limit, PostgresUserStore
    │   ├── config/                # Config.Load() — lee env vars, sin defaults para secretos
    │   ├── dbtest/                # Helper compartido: aísla cada paquete de test en su propio schema Postgres
    │   ├── httpmw/                # Middleware transversal (Recover, PublicCORS)
    │   ├── migrate/                # Runner de migraciones propio (sin Flyway/Liquibase), embed.FS
    │   ├── migrate/migrations/     # 0001_create_users.sql, 0002_create_stop_types.sql, ...
    │   ├── ping/                   # GET /api/ping — consulta real a Postgres
    │   ├── secretscan/             # Escáner de secretos propio (pre-commit)
    │   └── stoptypes/              # GET /api/stop-types — catálogo de tipos de parada, público
    ├── Dockerfile                  # Multi-stage: golang:1.25-trixie (build) → debian:trixie-slim (runtime)
    ├── go.mod, go.sum
infra/
└── docker/
    ├── docker-compose.yml    # Servicios api + postgres (postgres:16-trixie), solo desarrollo local
    ├── postgres/init.sql     # Tabla dummy `healthcheck`, vía docker-entrypoint-initdb.d/
    ├── .env.example           # Versionado — claves sin valores reales
    └── .env                   # NO versionado (.gitignore) — credenciales triviales de solo dev
specs/features/               # Especificaciones funcionales por feature (histórico SDD, congelado)
specs/api/                    # Contratos de API (SDD histórico — no confundir con apps/api/)
specs/data/                   # Modelos de datos, schemas
specs/ui/
├── design-system.md          # Filosofía visual + design tokens (actualizado)
└── frontend-conventions.md   # Reglas de frontend
openspec/                     # Metodología SDD activa (proposals/specs/design/tasks)
docs/                         # Documentación transversal (VitePress + TypeDoc + cargo doc)
memory/                       # Sistema de memoria persistente
```

## Filosofía Visual: "Asfalto Nocturno"
- **Concepto**: Cuero oscuro, negro asfalto y ámbar cálido. Cuaderno de bitácora, no HUD de competición. **Prohibido**: neón, glassmorphism, azules fríos, diales circulares.
- **Source of truth**: `specs/ui/design-system.md` y `src/shared/styles/tokens.css`.
- **Tokens activos** (`--bg-top`, `--panel`, `--amber`, `--ink`, `--font-display/ui/data`). **No usar** `--color-*`, `--glow-*`, `--neon-*` (eliminados).
- **Modo oscuro obligatorio**: Por seguridad vial, sin modo claro.
- **Hitbox mínima**: 56×56px para uso con guantes de moto.
- **Paleta**: Asfalto/cuero oscuro (`--bg-top`/`--bg-bottom`), ámbar único acento (`--amber`), óxido de apoyo (`--rust-line`).
- **Tipografía**: Roboto Slab (titulares) + Barlow (interfaz) + Barlow Semi Condensed tabular (cifras). Self-hosted en `src/assets/fonts/`.
- **Shadow DOM**: Los estilos globales reales viven en `tokens.css` (importado en cada `*.element.css`). `index.css` solo alcanza DOM ligero.
- **Historial**: Sustituye a "Telemetry & Freedom" (ADR-019). Archivos legacy eliminados (`estilos_base.scss`, `filosofia de diseño.md`).

## Quality Gates
- **Test pass rate**: 100% (frontend + backend)
- **Code coverage (TS)**: 80% (lines, functions, branches, statements)
- **AC coverage**: 100% (cada criterio de aceptación debe tener al menos un test)
- **ESLint**: 0 warnings, 0 errors
- **Clippy**: 0 warnings (deny)
- **rustfmt**: Código formateado
- **cargo audit**: Sin vulnerabilidades conocidas
- **Build**: tsc sin errores + cargo build exitoso + vite build exitoso + tauri build exitoso

## Estado Actual del Proyecto
- **Sesión 2026-08-07 (3) — `almacenamiento-fotos-backend` (ADR-042): primer uso real de MinIO, cifrado de aplicación AES-256-GCM, solo backend**: continuación directa de la sesión anterior, a petición del usuario ("vamos a crear una nueva spec... coge contexto... eso es, demomento solo la parte del back"). Cierra el hueco que dejó `rutas-en-la-nube` (dos `// TODO` en `route-detail.element.ts`, líneas 433/508) y le da a MinIO su primer consumidor real (ADR-041 lo dejó documentado como "provisión anticipada sin consumidor").
  **Diseño acordado con el usuario antes de proponer**: nuevo paquete `internal/photos/` (mismo patrón que `internal/routes/`), `BlobStore` sobre MinIO vía `minio-go`, **cifrado de aplicación** (no SSE-S3 de MinIO — la clave vive solo en `apps/api`, nunca junto a los datos, para que un compromiso de MinIO solo no baste para ver las fotos), cuatro endpoints (`POST/GET /api/routes/{id}/photos`, `GET/DELETE .../{photoId}`) siempre proxied (nunca una URL directa ni presigned de MinIO). El usuario preguntó explícitamente si cifrar era "pesado" antes de decidir — se confirmó con hechos del propio servidor (`/proc/cpuinfo` tiene `aes`, AES-NI real) que el coste es insignificante frente al tiempo de red.
  **Implementación en TDD real** (150/150 tests Go en verde tras el cambio, incluidos tests de integración contra un MinIO real, no solo Postgres): `Encrypt`/`Decrypt` (`crypto/aes`+`crypto/cipher` estándar de Go), `MinioBlobStore`, `PostgresPhotoStore` (propiedad resuelta siempre vía `route_id`→`routes.user_id`, nunca un `user_id` propio en `route_photos`), y los cuatro handlers.
  **Tres gaps reales encontrados verificando con `curl` de verdad contra el stack completo, ninguno visible solo con tests**: (1) `bitnami/minio` (elegido inicialmente para que CI pudiera levantarlo como *service container* declarativo, ya que la imagen oficial `minio/minio` exige el argumento `server /data` que el bloque `services:` de GitHub Actions no admite pasar) **ya no está en Docker Hub** — descubierto al intentar `docker run bitnami/minio:latest` de verdad, no al leer documentación; resuelto levantando MinIO oficial con un paso `docker run` manual + espera por `curl` desde el propio runner. (2) La respuesta de `POST .../photos` devolvía `created_at` vacío — `Create` nunca devolvía el valor real que Postgres asigna por `DEFAULT now()`; corregido con `RETURNING created_at`, cambiando la firma de `PhotoStore.Create` para devolver el `Photo` completo. (3) Una imagen fresca de MinIO (vía `docker compose down -v` + `up`) no tiene el bucket `images` — a diferencia de Postgres, la imagen oficial de MinIO no trae ningún equivalente a `docker-entrypoint-initdb.d`; resuelto con un servicio `minio-init` de un solo uso (`minio/mc`) en `docker-compose.yml`, sin ese problema en producción porque el bucket ya existía de antes (ADR-041).
  **Verificación real end-to-end en dos entornos**: local (`docker compose up --build` desde cero, subida→listado→descarga con bytes idénticos al original→borrado, aislamiento entre cuentas con 404 sin revelar existencia, e inspección directa de los bytes en MinIO con `mc cat` confirmando 97 bytes sin cabecera PNG — 69 originales + 12 nonce + 16 tag GCM) y **producción real, desplegada sin fusionar** (mismo patrón que ADR-034/038/039/041: imagen anterior etiquetada `docker-api:pre-almacenamiento-fotos`). En producción, además: usuario de MinIO dedicado a `apps/api` creado con una política acotada solo al bucket `images` (nunca las credenciales root), credenciales y `PHOTO_ENCRYPTION_KEY` generadas y escritas directamente en `.env.prod` del servidor por un script remoto — nunca visibles en la salida del propio agente. **Un `DELETE` a través de Tailscale Funnel devolvió un 404 transitorio justo tras el redeploy** (confirmado no ser un bug real: la misma petición directa contra `100.114.190.36:8080` devolvió el 401 correcto, y repetida por Funnel poco después funcionó) — probablemente Funnel con una conexión en vuelo hacia el contenedor recién reiniciado. Cuentas y rutas de prueba borradas después, `count(*) = 0` en las tablas relevantes.
  **Disciplina de commits de esta sesión**: el usuario pidió explícitamente comitear en local sin hacer `push` hasta que todo funcionara, para no disparar Actions de más — 7 commits locales en `feature/almacenamiento-fotos-backend`, `push` solo al llegar al paso de despliegue real (necesario para que el servidor pudiera hacer `git fetch`; confirmado antes de hacerlo que `ci.yml` solo dispara en `push` a `master` o en `pull_request`, nunca en el push de una rama feature suelta).
  **Próximo hito**: revisar el diff completo buscando secretos (hecho, sin hallazgos — solo nombres de variables y credenciales triviales de dev ya versionadas desde antes), `/opsx:archive` con review independiente, y PR de `feature/almacenamiento-fotos-backend` a `master`. Después, cambio de frontend aparte para cerrar los dos `// TODO` de `apps/mobile` (integración real de subida/borrado de fotos con este backend).
- **Sesión 2026-08-07 (2) — `hardening-despliegue-servidor` (ADR-041): contenedor no-root, script de despliegue versionado, MinIO documentado, servidor redesplegado en real**: continuación directa de la sesión anterior. Una auditoría de seguridad pedida por el usuario (`docs/informe-tecnico-seguridad.md`) encontró tres hallazgos: el contenedor de `apps/api` corría como `root`, MinIO estaba sin documentar en ningún ADR, y el servidor no se había redesplegado tras `rutas-en-la-nube` (`/api/routes` daba 404, faltaba la migración `0005`).
  **Revisión de los artefactos antes de continuar (a petición explícita del usuario, "revisa si todo es correcto")** encontró varios problemas reales, todos corregidos antes de desplegar: (1) `scripts/deploy-prod.sh` construía el comando remoto como `cd '$REMOTE_DIR'` con `$REMOTE_DIR="~/moto-routes"` — con comillas simples bash no expande el `~`, así que el primer uso real habría fallado con "No such file or directory"; verificado el bug y el fix con una simulación de las dos capas de shell (local construye el string → `ssh` → el shell remoto lo reparsea desde cero) antes de tocar nada. (2) Mismo script, un backtick dentro de un `echo "..."` con comillas dobles ejecutaba de verdad `` `chmod 600` `` (sin argumento) como sustitución de comandos en vez de imprimirlo literalmente. (3) Variable `REPO_DIR` sin usar. (4) `docs/informe-tecnico-seguridad.md` tenía basura de un tool-call mal cerrado al final del fichero. (5) `design.md` documentaba `addgroup`/`adduser` pero el `Dockerfile` real usa `groupadd`/`useradd` — corregido el texto del diseño para que coincida con el código ya verificado, no al revés.
  **Despliegue real verificado contra producción, desde la rama sin fusionar** (mismo patrón que ADR-034/038 — `scripts/deploy-prod.sh` hace `git pull origin master`, que todavía no incluye este cambio, así que se hizo `git checkout feature/hardening-despliegue-servidor` a mano en el servidor + `docker compose up -d --build`, no el script literal). Imagen anterior etiquetada `docker-api:pre-hardening` para rollback. Verificado con hechos, no solo logs: **usuario no-root** confirmado con `docker exec docker-api-1 id` (`uid=999(appuser)`) — `docker exec ... ps` no sirve porque `debian:trixie-slim` no trae `procps`, hay que usar `docker top <contenedor>` (usa el `ps` del host) o `id` dentro del contenedor. **Migración `0005` aplicada** (consultado `schema_migrations` por SQL directo). **`/api/routes` funcional de punta a punta**: cuenta de prueba real registrada contra el servidor de producción, verificada por SQL, login real, `GET /api/routes` con el JWT devuelto → `200 []` (401 sin token) — cuenta borrada después, `count(*) = 0` confirmado. `/api/ping` verificado `200` en la URL pública de Funnel.
  **Próximo hito**: revisar el diff completo buscando secretos antes de abrir la PR (gate de seguridad, tarea 5.3), luego PR de `feature/hardening-despliegue-servidor` a `master` (referenciando esta verificación real, mismo patrón que ADR-038/039) y fusionar.
- **Sesión 2026-08-07 — `rutas-en-la-nube` (ADR-040): primera persistencia de rutas en el servidor, ligada a la cuenta; implementación completa (grupos 1-8) verificada con backend real, Cypress real y dispositivo Android real.** PR #105 (incidencia de GitHub Actions de la sesión anterior) confirmada resuelta y fusionada a `master` al empezar esta sesión.
  **Backend** (`apps/api/internal/routes/`, patrón ya establecido por `internal/auth/`/`internal/stoptypes/`): migración `0005_create_routes.sql` (`routes`/`route_points`/`route_stops`, `routes.id` UUID = mismo id que genera el cliente, sin tabla de mapeo), `PostgresRouteStore.Upsert` (con guarda explícita: un `id` ya usado por otra cuenta nunca se sobrescribe, ni siquiera silenciosamente — `ErrRouteOwnedByAnotherUser`) / `ListByUser` / `GetByIDForUser`, tres endpoints (`POST/GET /api/routes`, `GET /api/routes/{id}`) tras `RequireAuth` + CORS/OPTIONS desde el primer commit. 110/110 tests Go en verde, `go vet`/`govulncheck` limpios.
  **Frontend**: `route-cloud-api.service.ts` (cliente HTTP, patrón `auth-api.service.ts`), fusión local+nube pura (`route-list-sync.transform.ts`) con indicador visual por estado (`data-cy="route-card-sync-badge"`, solo visible con sesión activa), botón "Subir a la nube" en `route-detail` (visible solo con sesión y ruta local), rama de detalle para una ruta exclusiva de la nube (`route-detail-cloud.service.ts`, ids de puntos/paradas sintetizados solo para presentación). 904/904 tests Vitest en verde, cobertura 96.25%/90.81%/94.06%/96.25% (statements/branches/functions/lines).
  **E2E Cypress nuevo** (`route-cloud-sync.cy.ts`, backend real): subida real, listado sin duplicar, ruta cloud-only sembrada vía API en el propio test, aislamiento entre cuentas — 51/51 specs de la suite completa en verde, sin regresiones.
  **Verificación en dispositivo Android real, con hallazgos reales que no habían salido en tests**: (1) **fallo real corregido** — las rutas cloud-only se añadían siempre al final del listado sin importar su fecha (`mergeLocalAndCloudRoutes` no ordenaba el resultado); arreglado con `.sort()` por `createdAt` descendente + test de regresión. (2) **gotcha nuevo de build Android**, ver sección "Build Android" más abajo — el atajo "copiar `dist/` a mano + Gradle-only" (documentado como fiable en una sesión anterior) no sirvió esta vez; solo el build completo de la CLI (con Rust) hizo que el WebView sirviera el JS nuevo, y la única verificación que lo detectó fue Chrome DevTools Protocol contra el WebView real (`unzip` del APK no basta — confirma qué se empaquetó, no qué se sirve en runtime). (3) Dos falsos positivos descartados en el propio proceso: un mensaje "Ã³" que parecía mojibake del pipeline y una réplica JSON corrupta por caracteres acentuados — ambos eran el propio terminal Git Bash de esta sesión mutilando UTF-8 al pasar argumentos, no un bug real (confirmado con un payload UTF-8 limpio vía fichero).
  **Ronda 2, mismo día — refinamiento de UI a petición del usuario tras probar en dispositivo real**: el botón "Subir a la nube" (ancho completo) pasa a un icono pequeño junto al título (nube-subir / nube-check, pulsable en ambos estados), detectando si la ruta ya está sincronizada con una consulta a `checkIfRouteIsSynced` (nueva, `route-detail-cloud.service.ts`) al cargar. Nueva **re-subida automática silenciosa**: guardar una nota o añadir/borrar una foto en una ruta ya sincronizada la re-sube sola en segundo plano (sin toast de éxito, solo aviso discreto si falla) — nunca sube una ruta puramente local; fotos en sí mismas quedan con un `// TODO` explícito en el código para cuando exista blob storage. El indicador de estado del listado pasó por **dos iteraciones visuales** antes de convencer al usuario (chip en la fila de distancia/duración → icono junto al botón de eliminar → **insignia superpuesta en la esquina de la miniatura**, la que se quedó) — las dos primeras dejaban el icono visiblemente descentrado, confirmado con medición real (no solo a ojo) vía Chrome DevTools Protocol contra el WebView del dispositivo. De paso, un `<h1>` sin el margen del navegador reseteado (gap real de ~20px, no solo en esta pantalla) corregido en el reset global de `tokens.css`. Ver ADR-040 (ampliada) y la sección "Build Android" más abajo para el gotcha nuevo de build encontrado reiteradamente en esta ronda.
  **`/opsx:archive` completado — veredicto `review.md`: APPROVED.** Revisión independiente (suites reejecutadas por quien revisa, no solo el resumen): 110/110 Go, 917/917 Vitest (cobertura 96.29%/90.95%/94.1%/96.29%), 52/52 Cypress, `tsc`/`eslint` limpios en todo `apps/mobile/src`. 18/18 escenarios del spec con test, dos gaps menores señalados (no bloqueantes, mismo camino de código ya cubierto por otro escenario). Nueva spec principal creada en `openspec/specs/route-cloud-sync/spec.md` (capacidad nueva, sin spec previa que fusionar). Archivado en `openspec/changes/archive/2026-08-07-rutas-en-la-nube/`.
  **Próximo hito**: PR de `feature/rutas-en-la-nube` a `master`, referenciando `review.md` ya archivado. Si en el futuro se retoma el atajo "Gradle-only" para builds de Android, no confiar en él sin re-verificar con CDP — y aun con el build completo, verificar el hash `dist/` vs APK cada vez, repitiendo copiar+rebuild si no coincide.
- **Sesión 2026-08-06 (4) — `pantallas-auth-mobile` implementado y verificado en dispositivo real; gap de CORS (PR #104) y optimizaciones de CI (PR #105) abiertas, bloqueadas por una incidencia real de GitHub Actions; auditoría de seguridad del servidor de producción y redespliegue a `master`**: sesión larga, continuación directa de las dos anteriores.
  **`pantallas-auth-mobile`** (27/30 tareas, dominio nuevo `src/auth/`): diálogos de registro/login/recuperar-contraseña, extensión de `fetchJson` (POST/headers/`checkStatus` explícito, no inferido del método), persistencia de sesión (`ISessionRepository`, SQLite/memoria), sección "Cuenta" en Perfil, E2E Cypress nuevo (`auth.cy.ts`, backend real). **Gap real encontrado por el propio E2E, invisible con `curl` o páginas same-origin**: `apps/api` no respondía a preflight `OPTIONS` ni declaraba `Access-Control-Allow-Methods`/`Headers` en los endpoints de auth — corregido en `httpmw.PublicCORS`, **PR #104** (rama propia, no bundleado con el frontend por tocar código ya en `master`). De paso, revisados `ci.yml`/`docs.yml`: sin `concurrency` group (runs de PR obsoletos no se cancelaban), `docs.yml` sin caché de Rust para `cargo doc`, Cypress/`govulncheck` sin cachear — **PR #105**. Ambas PRs con CI en verde salvo por una **incidencia real de GitHub Actions (Major Outage, iniciada 2026-08-06 15:22 UTC)** — jobs colgados 15 min sin runner o fallando con "Service Unavailable" al resolver actions, confirmado en `githubstatus.com`, nada que ver con el código. Pendiente: fusionar ambas en cuanto se resuelva.
  **Verificación en dispositivo real (grupo 10), tres gaps de conectividad, ninguno bug de la app**: build local por USB apuntando primero a Wi-Fi LAN, después a Tailscale — `PUBLIC_API_BASE_URL` exige prefijo `https://` (validación intencionada) pero el backend de pruebas solo habla HTTP, rompiendo el enlace de verificación por email en LAN; `tailscale serve` (solo-tailnet) no bastaba porque el móvil nunca se había unido a la tailnet; resuelto con `tailscale funnel` sobre el backend de pruebas de este PC (mismo mecanismo que ya usa producción, ADR-036), build "como la release" (mismo comando y parche de CSP que `ci.yml::build-and-release`). Los 5 escenarios probados a mano en el dispositivo real. Detalle completo en `tasks.md` grupo 10.
  **Auditoría de seguridad del servidor de producción (a petición del usuario, no parte de la spec)**: sin SSH tradicional expuesto (solo Tailscale SSH, cero puertos abiertos fuera de `tailscale0`), `ufw` con *deny* por defecto, Postgres solo loopback con `scram-sha-256`, todos los ficheros de secretos en `600`, sin intentos de login fallidos ni sesiones de origen desconocido. **Hallazgo nuevo, confirmado legítimo por el usuario**: servicio **MinIO** corriendo (`systemd`, solo loopback `127.0.0.1:9000`/`9001`) sin documentar en ningún ADR — provisión anticipada para el "blob storage" mencionado como objetivo futuro en ADR-034, **todavía sin ningún código que lo use**. Borrado un backup suelto de `.env.prod` (`.env.prod.bak.*`, permisos correctos, nunca rastreado por git, solo redundante). Recordatorio, no hallazgo nuevo: `gonzalo` sigue con sudo `NOPASSWD: ALL` (ADR-033, trade-off ya aceptado). **Servidor redesplegado a `master`** (`722307f`, 9 commits que llevaba de retraso) tras la auditoría, verificado sano (`/api/ping` 200, migraciones `0001`-`0004` aplicadas).
  **Próximo hito**: fusionar PR #104/#105 en cuanto GitHub Actions se recupere; commitear el cierre de `pantallas-auth-mobile` (`tasks.md` grupo 11, este mismo resumen); `review.md` independiente; `/opsx:archive`; PR final del frontend a `master`. Si en algún momento se implementa el blob storage de MinIO, documentarlo con su propia ADR.
- **Sesión 2026-08-06 (3) — `estilizar-pagina-verificacion`: la página de verificación de email deja de ser un `<p>` plano**: cambio pequeño, `skip_specs: true` (sin comportamiento observable nuevo, solo presentación — confirmado que ningún `Requirement`/`Scenario` de `email-verification` cambia). Extraído `authPageCSS`/`authPageShell` a `apps/api/internal/auth/auth_page_layout.go`, compartido entre la página de reset de contraseña (`reset-contrasena`, ADR-039) y la de verificación — sin duplicar el CSS. De paso, consolidado `writeVerificationHTML` (duplicado de `writeHTMLPage`, ya existente) en un único helper. Verificado con `curl` (marcado `class="panel"` presente) y confirmado visualmente por el usuario en un navegador real, tanto la página de error como la de éxito. 90/90 tests Go en verde, sin tocar `reset_password_confirm_test.go` (el refactor no cambió su HTML final). Sin ADR nueva — refactor de presentación, no una decisión de arquitectura.
  **Próximo hito**: `/opsx:archive` de este cambio (más corto, probablemente sin necesitar despliegue a producción por ser solo presentación) y luego la pieza grande pendiente: pantallas de `apps/mobile` para registro/login/recuperación de contraseña, como flujo aislado que no exige login para el resto de la app todavía (decisión ya tomada con el usuario).
- **Sesión 2026-08-06 (2) — `reset-contrasena` (ADR-039): página web real con formulario, estilo "Asfalto Nocturno", verificada en local y producción**: continuación directa de la sesión anterior, tras fusionar `confirmacion-email-usuarios` (PR #100). El usuario pidió explícitamente tres garantías de seguridad antes de implementar: caducidad del enlace, que no se pueda cambiar la contraseña de otra cuenta, y que el enlace no lleve email/username — las tres se dejaron como requisitos explícitos en la spec con tests dedicados, no solo implícitas en el diseño.
  **Propuesta completa** (29 tareas) sobre `apps/api/internal/auth/`: `POST /api/auth/reset-password/request` (sin enumeración, rate limited) y `GET`/`POST /api/auth/reset-password/confirm` — a diferencia de la confirmación de email (un enlace basta), el reset necesita un formulario real para escribir la contraseña nueva, así que **esta página sí lleva ya el diseño de la app** (valores `oklch()` de `tokens.css` copiados como CSS embebido en Go, sin pipeline compartido entre `apps/api`/`apps/mobile`) — decisión tomada explícitamente con el usuario, dejando el resto de páginas (verificación de email) para un futuro cambio de frontend.
  **Garantías de seguridad verificadas con tests dedicados, no solo por diseño**: token de reset caduca en 1h (más corto que el de verificación de email, 24h — más riesgo si se filtra), y un test que intenta colar un `email`/`user_id` de otra cuenta en el `POST` del formulario confirma que la cuenta afectada se determina siempre desde el token, nunca desde ningún campo del formulario. Aprovechado para renombrar `generateVerificationToken`/`hashVerificationToken` a nombres genéricos (`generateOneTimeToken`/`hashOneTimeToken`) ya que este cambio introduce su segundo consumidor real — a diferencia del rename de `LoginRateLimiter` que se dejó como deuda en ADR-038, este se hizo en el momento por ser barato.
  **Gap real encontrado verificando en local (no en tests)**: `infra/docker/.env` seguía con el remitente sandbox de Resend de antes de verificar el dominio propio — ya había agotado su envío de prueba único, mismo error `Domain is not verified` que ya se había resuelto en producción. Corregido alineando el `.env` local al remitente ya verificado.
  **Verificación real en dos entornos**: local (formulario visto y confirmado visualmente por el usuario en un navegador real, contraseña cambiada de verdad, login con la anterior rechazado y con la nueva aceptado) y producción real, desplegada sin fusionar (mismo patrón que ADR-038: imagen anterior etiquetada para rollback). En producción, un primer intento de reset con una contraseña de 6 caracteres fue rechazado de verdad server-side (confirma la política mínima en producción, no solo en tests), y el flujo completo funcionó desde fuera del tailnet. Cuenta de prueba borrada después.
  **Próximo hito de la siguiente sesión**: `/opsx:archive` de `reset-contrasena` (con revisión independiente igual que ADR-038) y PR a `master`. Después, el frontend pendiente: pantallas de `apps/mobile` para todo este bloque de auth, y estilizar el resto de páginas de `apps/api` (verificación de email) con el mismo criterio ya sentado aquí.
- **Sesión 2026-08-06 — `confirmacion-email-usuarios` (ADR-038): login exige email verificado, con despliegue y verificación reales en local y en producción**: continuación directa de la sesión anterior — PR #98 (`rules.security`) fusionada, y se detectó que esa sección quedaba huérfana (la CLI de OpenSpec no la inyecta en ningún artefacto, solo reconoce `rules.proposal/specs/design/tasks`) — corregido en PR #99 aparte (fusionada primero, rama propia) fusionando su contenido en `context`, que sí llega a todos los artefactos.
  **Propuesta completa** (`proposal.md`/`specs/`/`design.md`/`tasks.md`, 37 tareas) sobre `apps/api/internal/auth/` ya existente (ADR-034): nueva columna `email_verified`, tabla `email_verification_tokens`, `POST /api/auth/verify-email/request` (solicitud/reenvío sin enumeración de cuentas) y `GET /api/auth/verify-email/confirm` (confirma el token, HTML mínimo — `GET` porque el enlace lo dispara un email sin frontend detrás). **Decisión BREAKING confirmada explícitamente con el usuario antes de escribir la spec**: el login pasa a rechazar cuentas sin verificar (403), no se dejó como puramente informativo.
  **Implementación en TDD real, ciclo rojo→verde confirmado en cada tarea**: 68/68 tests Go en verde, `go vet`/`govulncheck` limpios (misma excepción ya documentada). Detalle completo de las decisiones técnicas (token `crypto/rand`+`sha256` en vez de `bcrypt`, Resend vía `net/http` puro sin SDK, reutilización de `LoginRateLimiter` para el rate limit del endpoint nuevo) en ADR-038.
  **Tres gaps reales encontrados verificando contra servicios externos de verdad, ninguno detectable solo con tests**: (1) `debian:trixie-slim` (runtime del `Dockerfile` de `apps/api`) no traía `ca-certificates` — cualquier HTTPS saliente nuevo fallaba con `x509: certificate signed by unknown authority`, nunca hizo falta hasta ahora porque la única dependencia externa era Postgres sin TLS; corregido instalando el paquete. (2) Los fallos de envío de email se descartaban en silencio pese a que `design.md` prometía loguearlos — corregido con `log.Printf` (nunca el token en claro). (3) El remitente sandbox de Resend (`onboarding@resend.dev`) solo entrega de verdad una vez; a partir de ahí Resend acepta con `200` pero la entrega falla de forma asíncrona por dominio no verificado — invisible para un manejo de errores que solo mira el status HTTP síncrono. Resuelto **comprando un dominio real** (`motor-routes.com`, Cloudflare Registrar) y verificándolo en Resend — detalle completo, incluidas las alternativas descartadas (SMTP propio en el servidor doméstico, relay vía Gmail), en ADR-038.
  **Verificación real end-to-end en dos entornos, no solo simulada**: local (`docker compose up --build`, registro real, email recibido, token extraído del enlace y confirmado contra `localhost:8080` — el enlace real no es alcanzable en local sin TLS en el 443, limitación esperada) y **producción real, desplegada desde la rama sin fusionar** (mismo patrón que ADR-034: imagen anterior etiquetada para rollback, migración `0003` aplicada sola, secretos de `.env.prod` añadidos vía script remoto que nunca los imprime). En producción, con el dominio ya verificado: registro real → email recibido → enlace confirmado **desde fuera del tailnet** (`https://debian.taildf3dab.ts.net`) → login pasa de 403 a 200 → token reutilizado rechazado (400). Cuenta de prueba borrada de la base de datos de producción tras verificar, confirmado `count(*) = 0`.
  **Próximo hito de la siguiente sesión**: `/opsx:archive` (review.md, veredicto) y PR de `feature/confirmacion-email-usuarios` → `master`, referenciando este resumen y ADR-038.
- **Sesión 2026-08-05 — investigación de la release `v0.1.0` rota (ADR-036) + overlay de permiso GPS con falso positivo (ADR-037), releases `v0.1.1`/`v0.1.2`/`v0.1.3`**: tres incidentes reales sobre la primera integración `apps/mobile`↔`apps/api`↔dispositivo real de punta a punta, cada uno con causa distinta a la que parecía a simple vista — detalle completo con alternativas en ADR-036/ADR-037, aquí solo el resumen operativo.
  **Release rota, tres causas acumulativas** (usuario: "no salta el menú de tipo de parada, no sé si es la llamada HTTP o un error de compilación" — ninguna de las dos, en realidad): (1) servidor Debian de producción apagado (`tailscale status` lo mostraba `offline` desde esta misma máquina, también en el tailnet); (2) una vez encendido, backend desplegado seguía en `feature/migrar-api-golang` anterior al catálogo de tipos de parada (`404` en `/api/stop-types`) — redesplegado a `master` (`git reset --hard origin/master`, historia local divergente sin commits propios de valor); (3) el secret de GitHub `MOBILE_PROD_API_BASE_URL` estaba guardado como IP pelada (`100.114.190.36`) en vez de `http://100.114.190.36:8080` — el bundle JS horneaba esa URL rota y el `fetch()` se resolvía como ruta relativa contra el propio origen de la app, nunca contra el servidor real; el check de CI que verifica la release (`grep -qF "$PROD_API_BASE_URL"` sobre el HTML empaquetado) no lo detectó porque solo comprueba presencia del literal, no que sea una URL absoluta válida (deuda técnica anotada en ADR-036, no arreglada esta sesión). Corregido el secret, publicada `v0.1.1`. Efecto colateral real observado: instalar `v0.1.1` sobre el `v0.1.0` ya puesto puede fallar en silencio por firma de keystore de debug distinta (cada build de CI genera una nueva) — la app vieja se queda corriendo sin aviso, indistinguible en la UI de "la release sigue rota".
  **Cuarto problema, esta vez de diseño y no operativo, señalado por el propio usuario** ("cualquier persona debe poder acceder a la API sin entrar con mi cuenta"): con `v0.1.1`/`v0.1.2` ya sirviendo el catálogo bien en el dispositivo de desarrollo, resultó que `100.114.190.36` es una IP de Tailscale — solo alcanzable para dispositivos dentro del tailnet del desarrollador. ADR-033 ya eligió deliberadamente "solo Tailscale" por no tener IP pública, válido para uso interno, pero incompatible con una release pública real (un usuario cualquiera no puede unirse a la red privada del desarrollador). Corregido activando **Tailscale Funnel** en el servidor (`sudo tailscale funnel --bg http://100.114.190.36:8080` — el target por defecto, loopback, no sirve porque `apps/api` solo escucha en la IP de Tailscale) — expone `https://debian.taildf3dab.ts.net`, verificado genuinamente público (DNS público de Google, `curl` con certificado válido sin estar en el tailnet). Secret actualizado a esa URL HTTPS, publicada `v0.1.3`. Detalle completo y alternativas descartadas en la actualización de ADR-036.
  **Overlay de permiso GPS, causa distinta a la sospechada**: `navigator.permissions.query({name:'geolocation'})` (usado por `checkPermissions()` desde que existe, para evitar el crash real de `RecordingService.kt` sin permiso) se queda encallado en `'prompt'` en el WebView real de Android aunque `adb shell dumpsys package` confirme el permiso del SO ya concedido — comportamiento conocido de los WebView Android (Permissions API refleja un estado interno del WebView, no el permiso real del SO), no detectable sin dispositivo real. Diagnosticado con un toast de diagnóstico temporal + `adb exec-out screencap` (logcat inutilizado por saturación de logging JNI verbose del propio proceso — `LOGS OVER PROC QUOTA... DROPPED`). Sustituido por `probeGeolocationPermission()` (pide una localización real con `timeout: 8000`, solo `PERMISSION_DENIED` cuenta como denegación real). Primer intento de la sesión —esperar la comprobación antes del primer `render()`— revertido tras romper `cockpit.cy.ts`/`cockpit-mark-stop.cy.ts` en Cypress (Electron headless no resuelve geolocalización real sin stub) **y** ser mala UX real (cockpit en blanco con señal lenta); solución final: caché de fondo como camino rápido + repetición del sondeo real justo antes de decidir en `handleStartStop()`, sin bloquear nunca el render inicial. Nuevo comando Cypress compartido `stubGpsPermissionGranted`. PR #95 → `master`, release `v0.1.2`.
  **Verificación real repetida en dispositivo, no solo simulada**: build local por USB (nunca la release de CI directamente, para poder iterar) con el gotcha de JBR/Java 25 ya documentado en este mismo fichero (`JAVA_HOME` forzado a `jdk-24`) y el gotcha de assets desactualizados en el APK (sync manual + reempaquetado solo-Gradle) reproducidos y aplicados de nuevo tal cual estaban anotados — sin sorpresas nuevas, la documentación existente bastó. `adb shell dumpsys package`/`screencap`/`input tap`/`input swipe` (long-press simulado) para verificar sin depender de que el usuario reportara lo que veía. 814/814 Vitest, 41/41 Cypress, `tsc`/ESLint limpios en ambos cambios.
  **Docker Desktop y el backend local se pararon y volvieron a levantar varias veces** en la misma sesión para verificar `cockpit-mark-stop.cy.ts` (necesita `apps/api` real) — recordar que el pre-commit hook lo exige también en local, no solo en CI (`.husky/pre-commit` ejecuta la misma suite `pnpm test:e2e` completa).
  **Arranque de la próxima spec, "usuarios" — sesión pausada a mitad, retomar aquí**: el usuario pidió crear infraestructura de usuarios (BBDD, autenticación, confirmación por email). Antes de proponer nada se investigó `apps/api/internal/auth/` y resultó que **la mayor parte ya existe** desde `migrar-api-golang` (ADR-034) — `spec/user-auth` ya está en `openspec/specs/`: registro (`register.go`), contraseñas con `bcrypt` (`password.go`), login + JWT (`login.go`/`token.go`), middleware de autenticación, rate limiting de login. **Lo único que falta de verdad**: confirmación de email (sin columna `email_verified`, sin envío de correo, sin tabla de tokens de verificación) — el resto de lo pedido ya estaba resuelto, evitó duplicar trabajo. Decisiones ya tomadas con el usuario para la spec de confirmación de email (pendiente de abrir con `/opsx:propose`): proveedor transaccional **Resend** (no SMTP propio, la máquina doméstica no tiene reputación de IP saliente), **sin reset de contraseña todavía** (spec aparte más adelante), alcance solo backend (sin frontend por ahora).
  **Paso previo ya hecho, PR #98 abierta, pendiente de que el usuario la revise/fusione**: nueva sección `rules.security` en `openspec/config.yaml` (secretos nunca en código/commits ni "para probar", nada de criptografía o parseo de tokens a mano, respuestas de registro/login/reset sin permitir enumeración de cuentas, rate limiting explícito en cualquier endpoint de auth nuevo, checklist CRÍTICO ampliado en `operations.archive.guidance` para cambios de autenticación) — se adelantó a la spec de usuarios porque va a tocar justo esa superficie. Validado con `openspec validate --all` (11/11 specs existentes siguen pasando). Rama `docs/openspec-normas-seguridad`.
  **Próximo hito de la siguiente sesión**: (1) fusionar PR #98 si el usuario da el visto bueno al texto; (2) `/opsx:propose` para la confirmación de email sobre `apps/api/internal/auth/` (nueva rama desde `master`), citando `spec/user-auth` ya existente como lo que NO hay que reimplementar.
- **Sesión 2026-08-04 — `apps/api` migrado de Java/Spring Boot a Go (`migrar-api-golang`, ADR-034), con autenticación de usuarios y desplegado en producción real**: cambio completo `propose→apply` (archive pendiente) sobre la decisión de ADR-032. Motivado por funcionalidad ya conocida (a diferencia de ADR-032, que eligió Spring Boot sin saber qué traería el crecimiento): validación/autenticación de usuarios, apartado de seguridad explícito, y preparar el terreno para blob storage tipo Azure y notificaciones en tiempo real tipo SignalR (ninguna de las dos implementada, solo no bloqueadas). Decisión Go sobre Rust confirmada con el usuario antes de proponer: SDK de Azure Blob Storage más maduro, librerías JWT/OAuth más asentadas, goroutines más naturales para tiempo real — el trade-off descartado fue reutilizar la disciplina Rust ya cableada en `apps/mobile/src-tauri`.
  **Stack Go nuevo** (detalle completo con alternativas en ADR-034): `chi` (router/middleware sobre `net/http`, no un framework completo), `jackc/pgx/v5` (driver Postgres), `golang-jwt/jwt/v5` + `golang.org/x/crypto/bcrypt` (tokens de sesión y hashing), runner de migraciones propio (ficheros `.sql` + tabla `schema_migrations`, se ejecuta solo al arrancar el binario — decisión corregida en `apply`: inicialmente se pensó en aplicar la migración de producción a mano por SSH como la tabla dummy de ADR-033, pero al ser un mecanismo automático embebido en el binario no hace falta, corre igual contra el Postgres nativo), rate limiting de login en memoria (mapa+mutex, sin Redis, aceptado mientras sea una sola instancia).
  **Gap real de seguridad encontrado en `apply`, no detectable antes de tener código**: al llegar a la tarea de auditoría (`govulncheck`, primera vez que se audita Go en este proyecto) aparecieron 2 vulnerabilidades reales y alcanzables desde el propio código — `GO-2026-5004` (inyección SQL por confusión de placeholders en `pgx@v5.8.0`, alcanzable desde `PostgresUserStore.FindUserByID`) y `GO-2026-5970` (bucle infinito en `x/text@v0.29.0`). Ambas versiones vulnerables habían sido fijadas deliberadamente para ser compatibles con Go 1.24 (la versión que trae el paquete `apt` `golang-1.24` de Debian trixie, elegida en un primer momento por coherencia con cómo se instaló el JDK en ADR-032). Las versiones que corrigen ambos CVEs exigen Go 1.25, que trixie no empaqueta todavía — resuelto cambiando la etapa de build de `apps/api/Dockerfile` a la imagen oficial `golang:1.25-trixie` (confirmado que el tag existe) en vez del paquete `apt`, manteniendo `debian:trixie-slim` sin cambios en el runtime. `govulncheck` quedó en 0 vulnerabilidades alcanzables tras el ajuste. Se dejó `govulncheck` cableado en `.husky/pre-commit` (extendiendo `pre-commit-audit-gate.spec.ts`, no duplicándolo) y como job `quality-go` nuevo en `ci.yml`.
  **TDD real en todo el flujo**: cada requisito de `user-auth`/`api-security` con su ciclo rojo→verde confirmado ejecutando `go test` de verdad (no solo revisión de código) — incluye tests de integración reales contra PostgreSQL (vía Docker local) para el runner de migraciones y el `PostgresUserStore`, no solo mocks. **Verificación E2E real repetida en tres entornos**: local sin Docker (binario Go directo contra Postgres del compose), Docker Compose local completo (`docker compose up --build`, ciclo `down`/`up` sin `-v` para confirmar persistencia, `docker stop postgres` para confirmar 503 controlado — tardó ~7.8s por resolución DNS de Docker al parar el contenedor, no por el timeout de conexión que se añadió como red de seguridad adicional; se aceptó por no ser "colgado indefinido", el requisito real), y **producción real** (ver más abajo).
  **Despliegue real en el servidor de producción, sobre la rama sin mergear todavía** (decisión explícita del usuario: desplegar desde `feature/migrar-api-golang`, no esperar al merge — precedente ya sentado por el propio ADR-033 de clonar y construir directamente en el servidor). Encontrado el usuario SSH real del servidor por prueba (Tailscale SSH deniega por política a varios candidatos hasta dar con `gonzalo`, con sudo sin contraseña — coincide con lo ya documentado en ADR-033, solo que esta sesión no tenía el dato a mano por estar redactado en memoria). **Antes de tocar nada**: imagen Java etiquetada `docker-api:pre-go-migration` para rollback; `infra/docker/.env.prod` transformado en el propio servidor vía script remoto que nunca imprime valores reales (`DB_URL`/`DB_USERNAME`/`DB_PASSWORD` → `DATABASE_URL` recompuesto; `AUTH_TOKEN_SECRET` nuevo generado con `/dev/urandom`, no existía antes). Verificación real post-despliegue: `curl` → 200; `ss -tlnp` confirma que solo escucha en la IP de Tailscale; registro/login/`me` reales contra producción (limpiados después); `schema_migrations` confirma la migración aplicada sola, sin paso manual por SSH; `systemctl restart docker` → el contenedor vuelve solo; `pg_hba.conf`/`postgresql.conf` no tocados (por construcción, el mecanismo nunca los toca). **Antes de este despliegue se auditó explícitamente, a petición del usuario, que ningún secreto real quedara en algo commiteable** (`.env`/`.env.prod` fuera de git, valores de ejemplo triviales, `git grep` sin coincidencias de secretos reales).
  **Entorno de esta máquina de desarrollo cambiado**: Go 1.26.5 instalado (`winget install GoLang.Go`, no existía antes) — el `go.mod` de `apps/api` fija `go 1.25` como mínimo, coherente con la imagen de build. `govulncheck` instalado vía `go install` (mismo patrón que `cargo-audit`, ya en `PATH` de usuario). Docker Desktop no estaba arrancado al empezar la sesión, se inició a mano.
  **Cerrado más tarde en la misma sesión**: `review.md` (veredicto APPROVED WITH MINOR ISSUES), `/opsx:archive`, PR #92 pasada de draft a lista y **mergeada a `master`** (confirmado por el usuario). Cambio archivado en `openspec/changes/archive/2026-08-04-migrar-api-golang/`.
- **Sesión 2026-08-04 (2) — catálogo de tipos de parada (`catalogo-tipos-parada`, ADR-035), primera llamada real `apps/mobile`→`apps/api`**: continuación directa de la sesión anterior, sobre `master` ya con el backend Go. Nueva tabla `stop_types` (8 categorías sembradas, icono emoji) servida por `GET /api/stop-types` (único endpoint público además de `/api/ping`), cacheada en SQLite local en el móvil. En el cockpit, pausar la grabación abre un modal para elegir el tipo de la parada (catálogo cacheado, funciona offline); el timeline y el mapa dejan de recalcular/detectar paradas por velocidad y pasan a leer solo paradas manuales con tipo persistidas en `route_stops` — una parada automática (semáforo) nunca se muestra ni se persiste.
  **Corrección de diseño a mitad de `apply`, confirmada con el usuario**: el modal iba a saltar también en paradas detectadas por GPS; corregido para que solo lo dispare una acción manual explícita del usuario.
  **Tres gaps reales encontrados durante la verificación (grupo 8), ninguno detectable sin backend real ni dispositivo real**: (1) `apps/api` no enviaba cabeceras CORS — el navegador bloqueaba en silencio el `fetch` del catálogo, `refreshStopTypesCache` (best-effort) lo confundía con un fallo de red normal; corregido con `internal/httpmw/cors.go::PublicCORS` (`Access-Control-Allow-Origin: *`, acotado a esa única ruta pública). (2) Verificado en dispositivo Android real (Realme `75fe536b`, misma WiFi, CSP/`.env.local` editados solo en local y revertidos después): el botón dedicado "Marcar parada" confundía al usuario, que esperaba que fuera el mismo botón que ya pausa la grabación — consolidado sobre "Pausar" (cancelar el modal no revierte la pausa). De paso, investigado un tercer síntoma que **no** era un bug de este cambio: el overlay de permiso GPS parecía "colgado" — `getCurrentPosition()` no lleva `timeout`, espera indefinidamente sin feedback hasta fijar posición y luego arranca la grabación sola; permiso de Android ya estaba concedido, pre-existente, dejado pendiente a petición del usuario. (3) La release de GitHub Actions (`ci.yml`, tag `v*`) no tenía ningún mecanismo para apuntar al servidor real — habría publicado un APK que solo sabe hablar con `localhost`; corregido con un secret nuevo `MOBILE_PROD_API_BASE_URL` (ya configurado por el usuario) que se inyecta en el runner (nunca comiteado) tanto en `VITE_API_BASE_URL` como en el CSP de `index.html`/`tauri.conf.json` — este último confirmado que `--config` de Tauri **no** lo reescribe para Android, gap documentado en ADR-035.
  **Verificación real, no solo tests**: `docker compose up` con backend real + nuevo test E2E Cypress (`cockpit-mark-stop.cy.ts`, backend real sin mocks — primera vez que la suite E2E ejercita la integración mobile→API de verdad) + build/instalación repetida en dispositivo Android real (gotcha de frontend desactualizado en el APK, ya documentado, reproducido 3/3 veces esta sesión, siempre resuelto con el fix manual de assets + rebuild solo-Gradle). **814/814 tests Vitest, 41/41 Cypress, `tsc`/ESLint/`go vet` limpios.** **Cerrado en la misma sesión**: `review.md` (veredicto APPROVED WITH MINOR ISSUES, único issue no bloqueante: el mecanismo de CI que apunta al servidor real aún no se ha probado en un runner real), `/opsx:archive` (nace `openspec/specs/{stop-types-catalog,cockpit-manual-stops,route-stop-types-display}/`), **PR #93 abierta** (`feature/catalogo-tipos-parada` → `master`, CI en marcha). Cambio archivado en `openspec/changes/archive/2026-08-04-catalogo-tipos-parada/`.
- **Sesión 2026-08-03 (2) — primer despliegue real: `apps/api` corriendo en el servidor de producción (`despliegue-servidor-tailscale`, ADR-033)**: continuación directa de `entorno-api-docker` (misma sesión, ver más abajo). El servidor real es un Debian 13 doméstico sin IP pública, alcanzable solo por Tailscale (`[tailscale-ip-redactada]` MagicDNS `debian`, usuario `[usuario-redactado]` con sudo) — **Tailscale se instaló en esta misma máquina de desarrollo** (`winget install tailscale.tailscale`) para poder desplegar por SSH desde la propia sesión; resultó estar ya autenticada de una instalación previa, sin necesidad de login interactivo.
  **Hallazgos reales por SSH que contradecían lo asumido**: (1) Docker no estaba instalado (se asumía que sí); (2) ya existía un **PostgreSQL 17 nativo** (no Docker) con `appdb`/`appuser` creados de antemano por el propio usuario específicamente para este despliegue — confirmado explícitamente que no es de otro proyecto — solo aceptando conexiones por loopback (`pg_hba.conf` sin exposición de red, sin `listen_addresses` personalizado); (3) Debian trixie solo empaqueta `docker-compose` v1 (Python, EOL), no el plugin `docker compose` v2 — resuelto añadiendo el repositorio oficial de Docker, **verificado por `curl` en esta sesión que sirve paquetes reales para `trixie`** (había reportes contradictorios sobre el soporte, se comprobó en vez de asumir) — Docker 29.7.1 + Compose v5.3.1 instalados (misma versión de Compose que el entorno local).
  **Decisiones de diseño** (detalle completo en [[ADR-033]]): el contenedor de `apps/api` usa `network_mode: host` para hablar con el Postgres nativo por `127.0.0.1` sin tocar su configuración (verificado con `sha256sum` de `pg_hba.conf`/`postgresql.conf` antes y después — idénticos); nueva propiedad `server.address=${SERVER_ADDRESS:0.0.0.0}` en `apps/api/src/main/resources/application.properties` (default `0.0.0.0` para no romper el entorno local) para que la API solo escuche en la IP de Tailscale, no en la LAN doméstica del servidor — confirmado con `ss -tlnp` que el puerto 8080 solo aparece asociado a `[tailscale-ip-redactada]`. Nuevo `infra/docker/docker-compose.prod.yml` (solo servicio `api`, `restart: unless-stopped`) — no se reutiliza el compose local, que levanta su propio Postgres y chocaría por el puerto 5432 ya ocupado por el nativo. Credenciales de `appuser` (regeneradas dos veces en la sesión — la primera copia se borró por higiene antes de guardarla donde tocaba, y hubo que regenerarla) en `infra/docker/.env.prod` en el propio servidor (`chmod 600`, no versionado), con `.env.prod.example` como plantilla versionada.
  La tabla dummy `healthcheck` se creó a mano vía `psql` (sin mecanismo `docker-entrypoint-initdb.d` en un Postgres nativo) — **gap real encontrado**: `sudo -u postgres psql -f ~/moto-routes/...` falla con "Permission denied" porque el usuario OS `postgres` no puede leer dentro del home de `[usuario-redactado]`; resuelto pasando el SQL por stdin. Tabla creada por `postgres` (superusuario), con `GRANT` explícito a `appuser` después (el endpoint actual usa `SELECT now()`, no toca todavía esta tabla).
  **Verificación real end-to-end, no solo local**: `curl http://[tailscale-ip-redactada]:8080/api/ping` desde la máquina de desarrollo (mismo tailnet) → 200 con timestamp real; `ss -tlnp` confirma exposición acotada a Tailscale; `sudo systemctl restart docker` → el contenedor vuelve solo (`restart: unless-stopped`) y sigue respondiendo. Verificado también que ningún fichero rastreado por git contiene la contraseña real (`git grep`).
- **Sesión 2026-08-03 — monorepo + primer backend (`entorno-api-docker`, ADR-032)**: primer paso hacia un servidor propio de Moto Routes (hasta ahora solo SQLite local). Reorganización a monorepo (`apps/mobile/` + `apps/api/` + `infra/docker/`) en el mismo cambio que el backend nuevo, porque la reorganización solo tenía motivo por su llegada.
  **Reorganización**: `git mv` de todo el contenido de raíz de la app móvil a `apps/mobile/` (preserva historial). **Gap real encontrado durante `apply`, no previsto en la propuesta**: `typedoc.json`/`scripts/docs-coverage.mjs`/`scripts/docs-prepare.mjs` leen `memory/`/`specs/ui/` y escriben en `docs/` — todos transversales, no exclusivos de la app móvil — así que NO se movieron; en su lugar, `package.json` raíz nuevo (solo scripts `docs:*` + `husky`, tres `devDependencies`, ninguna nueva) y `pnpm-lock.yaml` se quedan en la raíz (un workspace pnpm tiene un único lockfile). `pnpm-workspace.yaml` → `packages: [apps/mobile]`. CI (`ci.yml`) y `.husky/pre-commit` actualizados con TDD real (test en rojo confirmado antes de tocar cada fichero, verde después): `ci-workflow.spec.ts` y `pre-commit-audit-gate.spec.ts` ahora leen `.github/`/`.husky/` con `../../` (viven en `apps/mobile/src/` pero esos ficheros son transversales). **Segundo gap real, en Rust**: `cargo clippy` falló tras el `git mv` con un error de build script inexplicable a primera vista — la carpeta `target/` (gitignored, pero movida igualmente por ser un `git mv` de directorio a nivel de filesystem) traía cacheado un `OUT_DIR` con la ruta absoluta *anterior* al movimiento; resuelto con `cargo clean`. Sin regresiones: 759/759 Vitest, tsc/ESLint limpios, cargo test 5/5, 39/39 Cypress.
  **Backend nuevo (`apps/api/`)**: Java 21 + Spring Boot 3.5.13 + Maven (wrapper `mvnw` generado con el plugin oficial vía un contenedor Maven desechable, sin instalar Maven en local — coherencia con "todo en Docker"). Único endpoint `GET /api/ping` (`PingController`/`PingService`/`PingResult`) que ejecuta `SELECT now()` de verdad contra Postgres — 200 con el timestamp real si hay conexión, 503 controlado si no la hay. Test unitario (`PingControllerTest`, JUnit+Mockito+AssertJ, sin contexto Spring) con TDD real: rojo (error de compilación) confirmado antes de implementar, verde (2/2, `BUILD SUCCESS`) después, ejecutado dentro de un contenedor Maven (`docker run ... mvn test`) al no haber Maven instalado en local.
  **Docker**: `apps/api/Dockerfile` multi-stage, ambas etapas `debian:trixie`/`debian:trixie-slim` — confirmado en un contenedor real que `openjdk-21-jdk-headless`/`openjdk-21-jre-headless` y `maven` (3.9.9-1) existen tal cual en los repos de trixie (`eclipse-temurin`, la imagen oficial de OpenJDK, no publica todavía ninguna variante trixie). `infra/docker/docker-compose.yml` (`postgres:16-trixie` + `api`, `healthcheck`/`depends_on: condition: service_healthy`, volumen persistente). **Tercer gap real**: la propuesta inicial contemplaba credenciales de Postgres inline en el compose (justificadas como "no es un secreto real" por ser solo de desarrollo) — corregido antes de implementar porque la regla del proyecto de "nunca secretos en código" no tiene esa excepción; ahora vía `infra/docker/.env` (no versionado) + `.env.example` (versionado, documenta las claves). **Verificación end-to-end real con Docker** (no solo tests): `docker compose up --build` arranca ambos contenedores; `curl /api/ping` → 200 con timestamp real; `INSERT` manual + `docker compose down`/`up` (sin `-v`) → la fila persiste; `docker stop` de `postgres` con la API arriba → 503 controlado, sin crash.
  **Nueva ADR-032** en `memory/decisions.md` (decisiones de arquitectura: monorepo por `apps/`, Spring Boot sobre `HttpServer` plano, imágenes Debian trixie vía `apt` en vez de `eclipse-temurin`, sin Flyway/Liquibase todavía). Tres nuevas capabilities sincronizadas a `openspec/specs/`: `monorepo-layout`, `api-backend`, `local-dev-environment` (12/12 escenarios con verificación real, ninguno sin ningún tipo de verificación). **Cerrado con `/opsx:archive`: veredicto APPROVED WITH MINOR ISSUES** (`review.md`, verificación independiente con re-ejecución completa de toda la suite — TS, Rust, Java, Docker E2E — no solo revisión de código). Dos issues pendientes no bloqueantes, ambos dependientes de un entorno al que esta sesión no tiene acceso, no de comportamiento incorrecto conocido: (1) el `ci.yml` actualizado tiene cobertura estructural fuerte (`ci-workflow.spec.ts`) pero no se ha ejecutado todavía en un runner real de GitHub Actions — se confirma con la propia PR, mismo criterio que ya advertía ADR-031 sobre los límites de un test estructural; (2) `pnpm tauri android build` no se ha vuelto a ejecutar tras el movimiento a `apps/mobile/` — no obligatorio por criterio del propio proyecto (cambio puramente estructural, no toca GPS/cámara/persistencia/permisos/mapa), pero recomendado como build de humo antes de dar la reorganización por cerrada del todo. Cambio archivado en `openspec/changes/archive/2026-08-03-entorno-api-docker/`. Rama `feature/entorno-api-docker`, sin PR abierta todavía.
- **Sesión 2026-08-02 (6) — CI/CD real en GitHub Actions (`ci-cd-pipeline`, ADR-031)**: hasta ahora el único workflow (`docs.yml`) estaba roto y ningún gate de calidad corría server-side — todo vivía solo en `.husky/pre-commit`, saltable. Segundo cambio OpenSpec real sobre código (`propose→apply→archive` completo). **Un solo workflow `.github/workflows/ci.yml` con 3 jobs** (no 3 ficheros — `needs:` solo funciona dentro del mismo fichero, `workflow_run` habría sido más lento): `quality-ts` (tsc, cobertura de docs, ESLint, Vitest 80%, Cypress E2E) y `quality-tauri` (fmt, clippy, cargo test, cargo audit con la misma excepción `RUSTSEC-2023-0071`) en cada push a `master` y cada PR; `build-and-release` (`needs: [quality-ts, quality-tauri]`, solo en tags `v*`) compila el APK Android y lo publica como asset de un GitHub Release — debug sin firma (decisión confirmada, sin secretos nuevos). Trigger ya estaba bien acotado desde el principio (`push.branches: [master]`, nunca ramas sueltas — lo que parecía "correr en cada rama" era el trigger `pull_request` re-disparándose en cada commit de la PR abierta, comportamiento correcto).
  **Hallazgo real durante `propose`**: `src-tauri/.cargo/config.toml` tenía hardcodeada una ruta de Windows de esta máquina en concreto para el linker del NDK — inservible en cualquier runner. Resuelto sobreescribiendo `CARGO_TARGET_AARCH64_LINUX_ANDROID_LINKER` por variable de entorno del job, sin tocar el fichero (sigue haciendo falta tal cual en local).
  **3 gaps reales encontrados en la primera ejecución de verdad en GitHub Actions** (tag de prueba `v0.0.1-test`, ninguno detectable con el test estructural local — exactamente para lo que sirve verificar en CI real y no solo confiar en el test): (1) `sdkmanager` no está en `PATH` por defecto en `ubuntu-latest` pese a traer el SDK preinstalado — localizado dinámicamente con `find`; (2) `${{ env.ANDROID_HOME }}` en un campo estructurado (`path:` de `actions/cache`) es una expresión inválida si no se declara en un bloque `env:` del workflow — variables de entorno del runner en tiempo de ejecución no cuentan, hay que promocionarlas vía `$GITHUB_ENV` antes; (3) el `GITHUB_TOKEN` automático solo tiene `contents: read` por defecto, `softprops/action-gh-release` fallaba con 403 — añadido `permissions: contents: write` acotado solo al job `build-and-release`. Tras corregir los 3, el tag de prueba compiló, verificó (mismo comando de `memory/context.md` para detectar frontend desactualizado) y publicó el Release correctamente — tag/release de prueba borrados después.
  Test de regresión nuevo `src/shared/ci/ci-workflow.spec.ts` (22 aserciones, texto plano sobre el YAML, sin parser nuevo — mismo patrón que `pre-commit-audit-gate.spec.ts`). **754/754 Vitest, `tsc`/ESLint limpios.** PR #87 (`feature/ci-cd-pipeline` → `master`), todos los checks de CI en verde salvo el `Docs` preexistente sin relación. **Cerrado con `/opsx:archive`: veredicto APPROVED** (`review.md`, 15/15 escenarios verificados — varios con ejecución real en GitHub Actions, no solo estructural). Nace `openspec/specs/ci-cd/spec.md` (segunda spec "oficial" del proyecto tras `security-audit`). Cambio archivado en `openspec/changes/archive/2026-08-02-ci-cd-pipeline/`. Pendiente: mergear PR #87.
- **Sesión 2026-08-02 (5) — flujo de Git explícito (ADR-029) + GitHub Project "Moto Routes" (ADR-030)**: revisión a petición del usuario de qué disciplina se había perdido en la migración a OpenSpec (rama/PR nunca automatizados ni documentados desde entonces — se llegó a ejecutar `propose→apply→archive` de `auditoria-seguridad` directo sobre `master` sin que nada lo señalara; `master` sin branch protection en GitHub). Decisión (confirmada con el usuario, tres niveles de enforcement planteados): **solo disciplina documentada**, sin hook técnico ni branch protection ni CI nuevo por ahora — nueva sección "Flujo de Git y Project" en `CLAUDE.md`/`.clinerules/00-project-rules.md` (idéntica en ambos), rama `feature/<nombre>` + PR obligatorios pero sin gate técnico. Corregida de paso una guía obsoleta en `openspec/config.yaml` que decía que Cypress estaba desactivado en pre-commit (ya no es así). Commit/PR de esta parte: rama `chore/flujo-git-openspec`, PR #86 (abierta, sin mergear todavía).
  **GitHub Project**: creado "Moto Routes" en la organización `crzverde` (dueña real del repo, no un usuario) — [github.com/orgs/crzverde/projects/1](https://github.com/orgs/crzverde/projects/1), requirió ampliar el scope del token (`gh auth refresh -h github.com -s project -s read:project`, login interactivo). Una card por **cambio de OpenSpec, nunca por tarea** (respeta ADR-027, que ya decidió no crear issues por tarea). Campo custom `Fase` (`Backlog`→`Propuesto`→`En progreso`→`En revisión`→`Hecho`) porque el campo `Status` nativo no se puede borrar ni reeditar sus opciones vía `gh` CLI. Cards iniciales: PR #85 (auditoria-seguridad, mergeada) → `Hecho`; PR #86 (flujo-git-openspec, abierta) → `En revisión`; backlog nuevo para la verificación de GPS en background pendiente desde hace sesiones. Limpieza: cerrados los issues huérfanos #53 y #51 (pre-OpenSpec, ya resueltos por trabajo posterior) con comentario explicando el estado real. Detalle completo de alternativas descartadas en [[ADR-030]].
- **Sesión 2026-08-02 (4) — fix crash real de "Grabar" (permiso GPS) + primer cambio OpenSpec end-to-end sobre código (`auditoria-seguridad`)**: dos piezas de trabajo sobre `master` ya con OpenSpec activo.
  **(1) Fix de crash reportado por el usuario**: al pulsar "Grabar" la app crasheaba de verdad (force-close, no solo un error JS) — diagnosticado con `adb logcat` en tiempo real: `RecordingService.kt:78` llama `startForeground(type=location)` sin que Android tuviera el permiso de ubicación realmente concedido (`SecurityException` fatal, Android 14+, `targetSDK=36`). Causa raíz de código real: `createBrowserGpsProvider().checkPermissions()` (`src/cockpit/cockpit.service.ts`) no comprobaba el permiso de verdad, solo `navigator.geolocation !== null` (casi siempre `true`), así que `hasGpsPermission` quedaba `true` de mentira tras un permiso revocado (p. ej. por una reinstalación del APK, que resetea permisos runtime) y el overlay de "pedir permiso" nunca se disparaba. Fix: usa `navigator.permissions.query({name:'geolocation'})` con fallback al comportamiento anterior si la Permissions API no está disponible (jsdom en tests, WebView muy antiguo). Tests de regresión nuevos en `cockpit.service.spec.ts`. Mergeado a `master` junto con el fix ya conocido de `perfil-usuario` (modelos de fabricante mixto) — PR #84, commit `d4d7a81`.
  **(2) `auditoria-seguridad` — primer `/opsx:propose` + `/opsx:apply` real de este proyecto sobre código (no tooling/docs, a diferencia de `migrar-sdd-a-openspec`)**: motivado por un gap real encontrado — `docs/06-seguridad.md` exigía desde hace tiempo que el pre-commit bloqueara en vulnerabilidades `high`/`critical`, pero `.husky/pre-commit` nunca ejecutó ni `npm audit` ni `cargo audit`. **Gap de diseño encontrado durante `apply`** (documentado en `openspec/changes/auditoria-seguridad/design.md`): `cargo audit` no tiene niveles de severidad (`--deny high` no existe, a diferencia de `npm audit --audit-level`) — por defecto ya bloquea en cualquier vulnerabilidad real y no bloquea en avisos `unmaintained`/`unsound`/`yanked`, así que no hacía falta ningún flag para el comportamiento deseado. **Resultado**: `pnpm audit` pasó de 10 vulnerabilidades (7 high, 3 moderate) a **0** — causa real: `vitepress@1.6.4` (última estable, no hay una posterior que lo arregle) arrastra internamente `vite@5.4.21`/`esbuild@0.21.5` viejos pese a que el `vite` de nivel superior del proyecto ya estaba en la versión segura `6.4.3`; corregido con overrides en `pnpm-workspace.yaml` (`vite: ^6.4.3` global, `postcss: 8.5.25` actualizado desde el `8.5.16` que se había quedado atrás, y `brace-expansion` acotado por padre — `minimatch@3>`/`@9>`/`@10>` — para sus 3 líneas major en el árbol sin forzar un major que su consumidor no espera). `cargo audit` quedó con **1 excepción documentada**: `RUSTSEC-2023-0071` (`rsa`, Marvin Attack, sin fix disponible, vía `sqlx-mysql` transitivo de `tauri-plugin-sql` — MySQL nunca se usa en runtime, código inalcanzable), ignorada explícitamente con comentario en el propio hook. Nuevos tests de regresión de configuración en `src/shared/http/` (mismo sitio que ya existía para leer `tauri.conf.json`, no un dominio nuevo): `tauri-conf.spec.ts` ampliado (CSP de `tauri.conf.json` e `index.html` idénticas, sin `unsafe-eval`/`unsafe-inline`) y `capabilities-allowlist.spec.ts` nuevo (permisos de `capabilities/default.json` no crecen sin que un test lo note). Los 4 comandos Rust sin validar (`app_info` y los 4 del foreground service) confirmados sin riesgo — solo reciben `AppHandle`, no input de usuario. Verificación en dispositivo Android real marcada **no aplicable** (confirmado con el usuario): ningún cambio de este audit toca código empaquetado en el APK. **733/733 tests Vitest** (+4 de este cambio), `tsc`/ESLint limpios, `cargo test` 5/5, `cargo clippy`/`fmt` limpios, `pnpm run docs` construye sin errores tras los overrides. Reafirma [[ADR-014]] sin abrir una ADR nueva (no hubo decisión de arquitectura más allá de lo que ADR-014 ya cubre). **Cerrado con `/opsx:archive`: veredicto APPROVED** (`review.md`, 8/8 escenarios con verificación — automatizada o de herramienta real documentada; un gap de cobertura encontrado durante la propia revisión, `pre-commit-audit-gate.spec.ts`, corregido en la misma sesión antes del veredicto). Spec nueva nacida en `openspec/specs/security-audit/spec.md` (primera vez que `openspec/specs/` deja de estar vacío). Cambio archivado en `openspec/changes/archive/2026-08-02-auditoria-seguridad/`.
- **Sesión 2026-08-02 (2) — MIGRACIÓN A OPENSPEC COMPLETADA (cambio `migrar-sdd-a-openspec`, primer `/opsx:propose` real del proyecto)**: el repositorio había quedado describiendo dos metodologías a la vez — el commit `801fdd2` borró `agents/` (la capa agnóstica que leían Cline y Claude) sin reubicar su contenido, dejando ~20 referencias muertas en `CLAUDE.md`, `.clinerules/`, `docs/01`, `03`, `05` y `README.md`, y el criterio de revisión sobreviviendo solo en `.claude/agents/review-agent.md`, es decir solo para Claude. Se arregló con el flujo completo de OpenSpec (`propose` → `apply`), `skip_specs: true` por ser tooling y documentación.
  **`openspec/config.yaml` es ahora el source of truth de la metodología**: `context` (3.036 chars: stack, dominios, Asfalto Nocturno, `data-cy`, seguridad, idioma, `memory/`, `specs/` congelado), `rules` por artefacto (proposal 5, specs 5, design 4, tasks 5) y `operations` (apply 11 guías, archive 8). **Verificado que la CLI lo inyecta de verdad** vía `openspec instructions <artefacto> --change ... --json` — sin un change existente ese comando falla, por eso no se pudo comprobar en la sesión de instalación.
  **Salvaguarda antes de borrar**: se extrajeron las reglas de los 7 subagentes (y de `agents/review-agent.md`, recuperado con `git show 801fdd2^:agents/review-agent.md`, comprobado que es un subconjunto operativo de la versión `.claude/`) y se mapeó cada una a su destino. Resultado en `openspec/changes/migrar-sdd-a-openspec/notas-migracion-agentes.md`: **10 reglas huérfanas rescatadas** como entradas nuevas de `config.yaml` (entre ellas el veredicto `CHANGES REQUESTED`, que faltaba; el gate de cobertura ≥80%, que no estaba en ningún sitio; no instalar dependencias sin confirmar; Rust sin `unwrap()` injustificado) y **9 descartadas** con motivo explícito.
  **Borrado**: 7 subagentes `.claude/agents/*` + 7 comandos `.claude/commands/sdd-*`. `.claude/commands/opsx/`, `.claude/skills/`, `.cline/skills/` y `.clinerules/workflows/` intactos (6 ficheros cada uno). **`CLAUDE.md` 98→39 líneas** y **`.clinerules/00-project-rules.md` 90→43**, con el mismo contenido salvo la sección de Tokens (propia de DeepSeek) y los nombres de workflow — deben mantenerse iguales, si divergen vuelve el problema. **Docs reescritos**: `01-arquitectura-sdd.md` (capas con `openspec/`), `02-workflow-sdd.md` (propose→apply→archive), `03-agentes-skills.md` **sustituido** por `03-configuracion-openspec.md` (incluye la tabla de dónde aterrizó cada agente), `05-memory-system.md` (árboles), `index.md`, `04-token-management.md`, sidebar de `.vitepress/config.mjs` y `README.md` (este último no estaba en el plan — se detectó al borrar el doc 03 y se arregló).
  **`specs/features/` CONGELADO**, no borrado ni migrado: 52 ficheros de 10 features cerradas, se consultan pero no se amplían ni se editan (ni siquiera sus 2 referencias históricas a `agents/review-agent.md`). Aviso en `specs/README.md` nuevo. `specs/ui/` sigue vigente y editable.
  **Sin regresiones**: 719/719 tests Vitest (74 ficheros), cobertura 96.15%, ESLint 0, `tsc` limpio, `cargo fmt`/`clippy -D warnings`/`test` 5/5 OK, `pnpm run docs` construye y cobertura de documentación 72% sobre umbral 70%. Verificado con `git status` que **no se tocó ni un fichero de `src/`, `src-tauri/` ni `cypress/`**.
  **Decisiones tomadas** (detalle en `design.md` del cambio y en la actualización de [[ADR-027]]): (1) se elimina también `review-agent` — trade-off asumido: se pierde la revisión en contexto fresco, con vuelta atrás definida; (2) **no se crean issues de GitHub**, `tasks.md` es la única trazabilidad — las issues abiertas #66-#80 y #53 quedan sin flujo que las cierre, limpieza pendiente aparte; (3) no se forkea el schema de OpenSpec. **Pendiente**: gate de revisión (`/opsx:archive`) y decisión del usuario sobre commit/push. Rama `feat/open-spec`.
- **Sesión 2026-08-01 (4) — ADR-027 ratificada, OpenSpec instalado (worktree `architecture-decisions`)**: el usuario confirmó explícitamente adoptar OpenSpec como framework SDD (ver [[ADR-027]] en `memory/decisions.md`, ahora `Aceptada`). Alcance de esta sesión: **solo tooling, sin migrar ninguna spec** — la propia guía oficial de adopción brownfield de OpenSpec desaconseja migrar specs cerradas como ejercicio de prueba ("write specs only for what you're about to change"), así que se descarta el "spike" original (migrar una feature ya cerrada) y se deja el primer `/opsx:propose` real para la próxima feature de verdad. CLI instalada global (`npm i -g @fission-ai/openspec@latest`, v1.7.0 — ojo, el paquete `openspec` a secas de npm es un placeholder de 2019 sin relación, el real es `@fission-ai/openspec`). `openspec init --tools claude,cline` ejecutado: creó `openspec/{specs,changes,config.yaml}`, `.claude/skills/openspec-*` + `.claude/commands/opsx/*.md` (namespace separado de los `sdd-*` existentes, sin colisión), `.cline/skills/openspec-*`. **Conflicto real encontrado y resuelto con el usuario**: OpenSpec necesita `.clinerules/` como carpeta (para `workflows/opsx-*.md`), pero el proyecto ya tenía `.clinerules` como archivo único (reglas SDD para Cline+DeepSeek, fichero protegido por regla de gobernanza de `CLAUDE.md`) — el init para Cline falló al chocar con eso. Confirmado con el usuario vía pregunta directa: se movió el contenido a `.clinerules/00-project-rules.md` y se re-ejecutó el init solo para Cline, que ahora sí generó `.clinerules/workflows/opsx-*.md` junto al fichero de reglas preexistente intacto. `openspec/config.yaml` → campo `context:` relleno con un resumen del stack/convenciones (no se copió `CLAUDE.md` entero) para que futuros `/opsx:propose` respeten las reglas del proyecto sin repetirlas. Verificado: `openspec doctor` sin errores, `openspec list`/`validate --all` limpios (specs/changes vacíos, esperado — brownfield delta-first). **No se ha tocado** `specs/features/*`, `CLAUDE.md`, el `.clinerules` raíz (solo reubicado, contenido intacto), ni `.claude/agents/`/comandos `sdd-*` — el SDD propio sigue operativo en paralelo, tal como documenta la ADR. Nada de esto está commiteado todavía (worktree `architecture-decisions`, rama `worktree-architecture-decisions` desde `origin/main`) — pendiente de que el usuario decida cuándo commitear/mergear. **Próximo paso real**: el primer `/opsx:propose` se hace sobre la siguiente feature de verdad que se aborde, no como ejercicio aislado.
- **Sesión 2026-08-01 (3) — feature `cobertura-e2e` CERRADO, `review-agent`: APPROVED (45/45 AC)**: la cobertura E2E del proyecto era prácticamente nula (1 spec Cypress roto, sin scripts npm para ejecutarlo). Flujo SDD completo: spec (`specs/features/cobertura-e2e.md`, 45 AC) → plan (16 pasos) → implementación paso a paso con `impl-agent` (TDD estricto en cada paso, con ejecución real de tests, no solo revisión de código) → `review-agent` con verificación independiente. **Pasos 1-8**: reparado `cockpit.cy.ts` (quitado test de "Modo Invisible", retirado 2026-07-27; `data-cy` en vez de selector CSS), scripts npm `cy:open`/`cy:run`/`test:e2e` + `start-server-and-test`, nuevos `data-cy` (`route-card`, `route-list-empty`, `photo-capture-input-file`), y el mecanismo de siembra `cypress-seed-routes` completo: `app-seed.types.ts`/`app-seed.transform.ts` (parseo puro con guardas explícitas)/`app-seed.service.ts` (`applyCypressSeed()`, doble guarda `isTauri()` por defensa en profundidad) + `MemoryRouteRepository.seed()` (carga directa sin pasar por `save()`) + `app.element.ts` reestructurado (decide primero por `isTauri()` en vez de por éxito/fracaso de SQLite) + infraestructura Cypress (`cy.visitWithSeed()` en `cypress/support/commands.ts`). **Pasos 9-11 — comportamiento NUEVO, no solo cobertura**: implementado el límite de 100 fotos por ruta que `fotos-ruta.md` documentaba como Constraint desde su creación pero nunca estuvo cableado (`countByRouteId()` existía sin consumidor de UI); nueva constante `MAX_PHOTOS_PER_ROUTE=100` (`photo-capture.types.ts`), función única `applyPhotoCaptureLimit()` (`photo-capture.limit.ts`) que decide `disabled`/`limitReached` a la vez, nueva propiedad `limitReached` en `PhotoCaptureElement` (mismo patrón que `disabled`/`loading`, texto accesible "Límite de fotos alcanzado" diferenciado del `loading` transitorio), wireada en `route-detail.element.ts` (`buildAddPhotoButton()`) y `cockpit.element.ts` (`refreshGallery()`/`handleDeletePhoto()`). **Pasos 12-16**: 5 specs Cypress (`cockpit.cy.ts` ampliado, `route-list.cy.ts`, `route-detail.cy.ts`, `fotos.cy.ts`, `timeline.cy.ts` nuevos) — **30/30 tests Cypress en verde** en ejecución real headless. Durante la implementación se encontraron y corrigieron **2 gaps reales de infraestructura/código**, no solo de test: (1) `cypress.config.ts` no tenía `includeShadowDom: true` — sin eso, **ningún** test Cypress del proyecto (ni siquiera los 3 ya "reparados" del Paso 1) podía encontrar ningún `data-cy`, porque toda la app usa Shadow DOM y Cypress no lo atraviesa por defecto; los tests fallaban con "elemento no encontrado" pese a que el screenshot mostraba el DOM renderizado correctamente — bug latente desde que existe Cypress en el proyecto, nunca detectado porque nunca se había ejecutado en verde; (2) `<route-detail>` no exponía `data-cy="route-detail-title"` en su `<h1>`, riesgo real de falso positivo al localizar el título por texto con `<route-list>` montado oculto en paralelo con el mismo nombre de ruta — añadido. **567/567 tests Vitest en verde** (549 previos + 18 nuevos de los Pasos 9-11 + regresión de `route-detail-title`), cobertura global 96.68%/90.99% (líneas/branches), `tsc`/ESLint limpios. `review-agent` verificó de forma independiente (no solo aceptando el resumen recibido): re-ejecutó `tsc`/ESLint/Vitest/Cypress él mismo, encontró un gap adicional no reportado (Paso 8 prometía documentar la siembra en `docs/07-cypress-e2e.md` y ese archivo no se había tocado — solo JSDoc en `commands.ts`) y otro de gobernanza (`CLAUDE.md` modificado en el working tree sin estar en ningún paso del plan). Veredicto inicial: **APPROVED WITH MINOR ISSUES**. Ambos issues resueltos en la misma sesión: se añadió la sección "Siembra de datos en Moto Routes" a `docs/07-cypress-e2e.md`; el cambio a `CLAUDE.md` (consolidación de la regla `data-cy` obligatoria, quitando una duplicación real entre "Convenciones de Frontend" y "Tests E2E") se confirmó explícitamente con el usuario vía pregunta directa — se mantiene. Veredicto final tras la corrección: **APPROVED (45/45 AC)**. Documentación: `specs/features/cobertura-e2e.{md,plan.md,review.md}`. **Nada de esto está commiteado a git todavía** — pendiente de que el usuario decida cuándo commitear/pushear.
- **Sesión 2026-08-01 (2) — feature `reorganizar-dominios` CERRADO (issue #52)**: reorganización estructural del frontend aplicando el patrón de `shared/`. `src/routes/` se dividió en `list/` (listado) y `detail/` (detalle + timeline como pestaña). `src/cockpit/` mantiene el núcleo en raíz (`cockpit.element/service/transform/render/types`) y sus servicios se agruparon por sub-responsabilidad funcional: `gps/` (native-gps + foreground), `persist/`, `photo/`, `stop/`, `long-press/`, `save-route-dialog/` — cada `x.ts` junto a su `x.spec.ts`. Refactor puro con `git mv` (git detectó renames 96-100%), sin cambios de lógica/CSS/dependencias ni de `data-cy`. **527/527 tests pasan**, cobertura global 96.15%, tsc/ESLint/Prettier limpias, `cargo test` 5/5. Build APK Android compilado e instalado y **verificado por el usuario en dispositivo real** (grabación, listado, detalle y diálogo de guardado OK). `review-agent`: **APPROVED** (9/9 AC). Documentación: `specs/features/reorganizar-dominios.{md,plan.md,review.md}`. Issues #61-#63 creados como pasos y cerrados; PR #64 mergeado a `master` (commit `5923054`). Commit del refactor cockpit: `7d571bb` (+ `3a52850` review). Rama local eliminada. **Pendiente**: la issue #53 (`specs/features` planos) sigue abierta — la propia spec observa que esa reorganización debe esperar a la decisión de adoptar OpenSpec (PR #50, ADR en curso).
- **Sesión 2026-08-01 — feature `mejoras-visuales-mapa` CERRADO** (`review-agent`: **APPROVED WITH MINOR ISSUES**, informe en `specs/features/mejoras-visuales-mapa.review.md`; commits `d947afe` — Pasos 8-10 — y el commit del review). 39/39 AC cumplidos (35 con test automatizado, 4 de verificación manual en dispositivo real, 4 retirados por decisión de producto). Issues GitHub #54-#60 cerradas con comentario explicando el estado real de cada una (incluida #58, controles de zoom, cerrada como "implementado y luego retirado"). Dos issues de severidad baja no bloqueantes en el review (ROAD_WIDTH_SCALE sin medición formal de contraste, `fullscreenchange` sin filtrar por instancia — sin efecto con el único consumidor actual) quedan anotadas para referencia futura, sin acción pendiente. Retomada tras compactación de contexto — Pasos 1-6 seguían commiteados (`89f4d70`/`f95be5a`) pero el trabajo de una sesión previa había dejado en el working tree (sin commit) el **Paso 8** ya completo: retirada de controles de zoom (decisión de producto, "no me han gustado"), fix del bug real de toggle de pantalla completa (`isElementFullscreen()` comparaba contra `document.fullscreenElement`, que la Fullscreen API retargeta al host del Shadow DOM — corregido leyendo `ShadowRoot.fullscreenElement`), y afinado de contraste (ancho de vía escalado `ROAD_WIDTH_SCALE=0.7` + `text-color` en capas de etiquetas `symbol`, antes ilegibles). Verificado con el usuario que el APK con estos cambios ya estaba instalado y probado — dio **cuatro puntos de feedback nuevos**, resueltos en el mismo turno como **Paso 9**: 1) la atribución OpenFreeMap/OSM (obligatoria por licencia, no se quita — confirmado explícitamente con el usuario vía pregunta directa) se hace más discreta con opacidad 0.35 por defecto y 1 en hover/foco (`.maplibregl-ctrl-attrib-button`); 2) los pines de inicio/fin ahora anclan por su punta inferior (`anchor: 'bottom'` en `maplibregl.Marker`, antes centro por defecto); 3) `ROAD_WIDTH_SCALE` baja de 0.7 a 0.5 (seguía viéndose grueso); 4) los marcadores de foto/cluster ganan una zona de clic invisible de `--hitbox-min` (56px) envolviendo el icono visible (`route-map-photos.ts`, nueva clase `.route-map-marker-hitarea`) sin cambiar su tamaño aparente — mismo enfoque que propuso el propio usuario. Todo hecho con TDD estricto (test RED confirmado antes de cada cambio). Spec (`mejoras-visuales-mapa.md`) y plan actualizados con AC-033 a AC-037 y el nuevo Paso 9, siguiendo el mismo patrón ya usado en el Paso 8 (corrección directa sobre la spec abierta, sin `review-agent` todavía). **526/526 tests TS pasan, `tsc`/ESLint/Prettier limpios.** APK recompilado (`pnpm tauri android build --target aarch64 --debug`) e instalado (`adb install -r`, dispositivo `75fe536b`). El usuario probó el APK y confirmó que los 4 puntos del Paso 9 quedaron bien, salvo un punto nuevo: la atribución (ya con opacidad reducida del Paso 9) seguía apareciendo **expandida** por defecto (mostrando el texto de crédito completo, no solo el icono "i"). Investigado leyendo el código fuente de `maplibre-gl-dev.js`: no es un bug propio — el `AttributionControl` de MapLibre en modo `compact` arranca expandido por diseño (`_updateCompact()` añade la clase `maplibregl-compact-show` + atributo `open` nada más crearse) y solo se colapsa cuando el mapa recibe un evento `drag` real del usuario, cosa que casi nadie hace en un mapa embebido de 200px. Resuelto como **Paso 10** (AC-038/AC-039): nuevo método `collapseAttribution(mapRoot)` en `route-map.element.ts`, invocado en el handler de `load` junto a `applyContrastOverrides`, que quita esa clase y el atributo `open` — replicando exactamente lo que MapLibre hace internamente al arrastrar, sin tocar su lógica de toggle al pulsar (eso sigue intacto). Test nuevo en `route-map.element.spec.ts` que inserta un `<details>` con las clases reales de MapLibre en `.maplibre-root` antes de `triggerLoad()` y confirma que se colapsa. **527/527 tests TS pasan**, tsc/ESLint/Prettier limpios. APK recompilado e instalado de nuevo. **Pendiente**: confirmación visual del usuario sobre este último ajuste antes de commitear (Pasos 8, 9 y 10 siguen sin commit) e invocar `review-agent` para cerrar la spec formalmente.
- **Sesión 2026-07-31 (2) — feature `mejoras-visuales-mapa` EN CURSO (Pasos 1-6/7 del plan implementados y commiteados, Paso 7 a medias)**: spec creada a partir de queja del usuario ("el mapa se ve muy apagado, hay que hacer mucho zoom para distinguir las calles") + petición de botón de pantalla completa integrado. Flujo SDD completo: `specs/features/mejoras-visuales-mapa.md` (29 AC) → `.plan.md` (7 pasos) → issues GitHub #54-#60 (uno por paso) → implementación paso a paso con `impl-agent`. **Pasos 1-6 implementados, testeados (515/515 tests, 0 regresiones, lint/tsc/prettier limpios) y commiteados** (`f95be5a` pasos 1-4: skeleton de carga, atribución OpenFreeMap/OSM reactivada `{compact:true}`, contraste de vías vía `route-map-contrast.ts` con overrides de `paint` sobre `highway_motorway_inner`/`highway_major_inner`/`highway_minor` del estilo `dark` real de OpenFreeMap — confirmado con `curl` que el fondo era casi negro y las vías casi indistinguibles — usando token `--ink-soft`; marcadores inicio/fin como pin SVG con `currentColor`; commit siguiente para pasos 5-6: `NavigationControl` en `top-left` con hitbox 56px, botón de pantalla completa real (Fullscreen API) en `route-map-fullscreen.ts`, aplicado sobre el contenedor exterior — no sobre `mapRoot` — para que el botón siga visible en fullscreen, `map.resize()` tras `fullscreenchange` sin necesidad de recapturar centro/zoom). **Paso 7 (regresión + verificación visual) pendiente de cerrar**: no se pudo verificar visualmente por navegador (extensión Claude-in-Chrome no conectada en este entorno); en su lugar se compiló y instaló el APK Android (`pnpm tauri android build --target aarch64 --debug` + `adb install -r`, dispositivo `75fe536b`) para que el usuario lo pruebe con sus rutas guardadas reales — **verificación pendiente de respuesta del usuario** (contraste real, solape de controles, y sobre todo si el botón de pantalla completa funciona en el WebView de Android, AC-020, la única incertidumbre real de todo el plan). **Próximos pasos al retomar**: 1) recoger feedback visual del usuario sobre el APK instalado, 2) si hay ajustes, aplicarlos y volver a compilar; 3) marcar Paso 7 `[x]` en el plan cuando quede confirmado; 4) invocar `review-agent` para el cierre formal de la spec (no se considera terminada sin `APPROVED`). El usuario avisó que trae más cambios que proponer en la próxima sesión (aún sin detallar).
- **Sesión 2026-07-28 (4) — fix: "Galería" no hacía nada al añadir foto**: bug reportado por el usuario (trabajando en paralelo con Cline) — el botón "Galería" de `<photo-capture>` no seleccionaba ninguna foto ni daba error visible. Causa raíz encontrada en el working tree: Cline había dejado a medias (sin commitear) un puente nativo Android nuevo (`src-tauri/src/photo_plugin.rs` + `PhotoPlugin.kt`, sin trackear; cambios en `lib.rs`/`Cargo.toml`/`AndroidManifest.xml`) que hacía que `pickFromGallery()` (`photo-capture-adapter.service.ts`) bifurcara por `isTauri()` hacia `@tauri-apps/plugin-dialog` → `invoke('copy_photo')` → `readFile`. Ese plugin nunca se instaló (no está en `package.json`), nunca se registró en Rust ni tiene permiso en `capabilities/default.json` — y además `vite.config.ts` (de una sesión mucho más antigua) ya redirigía ese import a un stub local que siempre devuelve `null`, así que la selección fallaba en silencio en vez de dar el error de permisos genérico de Tauri que cabría esperar. La spec `fotos-ruta.md` (AC-022) ya documentaba la decisión correcta desde antes: cámara y galería deben usar `<input type="file">` sin distinción de plataforma, porque el WebView de Android abre igual los selectores nativos — el código de Cline divergía de esa decisión ya tomada. **Fix aplicado**: revertidos los cambios de Cline (`git checkout` sobre `lib.rs`/`Cargo.toml`/`Cargo.lock`/`AndroidManifest.xml`, borrados los ficheros sin trackear), `pickFromGallery()` simplificado para usar `captureFromInput()` sin bifurcar por `isTauri()` (igual que `captureFromCamera()`, que nunca tuvo este problema), eliminado el alias muerto de `@tauri-apps/plugin-dialog` en `vite.config.ts` y su stub (`src/shared/tauri-plugins/plugin-dialog.ts`). Añadido test de regresión explícito (`pickFromGallery (Tauri Android)`) que confirma que con `__TAURI_INTERNALS__` presente se sigue usando `<input type="file" multiple>`. **484/484 tests TS pasan** (1 nuevo), `tsc`/ESLint sin errores, `cargo build`/`clippy -D warnings`/`fmt --check` sin issues. No se ha tocado `specs/features/fotos-ruta.md` porque el código ahora vuelve a cumplir AC-022 tal cual estaba documentado — no hubo cambio de decisión, solo corrección de una regresión no commiteada. **Verificado en dispositivo Android real** (build `aarch64 --debug` + `adb install -r`): "Galería" vuelve a funcionar, confirmado por el usuario. Al hacer `git push` apareció un commit de Cline en remoto (`3ac137e`, pusheado mientras se investigaba) que añadía `READ_MEDIA_IMAGES`/`READ_EXTERNAL_STORAGE` al manifest y `@tauri-apps/plugin-dialog` a `package.json` como intento paralelo — con nota de que "la galería sigue sin funcionar" (diagnóstico previo a este fix). Rebaseado sin conflictos y, con el fix ya confirmado funcionando sin esos permisos/dependencia, se limpiaron en un commit aparte (`chore(fotos)`, principio de permisos mínimos). **Commiteado y pusheado a `origin/master`**: `dc92db6` (fix) + `55f8c14` (limpieza). Feature cerrado.
- **Sesión 2026-07-28 (3) — cierre de `deuda-tecnica-auditoria`**: retomada la sesión anterior. Verificado visualmente en el navegador (`pnpm run dev`, modo web) el resplandor `--amber-glow` (AC-007) — se ve correctamente en el botón "Grabar" del cockpit. AC-003 (formato de fecha) no se pudo verificar contra datos reales en modo web (la BBDD vive en el dispositivo Android, sin rutas guardadas en este entorno) — dado por bueno por criterio del usuario (cambio mecánico de `toLocaleDateString` ya cubierto por tests). Se invocó `review-agent`: veredicto **APPROVED WITH MINOR ISSUES** (ISSUE-001, severidad baja: AC-005 — spinner de `photo-capture` respetando `prefers-reduced-motion` — sin verificación manual documentada; mecanismo en sí correcto). Resuelto en la misma sesión añadiendo 2 tests nuevos a `photo-capture.element.css.spec.ts` que confirman deterministamente que el override global `@media (prefers-reduced-motion: reduce)` de `tokens.css` (con `animation-duration: 0.01ms !important`) llega al CSS resuelto del componente, en vez de una verificación manual en DevTools (frágil de automatizar). **483/483 tests TS pasan** (2 nuevos). Veredicto final: **APPROVED**. Feature `deuda-tecnica-auditoria` cerrado — los 10 AC cumplidos y verificados.
- **Sesión 2026-07-28 (2) — feature `deuda-tecnica-auditoria` (10/10 AC, 9/9 pasos del plan)**: saneamiento técnico puro por SDD/TDD, sin funcionalidad nueva (salvo dos cambios visuales acotados y aprobados de antemano en la spec). **Pasos 1-3 (imports cruzados/ubicación de código compartido, "blast radius" alto — 15+ archivos tocados)**: `formatDuration`/`calculateAvgSpeed` → `shared/utils/format.ts`, `calculateDistance` → `shared/utils/geo.ts` (ambos antes en `cockpit.transform.ts`, importados directamente por `routes/`); `photo-capture.*` movido de `src/photos/` (carpeta eliminada) a `shared/photo-capture/` (mismo patrón que `photo-gallery`/`photo-viewer`); `formatRouteDate`/`buildRouteDisplayName` (nuevo) y `buildDefaultRouteName` (movida) → `shared/utils/date.ts`/`shared/utils/route-naming.ts`, deduplicando el formateo de fecha divergente entre `route-list` (mes corto) y `route-detail` (mes largo, y sin año en el título) — ahora ambos usan `month: 'short'` con año siempre. `detectStop` es la única excepción documentada de import cruzado `routes → cockpit` (depende de `StopDetectionState`), con comentario explícito en ambos extremos del import. **Pasos 4-6 (CSS/tokens)**: `shared/photo-capture/photo-capture.element.css` importa `tokens.css` sin fallbacks hardcodeados; `counter.element.css`/`confirm-dialog.element.css` importan `tokens.css` (antes no lo hacían pese a usar `var(--token)`); nuevo token `--amber-glow: oklch(60% 0.17 45 / 0.45)` sustituye los 6 literales `oklch(...)` hardcodeados en `cockpit.element.css`/`nav-bar.element.css` (ADR-026, referencia ADR-019 — no contradice la prohibición de "neón/glow" al quedar acotado a un token semántico único). **Ajuste no previsto por el plan**: los tests de `*.element.css.spec.ts` requirieron añadir `test: { css: true }` a `vitest.config.ts` (Vitest devuelve `''` para imports CSS por defecto) y comprobar contenido *resuelto* de `tokens.css` (p. ej. `--amber:`) en vez del texto literal `@import`, que Vite reemplaza en línea al procesar `?inline`. **Pasos 7-8 (Rust)**: `src-tauri/src/commands/mod.rs` gana su primer módulo `#[cfg(test)]` (5 tests: `save_file` rechaza absolutas/`..`/contenido vacío, acepta relativas válidas; `greet` rechaza nombre vacío) — `cargo test` pasa de "0 passed" a 5/5; `recording_service.rs` documenta como limitación conocida (comentario en código) que no tiene lógica pura testeable fuera de `#[cfg(target_os = "android")]`. **Paso 9 (cobertura wrappers Tauri)**: `sqlite-photo.factory.ts` 0%→100%, `sqlite-route.factory.ts` 73%→100% (nuevos specs mockeando `Database.load` de `@tauri-apps/plugin-sql`), `photo-storage.service.ts` 74%→98.8% (ramas de éxito/catch de `createPhotoRepository` en Tauri, `buildPhotoMetadata`, mime type `.png`). **481/481 tests TS pasan** (24 tests nuevos netos), cobertura global 95.9%/90.31%/94.48%/95.9% (stmts/branch/funcs/lines). `cargo test`/`cargo clippy -- -D warnings`/`cargo fmt --check` en verde. `tsc --noEmit` y `vite build` sin errores (hubo que ampliar `buildRouteDisplayName` para aceptar `string | null | undefined` en el nombre, ya que `Route.name` es `string | null`). Verificado: no queda ningún import cruzado `routes ↔ cockpit` fuera de la excepción documentada de `detectStop`, ni ninguna referencia residual a `src/photos`. **Pendiente**: verificación visual manual del usuario (antes/después) del resplandor `--amber-glow` (AC-007) y del nuevo formato de fecha en `route-detail` (AC-003) — ambos cambios visuales menores ya aprobados de antemano en la spec, pero sin captura confirmada todavía. AC-001 a AC-010 marcados `[x]` en la spec (a diferencia de `grabacion-rutas` Fase 2, aquí sí son verificables por completo con tests automatizados + compilación). Recomendado invocar `review-agent`.
- **Sesión 2026-07-28 — feature `grabacion-rutas` Fase 2 (AC-011 corregido, AC-020 a AC-023 IMPLEMENTADOS, pendiente de verificación manual — Pasos 12-18/19 del plan)**: se descubrió con datos reales de BBDD que el fix de PR #47 (foreground service) solo mostraba la notificación persistente, pero la captura de puntos GPS seguía dependiendo de `navigator.geolocation.watchPosition()` en el WebView (una ruta real de 64s guardó 1 solo punto). Esta sesión mueve la captura de ubicación a código nativo Android: `RecordingService.kt` ahora usa `FusedLocationProviderClient` (intervalo 1s) con fallback a `LocationManager` si Google Play Services no está disponible (AC-023), maneja `ACTION_PAUSE`/`ACTION_RESUME` sin detener el servicio/notificación, y reenvía cada punto vía `tauri::ipc::Channel` (`RecordingServicePlugin.kt` → `recording_service.rs` → evento Tauri `recording-service://location` → `cockpit-native-gps.service.ts`, nuevo `GpsProvider` que sustituye a `watchPosition()` como única fuente de puntos en Android/Tauri, con `selectGpsProvider()`/`isAndroidTauri()` para no romper web/desktop). De paso se corrigió un bug de duplicación de puntos en pausa/reanudación (`pauseRecordingAction` no llamaba a `loop.stopWatch()`, dejando un watch huérfano). Archivos nuevos: `cockpit-native-gps.service.ts`+spec, `cockpit-foreground.service.spec.ts`, `shared/tauri/commands.spec.ts`. Modificados: `cockpit.service.ts`, `cockpit-foreground.service.ts`, `cockpit.element.ts`, `shared/tauri/commands.ts` (+specs), y en Rust `recording_service.rs`/`commands/mod.rs`/`lib.rs` (nuevo `Channel`, comandos `pause_recording_location`/`resume_recording_location`), y en Kotlin `RecordingService.kt`/`RecordingServicePlugin.kt`/`MainActivity.kt`/`build.gradle.kts` (+ dependencia `play-services-location:21.3.0`). 49 tests TS nuevos, **457/457 tests pasan**, cobertura 94.81%/89.53%/93.7% (líneas/ramas/funciones). `cargo build`/`clippy -D warnings`/`fmt --check`/`test` sin errores en `src-tauri/`. `pnpm tauri android build --target aarch64 --debug` compiló sin errores a la primera e instalado con `adb install -r` en dispositivo real. **IMPORTANTE — esto NO está verificado como funcionando todavía**: falta el Paso 19 del plan (prueba manual de un trayecto real de varios minutos con la pantalla bloqueada, comprobando `route_points` en la BBDD del dispositivo) antes de dar el bug por resuelto — exactamente la misma prueba corta que ya se dio erróneamente por buena una vez con este mismo bug (sesión 2026-07-27) no debe repetirse. No marcar AC-011/AC-020 a AC-024 como `[x]` en la spec hasta completar esa verificación.
- **Sesión 2026-07-27 (3) — feature `timeline-ruta` (21/21 AC, 8/8 pasos del plan)**: implementada por SDD con TDD. Nueva pestaña "Timeline" en `<route-detail>` como 4ª pestaña del `<tab-bar>`, que muestra en orden cronológico Salida, paradas detectadas al vuelo (reutilizando `detectStop()` de `cockpit.transform.ts` con el mismo criterio conservador de 30 puntos ≤3 km/h), fotos intercaladas y Llegada, con velocidad media de cada tramo. Cinco archivos creados: `route-timeline.types.ts` (tipos puros), `route-timeline.transform.ts` (lógica pura: `detectStopsFromPoints`, `formatTimelineTime`/`Coords`/`Speed`, `buildTimelineSegments`, `buildTimelineData`), `route-detail-timeline.ts` (constructor DOM), y sus specs. Tres archivos modificados: `route-detail.element.ts` (4ª pestaña, guarda `_routePoints` completos con timestamp/speed, `refreshAllPanels` al cambiar fotos), `route-detail.element.css` (~100 líneas de estilos con tokens Asfalto Nocturno). 21/21 AC cubiertos con 27 tests nuevos. **419/419 tests pasan**, cobertura >80%. Sin cambios en Rust, SQLite ni dependencias. Build Android compilado (`pnpm tauri android build`), instalado en dispositivo real y **verificado visualmente por el usuario** — pestaña Timeline funcional con datos de rutas ya guardadas. Mergeado a `master` (PR #49).
- **Sesión 2026-07-27 (2) — feature `mejoras-guardado-rutas` (18/18 AC, 11/11 pasos del plan)**: nombre de ruta al guardar, notas editables, fix nav-bar. Mergeado a `master` (PR #48).
- **Sesión 2026-07-27 — fix: GPS no grababa en segundo plano**: foreground service real vía JNI. Mergeado a `master` (PR #47).
- **Fase**: APK Android compilado y funcional, con persistencia de rutas y fotos verificada en dispositivo real. Monorepo con backend propio (`apps/api`, Go) en producción real vía Tailscale — primera integración real `apps/mobile`→`apps/api` en curso de cierre (`catalogo-tipos-parada`).
- **Feature activo**: `catalogo-tipos-parada` (ver sesión 2026-08-04 (2) arriba) — cerrada y archivada, **PR #93 abierta** (`feature/catalogo-tipos-parada` → `master`, CI en marcha, sin mergear todavía). `migrar-api-golang` y `despliegue-servidor-tailscale` (ambas ver sesión 2026-08-04 arriba) cerradas y mergeadas a `master` — `apps/api` corre en Go en producción real vía Tailscale. `entorno-api-docker` (2026-08-03) mergeado (PR #88). Features anteriores a esa fecha: ver sus propias entradas de sesión más abajo para el estado detallado de cada una en su momento — no reauditadas en esta actualización.
- **Sesión 2026-07-31 — fix: cronómetro se quedaba atrás en segundo plano**: `elapsedTime` se calculaba incrementando un contador en cada tick de `setInterval(1000ms)` — mismo mecanismo que Chromium pausa/retrasa en segundo plano (por lo que el GPS ya usa eventos nativos). Distancia/velocidad venían de puntos GPS reales con timestamp correcto, pero la duración se quedaba muy corta → velocidad media disparada (~500 km/h reportado en una ruta real). Fix: `elapsedTime` ahora se deriva del reloj de pared (`Date.now()` menos tiempo en pausa) tanto en el tick como al llegar cada punto GPS (`computeElapsedSeconds()` en `cockpit.service.ts`), autocorrigiéndose si el intervalo se retrasa. 484/484 tests TS pasan, `tsc`/ESLint sin errores. Commiteado y pusheado a `origin/master` (`48c25ec`). **Pendiente igual que con el fix de GPS**: no dar por resuelto sin una prueba real de trayecto largo con pantalla bloqueada — build Android en curso para esa prueba.
- **Limpieza de GitHub (2026-07-31)**: cerrados 15 issues obsoletos (#13-17 Fases persistencia-rutas, #33-42 Pasos fotos-ruta) — trabajo ya mergeado en `master`, issues nunca cerrados tras el merge. Mergeado PR #50 (ADR: adoptar OpenSpec en vez de SDD custom, propuesto por `viictoryraves`) — decisión de arquitectura pendiente de discutir en detalle.
- **AC-017/AC-018/AC-019 de `grabacion-rutas` cerrados como NO implementados (2026-07-31)**: `grabacion-rutas.review.md` los marcaba erróneamente como "✅ Cubierto" citando solo el test del algoritmo puro (`detectStop`); en realidad nunca se persiste ninguna parada (`buildStops()` en `cockpit-persist.service.ts` devuelve siempre `[]`, aunque la tabla `route_stops` y el repositorio ya soportan guardarlas). Corregido el review y añadida nota en `grabacion-rutas.md`. Decisión: no se implementa aquí — la persistencia real de paradas (con diccionario de datos, migraciones, etc.) se abordará en una **spec nueva y dedicada** sobre paradas.
- **Sesión 2026-08-02 — feature `documentacion-codigo` CERRADO, `review-agent`: APPROVED (19/19 AC)**: el proyecto no tenía documentación del código ni normas que obligaran a los agentes (Cline/Claude) a documentar. Se implementó la Opción 1 completa: **TypeDoc** (API reference en `docs/api/` con `typedoc-plugin-coverage`, `entryPointStrategy: expand` sobre `src/`), **VitePress** (sitio navegable en `docs/` con home, Guías 01-07, ADRs, Design System y enlace a la API), **`cargo doc --no-deps`** (doc Rust en `src-tauri/target/doc/`), y **`eslint-plugin-jsdoc`** como regla obligatoria (`jsdoc/require-jsdoc` error, `publicOnly: true`, `*.spec.ts` exentos) — cualquier export público sin JSDoc bloquea el pre-commit. Los agentes quedan informados en `.clinerules` y `CLAUDE.md`. **Gate de cobertura de documentación al 70% (242/342 símbolos)** con script propio `scripts/docs-coverage.mjs` (el plugin v4 no expone `coverageThreshold`). Sitio CI en `.github/workflows/docs.yml`. **Sin regresiones**: 567/567 tests Vitest, lint 0 errores, clippy/fmt/test Rust OK, Cypress 30/30. Detalles: `pnpm run docs` genera todo (⚠️ usar `pnpm run docs` no `pnpm docs` — colisiona con comando nativo de pnpm); override `postcss: 8.5.16` en `pnpm-workspace.yaml` (pnpm v11: los overrides van ahí, no en `package.json`); VitePress rompía el build con `invoke<T>()` de los ADRs — `docs-prepare.mjs` escapa genéricos TS. Commits en `feature/documentacion-codigo`: `e3576c5` (infraestructura + JSDoc exports) + `8da27c6` (umbral 70% + doc de tipos/dominio). Documentación: `specs/features/documentacion-codigo.{md,plan.md,review.md}`.
- **Último push (2026-07-31)**: `48c25ec` (fix cronómetro) pusheado a `origin/master`.

## Desarrollo Web (Vite)
Desde `entorno-api-docker` (2026-08-03), todos los comandos de esta sección y de "Build Android" se ejecutan **desde `apps/mobile/`**, no desde la raíz del repo (monorepo — ver [[ADR-032]]). Las rutas de fichero mencionadas más abajo (`src-tauri/...`, `dist/...`) son relativas a `apps/mobile/`.

Para lanzar el proyecto en modo web (sin Tauri), usar:
```bash
cd apps/mobile
pnpm run dev
```

**Puerto fijo**: `1420` (configurado en `vite.config.ts` con `strictPort: true`).

## Build Android
```powershell
cd apps/mobile
pnpm tauri android build --target aarch64 --debug
adb install -r src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk
```

**Usar siempre este comando del CLI de Tauri — nunca `cargo build --target aarch64-linux-android` manual.** `scripts/build-apk.ps1` (que hacía justo eso, con copia manual de `dist/` a `app/src/main/assets/` y un `tauri.conf.json` "standalone" propio) se eliminó el 2026-08-02: su premisa era falsa. `tauri::generate_context!()` embebe `devUrl`/`frontendDist` en el binario Rust **en tiempo de compilación**, leyendo siempre `src-tauri/tauri.conf.json` (el real, con `devUrl: "http://localhost:1420"` para poder usar `pnpm tauri dev`). Un `cargo build` en profile `dev` (sin `--release`) hace que Tauri prefiera `devUrl` sobre `frontendDist` en tiempo de ejecución **sin importar qué JSON haya en la carpeta de assets del APK** — ese archivo no se relee en runtime. Síntoma exacto si esto pasa: la app abre con fondo negro y el texto `Failed to request http://localhost:1420/ — error sending request for url (http://localhost:1420/)`. El comando del CLI (`pnpm tauri android build --debug`) sí evita esto correctamente (usa `frontendDist`), es la única vía soportada.

**Gotcha del CLI**: cada `pnpm tauri android build` resetea `src-tauri/gen/android/app/tauri.properties` (`tauri.android.versionCode`) a `1000`. Si el dispositivo ya tiene instalado un `versionCode` mayor (de una build anterior), `adb install -r` falla con `INSTALL_FAILED_VERSION_DOWNGRADE`. No hay forma de evitarlo sin tocar el CLI — si pasa, `adb uninstall com.motoroutes.app` (pierde datos locales de prueba del dispositivo) y reinstalar.

**Gotcha grave (2026-08-02) — el build puede empaquetar frontend desactualizado sin avisar**: en builds sucesivas donde solo cambia el frontend (sin tocar Rust), `pnpm tauri android build` a veces **no vuelve a copiar el `dist/` recién generado dentro del APK** — el paso de `pnpm build` (`beforeBuildCommand`) sí se ejecuta y `dist/` queda correcto, pero el APK final sigue conteniendo el bundle JS de una build anterior (verificado con hashes de archivo distintos entre `dist/assets/index-*.js` y lo que de verdad contenía el APK). No hay ningún error ni warning — el build "termina bien" y el APK se instala sin problema, pero ejecuta código viejo. **Siempre verificar el contenido real del APK antes de dar una build por buena**:
```bash
unzip -p src-tauri/gen/android/app/build/outputs/apk/universal/debug/app-universal-debug.apk assets/index.html
```
Comparar el hash del `<script src="/assets/index-XXXX.js">` con el de `dist/index.html` recién generado — si no coinciden, el APK está desactualizado. Fix que funciona de forma fiable: copiar `dist/*` a mano a `src-tauri/gen/android/app/src/main/assets/` (`rm -rf .../assets/assets && cp -r dist/assets .../assets/assets && cp dist/index.html .../assets/index.html`) y luego reempaquetar SOLO con Gradle, saltando las tareas Rust (que fallan fuera del wrapper del CLI de Tauri, ver gotcha siguiente):
```powershell
cd src-tauri/gen/android
.\gradlew.bat assembleArm64Debug -x :app:rustBuildArm64Debug -x :app:rustBuildArmDebug -x :app:rustBuildX86Debug -x :app:rustBuildX86_64Debug
```
El APK queda en `app/build/outputs/apk/arm64/debug/app-arm64-debug.apk` (ruta distinta a la del build vía CLI, que usa `apk/universal/debug/`). Reverificar con el mismo comando `unzip` antes de instalar. **No dar nunca una verificación visual del usuario por buena sin haber confirmado primero, con este comando, que el APK instalado contiene el código que se cree que contiene** — en esta sesión se le pidió al usuario que probara una build que en realidad era una versión vieja, y confirmó "funciona" sin haber probado el cambio real.

**Corrección (2026-08-07, sesión `rutas-en-la-nube`) — el "fix fiable" de arriba (copiar `dist/` a mano + reempaquetar solo con Gradle) NO fue fiable esta vez.** El `unzip` del APK confirmaba el JS nuevo (`index-BeQkRJd_.js`), `adb install -r` decía éxito, y hasta forzar `am force-stop` + borrar `app_webview`/`cache`/`code_cache` del propio paquete (`adb shell run-as com.motoroutes.app sh -c 'rm -rf ...'`) + relanzar — la app seguía ejecutando el JS de la build anterior. La verificación por `unzip` del APK **no es suficiente**: confirma qué hay empaquetado, no qué sirve de verdad el WebView de Tauri en runtime. Verificación real vía Chrome DevTools Protocol contra el WebView (mucho más fiable que `unzip` + capturas de pantalla):
```bash
adb shell cat /proc/net/unix | grep webview_devtools   # localiza @webview_devtools_remote_<pid>
adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>
curl -s http://localhost:9222/json                     # lista páginas, cada una con su id/webSocketDebuggerUrl
# con Node 18+ (WebSocket global), Runtime.evaluate sobre ese id:
#   document.querySelector('script').src  → confirma qué bundle sirve de verdad
```
Solo el build completo de la CLI (`pnpm tauri android build --target aarch64 --debug`, con Rust incluido, sin `-x`) hizo que el WebView sirviera el JS nuevo. **Hasta no tener una explicación mejor, tratar el atajo "solo Gradle" como no confiable para cambios de solo-frontend y usar siempre el build completo de la CLI, verificando con CDP (no solo con `unzip`) antes de dar una build por buena.**

**Corrección adicional, misma sesión — ni siquiera el build completo de la CLI es fiable por sí solo, cada vez.** En la misma sesión, repetidas veces (no una excepción puntual): `pnpm tauri android build` terminó "bien" pero el `assets/index.html` empaquetado en el APK seguía referenciando el hash JS de la build ANTERIOR (`dist/index.html` sí tenía el hash nuevo — confirmado con el mismo comando `unzip` de más arriba). Rutina que sí funcionó, de forma consistente, cada vez que se repitió: (1) build completo de la CLI, (2) comparar `grep -o 'index-[A-Za-z0-9_-]*\.js' dist/index.html` contra el mismo grep sobre `unzip -p .../app-universal-debug.apk assets/index.html`; si no coinciden, (3) copiar a mano `dist/assets`→`.../app/src/main/assets/assets` y `dist/index.html`→`.../assets/index.html` (mismo comando que el "fix fiable" de más arriba), y (4) volver a lanzar el build completo de la CLI una segunda vez (no basta con Gradle-only, ver corrección anterior) — esa segunda vuelta sí empaqueta bien. **Tratar esto como el procedimiento normal, no como una excepción: verificar el hash después de cada build y repetir el ciclo copiar+rebuild hasta que coincida, antes de instalar.** Verificación de que el WebView del dispositivo sirve de verdad lo esperado (no solo que el `.apk` lo contenga) vía Chrome DevTools Protocol:
```bash
adb shell cat /proc/net/unix | grep webview_devtools   # localiza @webview_devtools_remote_<pid> (pid cambia en cada relanzamiento)
adb forward tcp:PUERTO localabstract:webview_devtools_remote_<pid>
curl -s http://localhost:PUERTO/json                    # lista páginas — puede devolver vacío/colgarse la primera vez tras instalar, reintentar
# con Node 18+ (WebSocket global) contra el webSocketDebuggerUrl de la página "type":"page":
#   Runtime.evaluate con awaitPromise:true, expresión async con su propio try/catch
#   y un setTimeout de proceso — el canal CDP se queda colgado o se cae sin previo
#   aviso tras uso intensivo; si un eval trivial (`1+1`) no responde en unos segundos,
#   no seguir insistiendo con la misma conexión: `adb forward --remove-all` y
#   reabrir desde cero (nuevo `adb forward` + nueva conexión WebSocket).
```
Con la página cargada, `document.querySelector('script').src` confirma el bundle real servido; `elemento.getBoundingClientRect()` sobre elementos dentro de un Shadow DOM (`customElements` de esta app) permite medir alineación real en píxeles sin depender de capturas de pantalla ni de que el dispositivo esté desbloqueado — más rápido y más preciso que pedirle al usuario una captura para un ajuste visual fino.

**Gotcha adicional (2026-08-02, misma sesión) — el JBR de Android Studio se autoactualizó a mitad de sesión y rompió el build**: `pnpm tauri android build` empezó a fallar con `A problem occurred configuring project ':buildSrc'. > 25.0.2` / `Caused by: java.lang.IllegalArgumentException: 25.0.2` en `JavaVersion.parse` — el parser de versión de Java del plugin Kotlin DSL (bundlado con la versión de Gradle/AGP de este proyecto) no reconoce Java 25. Causa: `C:\Program Files\Android\Android Studio\jbr\bin\java.exe` (usado como `JAVA_HOME` en todos los builds de esta sesión) se autoactualizó de una versión más antigua a `25.0.2` mientras la sesión estaba en curso — build que llevaba horas funcionando con el mismo comando exacto de repente rompió sin que cambiara nada del proyecto. Limpiar `.gradle`/`app/build` **no lo arregla** (no es un problema de caché). Fix: usar un JDK más antiguo como `JAVA_HOME` en su lugar — `C:\Program Files\Java\jdk-24` (24.0.1) funcionó. Si ese tampoco existe, instalar cualquier JDK 17-21 y apuntar `JAVA_HOME` ahí. Si un build que antes funcionaba empieza a fallar con un error de parseo de versión de Java sin explicación, comprobar primero `& "$env:JAVA_HOME\bin\java.exe" -version` — puede haberse actualizado solo.

## Convenciones
- **Estilo de código**: TypeScript strict mode + ESLint strict + Prettier
- **Commits**: Conventional Commits
- **Ramas**: feature/<nombre> desde main
- **Nombrado**: Carpetas en kebab-case, clases/componentes en PascalCase, funciones en camelCase
- **Idioma**: Docs y specs en español, código en inglés